| 版本 | 定版时间（UTC） | 变更摘要 | 交付方式 |
|---|---|---|---|
| v1 | 2026-09-18T06:35Z | 首版：移动 Web/H5 拍照搜题页。getUserMedia 取流实时预览、canvas 框选（8 手柄 + 整体拖动 + 任意方向重新框选 + 三分网格）、cover 映射精确裁剪、最长边/JPEG 质量/PNG 压缩导出、可配置后端地址提交 `/api/agent/recognize`、识别文本可编辑确认、失败与 503 如实展示并切标注过的演示兜底、相册导入同走框选、本地识别历史、相机与安全上下文自检 | 单文件URL版 |
| v2 | 2026-09-18T07:20Z | 并入「AI 视频解析」视图（顶部入口切换，进入即释放相机）。关键词输入 + 快捷词 + 最近搜索；Fetch 调用可配置接口（标题/地址字段名可自定义，兼容 items/videos/results/list/data 等返回结构）；动态创建 `<video>` 内嵌播放，支持切换条目时释放上一路、静音自动播放降级与「开启声音」；播放失败按 error.code 给出具体原因；拦截非 http(s) 协议地址并对 https 页面下的 http 视频做混合内容预警；未配置接口或请求失败时退回内置示例数据并明确标注为公开占位素材、与高数无关。修复三处缺陷：相机启动并发守卫被 stopStream 提前复位、取景与视图切换重复取流、占位层过期引用导致与视频叠加 | 单文件URL版 |
