# 高数学习教育智能体 · 数伴

当前本地分支已实现教师工作空间 v0.3：以真实的账号、班级成员、按班发布和提交统计替换教师端预设演示数据，保留 C 的题库、作答版本、人工反馈以及 B 的模型兼容接口。这里的“真实”指操作和数据实际持久化，不表示已经上线或验证了真实教学效果；未配置模型时，智能体仍明确显示演示内容。

## 当前状态（2026-09-21）

- 合并状态：`codex/c-local-teaching`与`main`已通过[PR #3](https://github.com/844346518scp-coder/agent-school-llm-usts/pull/3)完成整合（合并提交847f1b3），保留教师v0.3与拍照入口。82项Python、14项Node、8项回填/历史脚本检查及前端构建通过；模型/OCR超时等既有失败仍见[测试报告](docs/simulation-test-report.md)。
- 免费模型续测：官方Qwen3-VL-2B已在本机CPU真实运行，短请求返回正确极限值；应用带课程资料的Q01/Q02超时降级、两张题图识别超时502，完整接入未通过。模型仅是忽略缓存中的测试环境，不随源码下载、未加入默认启动。见[测试报告](docs/simulation-test-report.md)。
- 首次模拟验收（demo阶段）：82项Python、14项Node、构建、演示冒烟及18场景HTTP联动通过，师生页面闭环已跑通；双端操作出现SQLite锁冲突，当时真实AI缺配置，拍照入口/知识点弹窗未实现，尚不能判定整体通过。见[测试报告](docs/simulation-test-report.md)及[12章题单/OCR题干](docs/simulation-test-cases.md)。本轮只增加测试和文档，未修复业务、未合远端。
- 已发布基线：0.2 B/C 集成版已通过 PR #2 进入 main；题库、作业、人工反馈、SQLite v2 及演示问答有历史验收记录。
- 教师v0.3基线：首次建档、学生账号、多班级、发布快照和SQLite v3由`8799cd0`实现；后续界面及测试文档由`7877799`、`cfd1025`上传C分支，现与main整合。本地工作分支仍为`codex/teacher-workspace`。
- 教师界面：题库内嵌作业编辑，按同济《高等数学》第八版上下册12章顺序分组，直接勾选题目；作业合入班级管理，成员独立显示，教师侧栏从7项减为4项。实施与验收状态以 [最新开发日志](docs/development-log.md) 为准，不沿用0.2测试结果为新功能背书。
- 拍照入口：已整合main的`PhotoSearchDialog.vue`，与对话记录并列；识别文本经确认后追加提问，超过2000字会提示并保留两处文本。main已有的独立原型目录及`PCL.exe`原样保留，应用启动不使用这些附件。
- 仍未完成：正式身份体系/单点登录、批量导入、共同授课、AI 反馈复核、PostgreSQL / pgvector、审计与公网部署。完整真实模型接入尚未通过，拍照真机、另一台 Windows 实机和真实教学效果尚未验收；独立视频原型没有对应后端接口。
- 9/21验证：14项Node测试及前端构建通过，新增章节分组、自动topic和批量成员操作回归；隔离页面验收与限制见日志。
- 9/20验证：10项Node测试（含4项新增章节/选题回归）、TypeScript及生产构建通过；隔离数据库页面验收见日志。未重跑Python全套、源码冷下载安装或便携打包。
- 9/19验证：完整81项回归、随后4项针对性补强（现共82项，未全量重跑）、CSV的6项Node测试、B演示冒烟、前端构建、实际旧库副本迁移和师生浏览器闭环通过。当前本地库已升至v3，自动备份和原字段无损核对完成；曾触发迁移的旧测试隔离问题已修复，完整重跑前后实际库哈希不变。CSV内容与下载清理逻辑已验证，但应用内浏览器未捕获下载完成，落盘及Excel兼容尚未验收；新源码全量冷下载及便携包未重验，详见日志。

## 下载源码后直接运行（Windows）

1. 下载**包含本次修复的分支**源码 ZIP，完整解压到可写目录，例如 `D:\Shuban`。支持中文和空格路径；不要在压缩软件内直接打开启动文件。
2. 双击根目录 [一键启动.cmd](一键启动.cmd)。**首次需要联网**：脚本自动下载项目专用 Python、Node.js，校验 SHA256，安装锁定依赖并构建网页。可能需要数分钟，请保留窗口等待完成。
3. 浏览器就绪后自动打开。优先使用 `http://127.0.0.1:18080/`；被占用或被 Windows 预留时会自动选择可用端口并记住，以实际打开的网址为准。
4. 此后仍双击同一文件。环境、依赖和源码未变时复用本地结果；演示功能无需联网。远端模型调用需要网络和本地配置；本机模型服务需另行准备，不由此启动器安装。

**无需预先安装 Python、Node.js、npm 或数据库，不需要管理员权限，不修改系统 PATH。** 下载的环境位于被忽略的 `.runtime/`，前端依赖和构建结果分别在 `frontend/node_modules`、`frontend/dist`。这就是普通源码文件夹的自动初始化，无需另取便携包。

自动初始化面向 **Windows 10/11 Intel/AMD 64 位**。Windows ARM、Mac/Linux 未提供此自动安装脚本；学校的应用白名单、PowerShell禁用或网络限制仍需管理员处理。首次完全离线且没有缓存时不能安装环境。

常用参数，在源码根目录执行：

```powershell
# 只准备环境，不启动后端、不打开页面
./start.ps1 -SetupOnly
# 自动准备并运行，不打开浏览器
./start.ps1 -NoBrowser
# 指定端口（被占用时明确报错，不关闭其他进程）
./start.ps1 -Port 18081
```

每个文件夹的安装和启动分别有锁，避免重复双击造成同时安装或重复进程。关闭网页不会结束后端；重启电脑会结束。需要停止时，可在任务管理器按启动输出的后端 PID 核对并结束该项目进程，勿批量结束所有 Python。

## 首次失败怎么排查

- **下载失败/校验失败**：按窗口中的文件和网址检查网络，再次双击重试。已通过校验的下载会复用，未校验的内容不会执行。运行时源为 python.org、nodejs.org；pip源为 files.pythonhosted.org，包安装还需访问 PyPI 和锁文件列出的 npm 下载地址。
- **前端依赖被占用**：先停止这个文件夹正在运行的 Vite/构建进程，再次启动；安装器会尽可能提前检查，不主动关闭其他程序。
- **后端失败**：查看 `backend/server.err.log`；启动失败不等于数据已删除。端口冲突可去掉 `-Port` 自动选择。
- **空间不足**：保持项目盘有足够空间（建议至少2GB）。安装临时文件与npm/pip缓存放在项目 `.runtime/`，避免只依赖C盘空间。
- **旧启动器仍提示手动npm ci**：说明仍在使用旧源码。请重新下载最新main源码并完整解压；旧ZIP不会自动更新。

详细机制、已验证范围和限制见 [源码启动说明](docs/source-startup.md)。

## 可选：桌面窗口入口

除浏览器方式外，仓库根目录另有 `一键启动(桌面窗口).cmd`：双击后同样先准备项目内环境，然后打开一个**没有地址栏、没有标签栏**的应用窗口（Edge 或 Chrome 的 app 模式）。网页版入口 `一键启动.cmd` 行为不变，两个入口共用同一个后端服务、端口文件和目录锁，可以混用。

- 需要本机有 Microsoft Edge 或 Google Chrome；不需要安装任何新依赖，也不需要额外联网。
- 后端仍在后台运行：关闭窗口不会结束服务，与网页版一致。
- 该入口不参与便携 ZIP 打包，也不提供安装程序或桌面安装包。pywebview 方案经实测在当前项目内嵌 Python 运行时不适用，原因见开发日志 2026-09-21。

## 首次使用与账号

全新数据库默认不创建公开演示账号。启动后先创建第一位教师，填写姓名、用户名和自定密码；已有数据则继续登录原账号，不会覆盖旧数据。教师依次进入“班级管理 → 新建班级 → 班级成员 → 添加学生”，创建学生并分配临时密码，学生首次登录必须改密；教师可以停用自己创建的学生账号、重置临时密码，也可把自己管理的已有学生加入其他班级。

教师可以在账号设置修改姓名/密码、创建另一位教师；教师之间班级、题库、作业和学生账号管理权限隔离。当前是本地账号管理，不是学校实名认证、管理员体系或公网安全方案。新密码至少10位并包含字母和数字，密码只保存摘要；重置/改密撤销旧会话。

需要旧演示数据时，显式在本地环境设置 `SHUBAN_SEED_DEMO=true` 后启动。该开关用于隔离演示与测试，默认关闭；关闭开关不会删除已有演示数据。旧库迁移仍保留以下公开账号，因此不要直接开放旧库到公网。

| 身份 | 账号 | 密码 |
|---|---|---|
| 学生 | student | Student123! |
| 教师 | teacher | Teacher123! |

这些凭据只对应显式演示种子或旧库，不是新库的默认登录方式。个人对话按账号隔离；教师不能查看学生私人对话。不要使用真实学生隐私进行开发验收。

## C 教师闭环验收

教师建档/登录→班级管理创建班级与学生→班级卡片“布置作业”→“从题库选题”按章选择、跨章组合与调整顺序→返回完善要求/日期，保存草稿或发布→学生首次改密后提交→教师人工反馈→学生修改→教师复核新版→归档。

题库不再占用侧栏，但新建、编辑、归档题目及私有参考答案仍在选题窗口内。按上下册第1–12章顺序展示有题目的分组，自定义章节排在后面，不再先选章节再选题；这里内置的是分类和选题入口，不是自动导入教材习题。目录依据见[架构与教材来源](docs/architecture.md)。选题只追加标题/题干，不覆盖已填要求，不带入参考答案；每批最多30题，总题干不超过3000字符。

作业表单不再要求选择“所属章节”，截止日期保留。首次空白题干选同章题时自动使用该章，跨章或与现有不同章节内容混合时使用“综合练习”；手写新作业默认综合练习。未重新选题的旧草稿保留原有非空topic。

班级卡片“查看作业”进入本班草稿/已发布/已归档列表；“班级成员”进入独立成员与进度页，可返回班级管理。未分班草稿可从“全部作业与草稿”继续编辑。教师侧栏仅保留教学概览、班级管理、教学助手和账号设置。

空草稿可保存但不可发布；发布班级必须属于教师且至少有一位有效学生。发布时固定收件人，后加学生不自动收到旧作业；需要时复制并重新发布。已发布班级/题干锁定，可延期或复制新草稿。参考答案不进入学生响应。修改作答产生新版本，旧反馈不沿用；退班学生保留原作业只读历史，停用账号不能登录，归档班级下作业不可提交/批改。

班级进度、概览和 CSV 使用实际成员/收件快照、提交及人工反馈。应交是发布收件人数量，不因后来退班/停用而缩小；未交为应交减已交。默认总览排除草稿、归档作业及归档班级，不推断掌握度。验收使用合成账号，不能写成真实师生试用。

成员行仅显示“设置”，点击后改姓名、重置密码、移出/恢复或停用/启用账号，原权限不变。左上角“批量操作”开启勾选，支持移出/恢复成员；全选仅针对当前可见列表，切换筛选/页面或刷新会清空勾选。确认弹窗显示名单与人数，逐人执行，首个请求失败时停止后续操作并提示核实刷新结果；成功操作不回滚，超时不自动重试。批量仅改变本班成员身份，不停用账号、不影响其他班级，不是批量导入。

CSV仅导出当前班级的搜索和成员状态筛选结果，采用UTF-8 BOM、引号转义和公式前缀防护；包含学生账号、姓名与进度，请妥善保管。下载/Excel打开仍需在标准浏览器补验，不能仅依据生成测试认定落盘成功。

## 对话记录入口

在“教学助手”聊天顶部点击“对话记录”，可查看全部历史、搜索问题或章节、筛选已收藏和刷新；点击记录回到当前聊天查看原回复。桌面“最近的问题 → 全部记录”也可进入，小屏保留顶部入口。打开/关闭记录不会清空未发送内容，“新问题”才清空。新的提问仍独立处理，不自动带入历史上下文。学生原有学习记录/收藏导航保留，共享智能体页面也可使用此入口。

## B 智能体模式与边界

可将 `.env.example` 复制为根目录 `.env`，填写自己的配置。真实密钥只保存在本地，禁止提交。

- `AGENT_MODE=auto`：配置 `MODEL_BASE_URL`、`MODEL_API_KEY`、`MODEL_NAME` 后使用模型，否则演示；`demo` 明确禁止模型调用，含识别接口。`live` 缺配置时仍如实回到演示。
- 兼容 Chat Completions；`MODEL_BASE_URL` 可为服务的 `/v1` 地址或完整 `/chat/completions`。可选 `MODEL_VISION_NAME` 指定视觉模型。
- 问答页面和历史按每条回复显示模型/演示来源，展示检索引用与待复核状态。调用失败时明确降级，提示随回复保存。原有历史继续兼容读取。
- 模型网络超时默认30秒，配置限制为1–60秒；前端AI请求等待90秒。异常不会自动重试，接收失败应先查历史，避免重复保存。
- 本地检索是内置课程片段的BM25，资料仍待课程复核；不是已完成的课程上传、向量库或pgvector。
- 当前检索仅7个知识点、2个主题，与教师题库12章分类独立。9/21合成测试发现积分/级数错误建议导数主题，识别请求和返回格式校验亦有缺口；`model.ready`只表示配置齐全，不证明服务可用。完整发现与后续实测条件见测试报告。
- 步骤/诊断中的关键词只用于生成候选与自检提示，不能凭关键词确定学生答错或推断知识掌握。规则步骤反馈统一为`unclear`。
- B的流式问答、识别、诊断和步骤反馈保留后端接口。当前智能体页已有拍照搜题入口，识别文本可编辑并在确认后回填提问框；须配置可用视觉模型，真机与真实视觉质量仍待验收。步骤反馈和诊断页面入口仍待完成；建议知识点目前只是主题标签，识别结果不能直接作为已核实题干。
- 长期记忆、异步任务、语音、自动评分、正式身份体系、PostgreSQL与公网部署尚未完成。本地多账号/多班级不等于这些能力已经具备；师生试用及效果验证仍为暂定。

## 数据与更新

默认数据保存在 `backend/demo.db`（文件名为兼容保留，不表示内容都是演示数据），源码模式遵循 `.env` / `DATABASE_URL`。教师工作空间迁移目标为 schema v3，仅支持 SQLite。旧库先备份后事务升级，为各教师建立历史教学班并保留旧可见范围、作答和反馈；空库不植入演示数据。详见 [迁移与恢复](migrations/README.md)。

更新源码前停止本文件夹服务并备份数据库；保留本地 `.env` 和 `backend/demo.db`。不要把一个空数据库覆盖旧数据。启动器根据后端锁文件、前端锁文件和源码指纹决定重装/重建。已安装缓存可复用，依赖变更需要联网。其他电脑各自存储本地数据，尚无云端同步。

## 开发者运行与检查

若需要Vite热更新，可自行准备Node.js22.12+和Python3.11+：

```powershell
python -m venv .venv
.venv/Scripts/python.exe -m pip install -r backend/requirements.lock.txt
cd frontend
npm ci
cd ..
./start.ps1 -Dev
```

开发模式使用8000/5173；启动检查同时要求 B 标识、`teaching_version=0.3.0` 和 `schema_version=3`。自动模式发现同目录旧服务会拒绝复用，并提示先停止旧后端；不会替用户关闭进程，也不能发现所有其他路径的数据库写入者。手动运行后端：`.venv/Scripts/python.exe -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000`；前端在frontend运行`npm run dev`。自动模式使用构建页面与同源API，无需常驻Vite。

```powershell
# 使用隔离测试数据库，不操作实际demo.db
.venv/Scripts/python.exe -m pytest tests -q
# CSV筛选、内容安全和下载生命周期（原生Node，无额外依赖）
.runtime/node-v22.23.2-win-x64/node.exe --experimental-strip-types --test tests/test_class_progress.mjs tests/test_question_selection.mjs
# 完整源码冷启动验收（需要网络，耗时较长）
.venv/Scripts/python.exe tests/check_source.py
# TypeScript及生产构建
cd frontend
npm run build
```

原先的 [便携包工具](docs/portable-windows.md)保留为可选历史方案，不再作为下载源码后的必需步骤；旧C版便携ZIP不包含此次B/C集成。

## 协作入口

所有人及AI开始工作前完整阅读 [AGENTS.md](AGENTS.md)，再读 [开发日志](docs/development-log.md)、[架构](docs/architecture.md)、[接口](docs/contracts/README.md)。教师v0.3与main拍照增量现已在同一整合树中，发布提交和验证结果以最新日志为准。目录变更和上传前凭据确认仍按AGENTS执行。

C 当前任务与验收边界见 [C 负责人状态报告](docs/c-role-status-report.md)。本轮 v0.3 接口已先行登记；后续共同授课、跨教师学生共享、正式身份和数据库变更仍需由 C 统筹，目录结构变更需先获明确确认。

协作开发前确认分支基线，详见AGENTS.md与开发日志。独立原型随main历史保留，不代替正式Vue应用或后端能力。

项目仓库：https://github.com/844346518scp-coder/agent-school-llm-usts 。历史原始材料保留原文；PCL.exe沿用main重新加入的原始字节，不在应用构建或启动中执行，其来源与可执行安全未全面审计。

## 仓库原有说明（原文保留）

# agent-school-llm-usts
dddd
