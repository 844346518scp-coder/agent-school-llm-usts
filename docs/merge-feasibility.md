# B / C 分支合并可行性核验

**2026-09-18实施更新：** 用户已要求源码自动启动及B/C整合。当前在本地codex/source-bootstrap-integration合入main的B代码，保留C功能；修复模式、超时、识别演示禁调、关键词误判和错误信息兼容。现已更新原C分支并通过PR #2合入main，最终验证见开发日志。下文保留此前评估快照。

后续状态：同日再次核对时，远端main已到4bd8519（合并B的PR #1）。用户随后确认单独上传C分支，功能提交955bbbe已上传至codex/c-local-teaching并核对文件树一致；以下为此前评估快照，冲突与兼容项仍待集成处理。

日期：2026-09-17。结论：**可以整合，但当前不具备直接在 GitHub 合并两人成果的条件。应先保存 C 的本地修改，在集成分支解决冲突和兼容问题，通过验收后再提交 PR。** 本轮仅评估，未实际合并、提交或上传。

## 已核实的版本

- C：本地 `codex/c-local-teaching`，HEAD 为 `9a57238`，教师工作流0.2及文档仍是未提交修改，并不存在对应的远端C功能提交。
- B：远端 `chatbox/ai-agent-v0.2`，提交 `721b3e276fa79564ac8a3dfa410bdc85b662b2c9`。用户 ZIP 的64个文件与该提交逐文件 Git blob 哈希一致，可直接使用远端分支，不必把ZIP整包覆盖到项目。
- 当前远端 main：`a76925e93712eebfd3c621cadea8467ea1e8af96`，相较本地共同基线新增删除 `PCL.exe` 的提交；B 已基于该 main 开发。集成时保留此删除，不从本地旧文件树重新上传 PCL.exe。
- B 主要增加模型调用、内置课程 BM25 检索、流式/诊断/识别接口和测试；未改变前端、平台数据模型、教学模块和依赖。C 主要增加教师功能、数据迁移和相关前端，职责重叠较小。

## 合并预演结果

以 `9a57238` 为共同基线，对本地C工作区与B分支进行三方文本合并；评估时归一化UTF-8 BOM和CRLF，不改变项目源文件。

| 类型 | 文件 / 结论 |
|---|---|
| 需人工解决的3个文本冲突 | `backend/app/main.py`、`docs/architecture.md`、`docs/development-log.md` |
| 双方都改、文本可自动合并 | `.env.example`、`AGENTS.md`、`docs/contracts/README.md`；仍需人工核对语义，避免“未接入模型”等状态表述与新接口矛盾 |
| 后端入口处理原则 | 保留C的SQLite迁移调用和应用版本0.2.0，加入B的新路由及动态模型模式；不能简单选“全部采用一方” |
| 功能保留原则 | 保留C的题库、草稿、反馈、归档、统计与迁移；加入B的AI模块。B ZIP 内携带的旧教师前端和数据库文件不能覆盖C版本 |

在系统临时副本中仅组合后端入口、保留其余自动合并的代码，双方现有测试合计 **48 passed，2条既有依赖弃用提示**。文档冲突未在临时副本中假装处理完成。该结果说明基础代码可以共存，不等于真实模型、完整前端和所有异常场景已验收。

## 必须处理的兼容问题

1. **启动器不接受 live 模式。** C 的 `start.ps1` 健康检查要求 `agent_mode == demo`。组合入口在配置模型后返回 `live`，临时探针已复现现有启动条件为false。应允许正常的demo/live模式并保持版本校验，而不是把真实模型服务判为启动失败。
2. **前端仍固定演示文案与类型。** `Conversation.mode` 只声明demo，智能体窗口固定显示“预设演示回复”；B实际可能返回live、notice和references。需按真实返回状态展示来源及失败降级提示，并明确本轮是否接入B新增的流式、识别和诊断入口。
3. **超时不一致。** 前端统一超时15秒，B默认模型超时30秒。慢响应可能已在后端保存，前端却报失败，用户重试可能生成重复记录。应为AI请求单独协调前后端超时并明确重试行为。

## B 模块自身的待修正项（不是 Git 文本冲突）

- `recognize_question` 只检查model_ready，不检查resolved_mode。用假配置及模拟模型客户端核验：AGENT_MODE=demo时仍调用识别模型并返回live。需落实演示模式不调用模型的约定；本次没有真实模型请求或费用。
- `step_feedback` 只要命中规则关键词就给出incorrect。已复现“可导必连续。”被判incorrect。应将关键词命中视为待复核提示，不能直接形成确定错误结论，并修改相应测试。
- 现有48项测试未覆盖上述全部问题，因此“测试全过”不能替代这些场景的补测。

## 推荐操作顺序

1. 将当前C代码与文档保存为本地功能提交，保留全部现有改动。
2. 从最新远端main建立集成分支，纳入C提交与B分支；使用Git提交历史，不整包覆盖，不恢复远端已删除的文件。
3. 解决3个文本冲突，统一配置、状态和接口文档；完成上述兼容修正并与B确认规则判错/演示模式问题。
4. 跑双方测试并补充live健康检查、模拟超时、演示识别禁止外调、规则不误判等测试；验收教师闭环与AI问答。真实模型实连仅在用户提供本地配置并明确测试范围后进行。
5. 完成工作区、暂存区、待上传历史和附件的凭据检查；用户确认上传范围及凭据状态后推送集成分支、创建PR，由另一成员审核后合并main。当前没有任何上传或合并授权执行。

## 核验边界

本次只读查询了远端并fetch，临时文件位于系统Temp；未改业务代码、未触碰实际数据库、未修改线上仓库。没有重跑前端构建/浏览器验收，没有调用真实模型。ZIP文本凭据模式扫描未发现常见令牌或私钥；两处连接串命中均为已核对的USER/PASSWORD模板。未完成全历史、图片、PDF等载体的完整秘密检查，整体密钥状态仍为未核验，不作为上传放行依据。
