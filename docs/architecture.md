# 架构与决策

最后更新：2026-09-19。

当前以数伴0.2 B/C集成版为基线，在本地`codex/teacher-workspace`落实教师工作空间v0.3/schema v3（未发布）。仍为Vue 3 + TypeScript + Vite 6、Element Plus、KaTeX、ECharts与FastAPI + SQLAlchemy，本轮没有增加依赖或维护目录。

此前已从建设计划书确认技术路线。PostgreSQL / pgvector、百炼、Tavily、SymPy、worker和Docker Compose属于正式路线而非全部已实现。当前默认SQLite，v3迁移拒绝非SQLite；真实外部模型效果未经验证，不能以兼容接口代替效果验收。

学生端与教师端调用后端接口；平台模块负责身份、权限、文件与持久化；教学模块负责题库、作业及复核；AI 模块负责识别、检索、诊断与学习策略。AI 输出通过平台保存，前端负责呈现，不直接调用需要秘密凭据的模型服务。远端main后续拍照入口仍调用既有后端识别API，不在浏览器保存模型密钥。

后续目标：AI 返回圈注坐标和解释，学生端显示；AI 决定记忆更新策略，平台存储并控制访问；教师端提供 AI 反馈复核入口，AI 使用复核结果修正后续反馈。上述圈注/记忆/教师AI复核能力本版尚未实现；当前新增的教师人工评语只针对学生作答，不驱动 AI。

学生问答和 C 教师闭环均已打通：题库→草稿→发布→学生提交→教师按版本反馈→学生修改→教师复核→归档。B/C 已集成并通过 PR #2 合入 main。模型或演示来源均明确标记；本地假模型与固定演示内容不可作为真实模型效果或真实教学效果的证据。

## 当前实现

- 页面：无教师空库先建档，已有账号登录后恢复工作台。教师有真实班级/成员、账号设置、按班概览/作业、题库、人工反馈与成员CSV导出；学生首登临时密码先改密，再访问本人收件作业、问答和历史。头像/姓名使用当前User，不再硬编码陈/林。
- 身份：默认不创建演示账号；首位教师通过一次性setup建立，可创建同级教师和所属学生。用户名唯一，新密码10–128位含英文字母数字，PBKDF2-HMAC-SHA256 260000次随机盐。HttpOnly/SameSite=Strict Cookie，库仅存token摘要；remember七天，否则八小时会话。临时密码限制业务访问，改密/重置撤销所有旧会话，停用账号拒绝认证。
- 请求：Vite将/api代理到FastAPI，写请求要求X-Requested-With，不开放CORS；浏览器不持有模型密钥。HTTPS需COOKIE_SECURE=true。当前仍缺正式身份、登录限流、统一管理员及公网部署保障。
- 数据：保留users/sessions/conversations/assignments/submissions/questions/reviews/schema_migrations，新增classrooms、class_members、assignment_recipients。默认backend/demo.db，名称兼容保留；教师资源归属、账号管理创建人、班级成员和作业收件人分别控制权限，不靠前端筛选。
- 问答：B模型兼容接口+本地BM25课程检索，未配置/调用失败时使用明确标注的固定示例；不做自动评分。收藏只是用户标记。图表仅统计实际记录，不推断掌握度。当前是单问题记录，不提供多轮推理上下文。
- 作业：draft→published→archived。草稿可不完整/未选班；发布须选本人未归档且含有效学生的班级，保存收件快照。后加成员不会自动收到旧作业，退班仍能读自己的历史但不可提交。发布后题干/班级锁定，仅延期；作业归档不可逆。复制不带提交/反馈，源班归档则新草稿class_id=null。题库复制不泄露参考答案。
- 反馈：学生保存作答递增 version；review 唯一对应 submission_id + version，保留 answer_snapshot。当前版本无反馈即待批改；学生修改不沿用旧结论。教师提交版本过期返回409。同版评语允许修改，旧版反馈保留。SQLite 写事务使用 BEGIN IMMEDIATE，使状态检查和写入串行化；截止当天可提交，归档后提交/批改均409。
- 统计：总览仅当前教师有效班级的已发布作业，按收件快照计算应交/未交，当前反馈分待批改/待改进/已完成；有效学生按活跃账号/成员去重。班级名册可查归档班级的历史进度，导出当前筛选结果。不读取私人问答，不推断掌握度。
- CSV：`shared/classProgress.ts`集中实现当前列表筛选、安全序列化和下载；页面与导出共用筛选结果。UTF-8 BOM与引号转义保留中文/逗号/多行，公式及前导空白风险加文本前缀；下载链接挂载后点击、移除，延后1秒释放对象URL。原生Node测试覆盖内容与清理顺序，不替代真实浏览器落盘/Excel验收。
- 初始化与迁移：upgrade先备份再BEGIN EXCLUSIVE升级到v3，失败回滚不启动；历史每位教师建立班级并保留旧可见范围，单教师旧库才推定学生账号管理归属。多教师归属不明以manageable=false展示。当前v3重启校验表/列；显式种子仅在空用户表执行，不重建被修改账号/作业。restore验证并恢复到新路径，拒绝覆盖。
- 本地运行：根目录一键启动.cmd 调用 start.ps1；默认由 bootstrap.ps1 在项目 `.runtime` 内下载并校验固定 Python/Node/pip、安装锁定依赖、构建前端，再由单个 FastAPI 进程同源提供页面/API。重复运行复用本目录服务，目录锁防止并发启动，端口不可用时自动换端口；支持 -SetupOnly、-NoBrowser、-Port。-Dev 才使用系统开发环境的8000/5173热更新。当前没有公网部署、Docker Compose 配置或 CI 工作流。

## 当前工作区与远端main差异（2026-09-19）

本地v0.3从285f1aa开始，已有本轮未提交改动。此前只读核对的远端main为168928d，后续拍照Vue入口、两个根目录原型和PCL.exe未合入。本节只记录差异，不代表接受其架构或完成验收。

远端Vue拍照入口沿用`POST /api/agent/recognize`：相机/相册→框选→压缩→识别草稿→人工编辑确认→回填既有提问框，组件本身不入库。贡献者记录的验证仅为TypeScript和Vite构建；真机相机、401/502/503、后端联调和真实视觉模型均未验证。独立原型中的“AI视频解析”默认请求`/api/agent/video-search`，当前后端没有该接口，因此仅是静态演示，不进入正式能力清单。

后续集成需要A或另一成员复核共享前端，C检查接口/启动、根目录授权和附件来源。本轮在已验证集成基线上落实C功能，没有修改、执行、删除或合入远端增量；不得用旧前端覆盖新账号/班级页面。

目录树与变更规则统一维护于 [AGENTS.md](../AGENTS.md)。公共接口、依赖和数据库迁移由 C 统筹审核，C 已由当前用户认领，A/B 与公共文件复核人待确认。接口细节见 [接口约定](contracts/README.md)。

## 决策记录

| 日期 | 决定 | 依据与状态 |
|---|---|---|
| 2026-09-17 | 初始化基础目录，保留现有素材原位 | 用户授权搭建协作底座，已完成 |
| 2026-09-17 | AGENTS.md 统一承载规则、目录和项目记忆 | 支持不同 AI 工具，需使用者确保加载 |
| 2026-09-17 | 清理原始素材与 output 两个目录，仅保留开发协作骨架 | 用户确认方案并手动删除，已核实；取代初始化时保留素材的安排，业务边界与技术建议不变 |
| 2026-09-17 | 按恢复的建设计划书搭建数伴 0.2 本地 MVP | 素材在本轮开始时已恢复，本轮保留；用户授权自由完善最小版本，沿现有业务目录实施 |
| 2026-09-17 | SQLite 作为本地体验适配，保留 PostgreSQL 路线 | 避免首次体验依赖数据库服务；实测 SQLite，PostgreSQL / pgvector 后续验证，不自动迁移本地数据 |
| 2026-09-17 | 记住密码通过持久会话实现；智能体明确演示 | 不在浏览器保存明文密码，不把固定回复当真实模型输出 |
| 2026-09-17 | 根目录增加可双击的一键启动入口 | 复用并增强 start.ps1，就绪后自动打开页面，避免用户手动操作两个终端；不新增目录 |
| 2026-09-17 | 先完成 C 本地教师闭环，正式账号、多班级和 PostgreSQL 后置 | 题库、草稿/发布/归档、版本化作答、人工反馈和统计已完成并验收 |
| 2026-09-18 | 普通源码自动初始化取代便携包成为默认分发方式 | 固定运行时版本与哈希，项目内安装，不要求系统开发环境或管理员权限 |
| 2026-09-18 | B/C 集成后通过 PR #2 合入 main | 保留 C schema v2 与教师流程，加入 B 模型/检索接口并修复模式、超时、降级和规则误判兼容问题 |
| 2026-09-19 | 下一阶段先设计正式账号与多班级权限 | 当前演示班级无成员关系或资源授权模型；PostgreSQL、AI复核和部署继续后置 |
| 2026-09-19 | 将远端拍照入口与独立原型视为待复核增量 | 当前工作区未合入；Vue入口只有构建验证，独立视频原型缺少后端接口，PCL.exe及根目录变化需另行核对 |
| 2026-09-19 | 用户后续授权落实教师演示功能，实施v0.3 | 首次建档、教师所属学生、多班级、发布收件快照、schema v3；不新增目录或外部依赖，不代替真实AI/正式身份验收 |
| 2026-09-19 | 区分成员、账号与发布收件人状态 | 退班不删历史，停用撤销会话，发布快照不因名册变动重新计算；历史归属不明不推定重置权限 |

后续实现时记录技术选择、原因、影响及验证结果；未经确认不得修改目录结构。

## 首次仓库集成

按用户授权，将本地项目接入已有 main 历史，不强制推送；原有 PCL.exe 与 AVIF 图片保持原始字节，原 README 全文保留在 README 的独立章节。AI 主动检查秘密凭据的责任与本轮直接上传授权见 AGENTS.md 第 6 节。未新增业务目录或改变运行架构。

## 2026-09-17 / 本地教师工作流决定

用户确认先完成本地题库、作业状态、人工反馈和统计，正式账号/多班级/PostgreSQL/云端部署后置。仅在现有目录新增普通文件，不改目录结构；没有引入依赖或外部模型。前端进入作业页/概览/演示班级、操作完成或手动刷新时读取接口，无实时推送。该阶段功能提交为955bbbe，其独立交付边界随后已被2026-09-18的B/C集成取代，并通过PR #2进入main。


## 2026-09-18 / Windows便携测试分发

当前C版新增独立便携构建方式：开发机将前端构建为静态文件，随独立CPython 3.11运行时、标准库、原生DLL及按锁文件版本核验的后端包一起分发。测试端由FastAPI同源托管网页/API，不启动Vite、不依赖系统Node/Python，不复制开发机虚拟环境。官方关于应用内分发与路径隔离的说明见 https://docs.python.org/3.11/using/windows.html 。本轮复用已验证的uv管理CPython 3.11.15完整运行时，未另下载嵌入式版本。

start.ps1根据portable-manifest.json识别测试包并调用platform/portable.py；后者使用字节范围锁串行启动，校验端口、页面及目录实例标识，仅绑定127.0.0.1，默认18080；普通源码模式保持原启动方式。可用-Port更换便携端口。数据库固定为包内backend/demo.db，避免继承其他应用DATABASE_URL；Python通过_pth隔离系统配置。/api/health仅在指定环境变量时增加instance_id用于区分同版本不同目录。未引入业务接口或数据模型变更。

构建目录与ZIP在已忽略dist内；只新增现有目录内普通源码/说明/验收文件，不新增业务目录。不打包用户数据库、.env、日志、Git、PCL.exe、原始素材；随包保留Python及依赖许可、版本和逐文件哈希。20260918-083356最终候选是集成前的历史C-only包；B/C集成现已完成，但旧包不会自动获得新代码。若从当前源码重新构建便携包，必须按新的文件内容重新做凭据检查、隔离验收和实机边界说明。

端口补充：默认优先18080，记住上次成功端口；自动模式遇到占用或Windows预留端口时改用系统分配的可用端口，显式-Port遇冲突则报错。持久化文件backend/launcher-port.txt与锁文件都被忽略，不含会话信息。

## 智能体（B）处理流水线（2026-09-17 追加）

目的：把“演示问答”升级为可接真实模型的骨架，同时保证没有模型密钥时流程仍然完整、且不把预设文案冒充模型输出。

流水线：

1. **配置**：`backend/app/ai/config.py` 读取 `AGENT_MODE` 与 `MODEL_*`，得出 `resolved_mode`（`demo` / `live`）；`live` 缺凭据时如实降级为 `demo`。
2. **检索**：`knowledge.py` 对 `KNOWLEDGE_POINTS` 建本地 BM25 索引（中文按字 bigram），返回带出处（`source`）与命中词（`matched`）的片段，因此引用可解释、可复核。
3. **提示词**：`prompts.py` 组装分层提示（概念层 → 例题层 → 迁移层），要求只依据片段作答并在句末标注 `[n]` 编号。
4. **调用**：`llm.py` 用 httpx 调 OpenAI 兼容 Chat Completions（含流式），把失败统一翻译成 `ModelUnavailable` / `ModelCallFailed`。
5. **组装与降级**：`service.py#compose_answer` 决定走 live 还是 demo；模型失败时回退 demo，并在 `notice` 里写明原因。
6. **诊断与步骤反馈**：`diagnosis.py` 规则用于产生可追溯的候选薄弱点和自检提示；启发式 confidence 未经效果校准。demo 下步骤 verdict 统一为 `unclear`，不能因关键词直接判错；live 模式模型判断也必须由教师或学生复核。

模块边界：B 只产出结构化结果，不实现权限与持久化；鉴权与存储仍走 `platform`（C）。B 未新增数据库列，历史记录的 `mode` 由答案文本中的演示标记反推。

升级路径：把 `knowledge.retrieve()` 换成向量检索、把 `llm.chat()` 换成多模型路由，都不需要改动路由层与前端。


## 2026-09-18 / 当前源码自举与B/C集成（优先于此前阶段描述）

用户明确要求普通源码文件夹下载后直接启动；此前便携分发仅保留为可选工具。start.ps1默认走bootstrap.ps1项目私有安装，首次联网取得固定版本和SHA256校验的Python/Node/pip，安装锁定依赖并预构建页面，交由FastAPI同源托管；无管理员/全局PATH变更。-Dev才走原Vite方式。源码模式保留.env与数据库语义，便携模式仍固定包内数据库。详细机制见source-startup.md。

origin/main在4bd8519已包含B，用户提供的main ZIP逐文件与该提交一致。随后在codex/source-bootstrap-integration合入其历史，保留C教师流程、schema版本2及备份恢复，保留main删除PCL.exe。共享入口保留C迁移、B路由/动态模式和目录实例标识；原C分支已更新，并通过PR #2合入main（b52f194）。当前后续开发应基于该集成状态，不再重复执行旧的B/C合并计划。

## 2026-09-19 / 教师工作空间 v0.3 与后续边界

本轮已按先行契约实现教师→班级→成员和作业→收件人两套关系。classrooms归属于一位教师；User.created_by用于学生账号管理，不等于成员身份；AssignmentRecipient固定发布时对象。班级归档可恢复，作业归档不可逆，两者独立。课程/学期暂为标签，不提供课程共享、助教或跨教师学生转移。

独立测试覆盖至少两教师、两班与多学生的列表/提交/批改/统计/题库越权，以及首位教师竞争、重复账号、归档与提交竞争。具体执行结果见最新日志。PostgreSQL、pgvector、正式身份、审计和部署仍分别验收；AI复核未来须保存原结果、教师修改和发布状态，不自动把AI判断作为正式评价。

前端普通问答支持demo/live、调用失败提示及待复核引用；AI请求等待90秒、模型网络阶段超时1–60秒，禁止自动重试。回答来源前缀和降级提示随正文持久化，无数据库结构变更；旧记录兼容读取。步骤关键词不能确定错误，一律unclear；demo识别不外调，模型错误正文不回显秘密，流式网络异常规范降级。课程资料仍待复核，模拟live测试不等于真实服务验收。
