# 架构与决策

最后更新：2026-09-17。

当前为数伴 0.1 本地 MVP：Vue 3 + TypeScript + Vite 6，Element Plus 提供弹窗等基础交互；KaTeX 渲染公式，ECharts 绘制实际提问量；FastAPI + SQLAlchemy 提供账号、会话、对话和作业接口。完整依赖版本见锁文件。

本轮原始素材目录已恢复，已读取建设计划书确认技术路线。正式方案采用 PostgreSQL / pgvector、百炼、Tavily、SymPy、worker 与 Docker Compose。本版为方便本地立即体验，默认 SQLite，提供 PostgreSQL 连接能力，但未实连验证。其余外部能力均未接入，不承诺计划中的完整能力。

学生端与教师端调用后端接口；平台模块负责身份、权限、文件与持久化；教学模块负责题库、作业及复核；AI 模块负责识别、检索、诊断与学习策略。AI 输出通过平台保存，前端负责呈现，不直接调用需要秘密凭据的模型服务。

后续目标：AI 返回圈注坐标和解释，学生端显示；AI 决定记忆更新策略，平台存储并控制访问；教师端提供复核入口，AI 使用复核结果修正后续反馈。上述能力本版尚未实现。

首轮闭环“示例题提交、模拟反馈、页面展示、记录保存与重新查看”已打通，并完成教师发布、学生提交及教师查看作答。模拟内容明确标记，不可作为真实模型评测结果。

## 当前实现

- 页面：首次无会话时进入登录页，可切换学生 / 教师界面；有效会话恢复对应工作台。学生有概览、智能体、课程探索、作业、复习收藏和学习记录；教师有概览、教学助手、作业、演示班级和对话记录。
- 身份：预设学生、教师各一个账号，密码使用 PBKDF2-HMAC-SHA256（260000 次、随机盐）；随机会话令牌通过 HttpOnly、SameSite=Strict Cookie 发送，数据库仅存令牌 SHA-256 摘要。remember=true 保留七天，否则为浏览器会话 Cookie、服务端八小时有效；重登替换当前令牌，退出即撤销。
- 请求：本地 Vite 将 /api 代理到 FastAPI；写请求要求 X-Requested-With 自定义头，不开放跨域 CORS；浏览器不得持有服务端模型密钥。部署 HTTPS 时需配置 COOKIE_SECURE=true。本地公开演示账号不具备正式账号管理、登录限流或公网部署保障。
- 数据：users、sessions、conversations、assignments、submissions。数据库默认 backend/demo.db，重启保留。个人对话按 user_id 隔离；教师只看自己的作业，学生只拿自己的答案。所有演示学生共享单一班级，不伪装成已实现多班级授权。
- 问答：基于关键词选择有限的固定示例，unknown 明确提示暂不能解答；不调用模型、不做自动评分。收藏只是用户标记。图表仅统计实际记录，不推断掌握度。当前是单问题记录，不提供多轮推理上下文。
- 作业：教师输入标题、内容、章节与截止日期；学生对一份作业 PUT 一份最新作答，截止当天可提交，之后返回 409；教师查看实际答案，不自动评分。
- 初始化：启动时创建缺失表并幂等种子数据，create_all 不升级旧表；后续迁移必须单独提交并审核。说明见 migrations/README.md。
- 本地运行：根目录一键启动.cmd 调用 start.ps1，检查依赖 / 端口，隐藏启动服务，等待健康检查与页面就绪、确认 API 代理后自动打开浏览器。重复运行复用服务，互斥锁防止并发启动；失败时显示原因和日志路径。支持 -NoBrowser。也可使用两个手动终端；先构建再启动 FastAPI 时，后端可托管 frontend/dist。当前没有公网部署、Docker Compose 配置或 CI 工作流。

目录树与变更规则统一维护于 [AGENTS.md](../AGENTS.md)。公共接口、依赖和数据库迁移由 C 统筹审核，具体人员待认领。接口细节见 [接口约定](contracts/README.md)。

## 决策记录

| 日期 | 决定 | 依据与状态 |
|---|---|---|
| 2026-09-17 | 初始化基础目录，保留现有素材原位 | 用户授权搭建协作底座，已完成 |
| 2026-09-17 | AGENTS.md 统一承载规则、目录和项目记忆 | 支持不同 AI 工具，需使用者确保加载 |
| 2026-09-17 | 清理原始素材与 output 两个目录，仅保留开发协作骨架 | 用户确认方案并手动删除，已核实；取代初始化时保留素材的安排，业务边界与技术建议不变 |
| 2026-09-17 | 按恢复的建设计划书搭建数伴 0.1 本地 MVP | 素材在本轮开始时已恢复，本轮保留；用户授权自由完善最小版本，沿现有业务目录实施 |
| 2026-09-17 | SQLite 作为本地体验适配，保留 PostgreSQL 路线 | 避免首次体验依赖数据库服务；实测 SQLite，PostgreSQL / pgvector 后续验证，不自动迁移本地数据 |
| 2026-09-17 | 记住密码通过持久会话实现；智能体明确演示 | 不在浏览器保存明文密码，不把固定回复当真实模型输出 |
| 2026-09-17 | 根目录增加可双击的一键启动入口 | 复用并增强 start.ps1，就绪后自动打开页面，避免用户手动操作两个终端；不新增目录 |

后续实现时记录技术选择、原因、影响及验证结果；未经确认不得修改目录结构。

## 首次仓库集成

按用户授权，将本地项目接入已有 main 历史，不强制推送；原有 PCL.exe 与 AVIF 图片保持原始字节，原 README 全文保留在 README 的独立章节。AI 主动检查秘密凭据的责任与本轮直接上传授权见 AGENTS.md 第 6 节。未新增业务目录或改变运行架构。
