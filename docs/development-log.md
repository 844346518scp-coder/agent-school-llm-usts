# 开发日志

采用追加方式保存每次开发与交接记录。不得记录密钥原文或真实学生隐私。

## 记录模板

```text
日期 / 任务：
负责人 / AI 工具：
开发了什么与原因：
修改文件：
验证命令或方式 / 结果（通过、失败、未执行）：
目录结构变更 / 用户确认依据（没有则写无）：
文档同步清单（逐项写已更新，或已核对无变化及原因）：
  - AGENTS.md 项目记忆与目录：
  - README.md：
  - docs/architecture.md：
  - docs/contracts/ 下全部约定：
  - 其他相关说明、配置模板、计划与图示：
GitHub 上传 / 密钥状态 / 检查范围 / 用户确认依据：
遗留问题与下一步：
```

## 2026-09-17 / 初始化协作底座

- 负责人 / AI 工具：Codex；A/B/C 实际人员待认领。
- 开发内容：创建学生端、教师端、平台与 AI 模块的基础目录；编写跨工具共用的 AGENTS.md、README、架构与接口入口、开发日志、环境模板及 Git 忽略规则。
- 原因：三名成员使用的 AI 工具不统一，需要共同规则、持久项目记忆和清晰职责。
- 修改文件：根目录 AGENTS.md、README.md、.gitignore、.env.example；docs 中架构、接口入口、本日志；各空业务目录 .gitkeep。
- 目录授权：用户本次明确要求搭建基本文件夹结构；既有原始素材与 output 保持原位。
- 文档同步：AGENTS.md、README、架构、接口入口与本日志已创建；环境模板仅空值。既有规划图示和素材不因本次目录初始化而改变，保留为历史资料；当前实施状态以 AGENTS.md 为准。
- 验证：已核对新增文件存在、文档相对链接与目录树对应关系；未执行应用测试，因为没有应用代码。
- GitHub / 密钥状态：未上传；本次新增模板无真实密钥；未进行全项目及历史密钥扫描，整体状态为未核验，没有上传确认。
- 下一步：认领分工，落实技术版本与首批接口，再搭建可运行骨架。

## 2026-09-17 / 素材清理执行受阻

- 负责人 / AI 工具：Codex。
- 任务与授权：用户明确要求执行“只保留开发骨架”方案，删除原始素材与 output 两个目录及 10 个素材文件，不另建备份。
- 已完成检查：完整阅读协作规范及相关文档；核实两个目标的绝对路径均位于当前项目内，目录内无符号链接或其他重解析点，文件清单与计划一致。
- 执行结果：递归删除命令被自动审批检查拒绝；改为明确列出 10 个文件逐个删除、再移除空目录的方案同样被拒绝。返回原因均为 blocked by policy，未提供更具体原因。命令未启动，素材未删除。
- 文档同步：本日志追加受阻记录；AGENTS.md、README.md、架构说明已核对，因清理尚未执行，不将目录树及项目状态改写为已清理；接口说明、.env.example 与 .gitignore 已核对，无变化，本次不涉及接口或配置调整。
- 验证：路径与素材清单检查通过；清理验收未通过；未运行应用测试，因为没有应用代码。
- GitHub / 密钥状态：未上传，未执行完整密钥扫描，仍为未核验。
- 下一步：待删除限制解除或用户手动删除这两个目录后，重新核实实际目录并完成项目记忆、目录树、README 和架构决策同步。

## 2026-09-17 / 素材清理完成与文档同步

- 负责人 / AI 工具：用户手动删除素材；Codex 核验并同步文档。
- 授权与完成依据：用户确认“只保留开发骨架”方案，随后告知“删除了”；实地检查确认原始素材与 output 两个目录均不存在，原计划中的 10 个素材文件已不在原路径。本次未创建备份目录。
- 修改内容：AGENTS.md 更新目录树、项目记忆及历史材料可用性说明；README.md 更新当前状态；docs/architecture.md 追加清理决策；本日志追加完成记录。此前“素材清理执行受阻”条目保留为历史，阻塞已通过用户手动删除解决。
- 文档同步：上述四份文档已更新；docs/contracts/README.md、.env.example、.gitignore 已核对，无变化，因为本次未涉及接口、配置或忽略策略；已删除的资料不再作为本地可读取依据。原有协作规则、项目方向、职责边界及技术建议保持有效。
- 验证结果：通过——两个素材目录不存在、15 个应保留文件全部存在、全部 Markdown 相对链接有效；当前目录树与实际目录一致，历史表述仅在明确的历史记录中保留。未运行应用测试，因为没有应用代码。
- GitHub / 密钥状态：未上传；未执行完整密钥扫描，状态仍为未核验。
- 下一步：认领分工，落实技术版本与首批接口，在现有目录中搭建可运行骨架。

## 2026-09-17 / 数伴 0.1 登录与教学学习 MVP

- 负责人 / AI 工具：Codex。用户明确授权搭建可体验页面、两类登录、预设账号、记住密码、智能体窗口，并依据原有讨论自由补充功能、更新文档。
- 开发内容：实现学生浅紫色与教师浅绿色登录 / 工作台；搭建真实 FastAPI 身份校验、PBKDF2 密码摘要、可撤销的 Cookie 会话；记住密码以七天会话实现，不在浏览器保存明文密码；实现课程入口、智能体固定演示问答、KaTeX 公式、收藏 / 搜索 / 历史、实际提问图表、教师发布作业、学生提交及教师查看答案；补充响应式导航、错误提示、加载状态、空状态和本地启动脚本。
- 技术依据：本轮开工发现原始素材目录已恢复，实际读取原始素材/output/pdf/高数学习智能体建设计划书.pdf 第 4 页，并重新读取关联任务。采用 Vue 3、TypeScript、Element Plus、KaTeX、ECharts、FastAPI；默认 SQLite 支持即装即用，SQLAlchemy 保留 PostgreSQL 连接支持。依赖版本锁定在前后端锁文件。
- 目录情况：只在已存在的 frontend、backend、migrations、tests、docs 等目录内添加文件；新增 .venv、node_modules、dist、缓存为已忽略的依赖 / 构建产物，未新增业务目录。恢复的原始素材在本轮开工时已存在，未被本轮移动或删除，已据实更新目录树。
- 修改范围：frontend 中配置、入口与 student / teacher / shared 页面；backend 中依赖、FastAPI 入口、platform / teaching / ai 模块；tests/test_api.py；migrations/README.md；start.ps1、.env.example、.gitignore；全部现行协作文档。
- 验证（通过）：后端 `python -m pytest tests/test_api.py -q` 共 11 项，覆盖两角色登录、错误凭据、角色不匹配、会话有效期 / 撤销、密码摘要、问答持久化与隔离、收藏、未知问题诚实回复、作业发布 / 提交 / 修改、字段校验和写请求头限制。首次测试遇到 Windows 临时数据库文件占用，已增加 engine.dispose 后清理，再次测试通过；剩余 2 条为依赖库弃用提示，不影响测试。
- 验证（通过）：frontend 的 `npm run build` 完成 TypeScript 检查及生产构建；按需引入 Element Plus，最终 UI JS 约 88 KB（gzip 32 KB），最终构建无大分块警告。
- 浏览器验收（通过）：学生登录、固定极限反馈与公式、收藏及刷新 / 重登后的记录；教师登录并发布“演示 · 导数的几何意义”；学生提交演示答案；教师查看真实提交；记住登录后刷新恢复、退出返回登录、错误密码提示；1440×1000 桌面与 390×844 手机断点、手机菜单、登录与作业页面无横向溢出。浏览器未记录 JS error。
- 启动验收（通过）：已实际运行 start.ps1 隐藏启动前后端；健康接口正常；后端提供生产构建首页及 9 项引用资源均为 HTTP 200；生产页面能登录。重启后仍有 1 条已收藏演示对话、2 份作业（1 份初始作业、1 份验收时发布作业），以及验收时学生提交的演示答案，便于继续体验。
- 文档同步：AGENTS.md 更新现状、目录树、授权与下一步；README 更新安装、启动、账号、体验流程、配置与边界；architecture.md 更新实现和决策；contracts/README.md 登记全部已实现接口；migrations/README.md 说明初始化与迁移限制；.env.example 更新实际配置项；.gitignore 补充 SQLite 附属文件；本日志追加。恢复的历史 PDF / 图示已核对为规划参考，未修改原文，计划中的未来能力不视为已实现。
- 未执行 / 限制：未实连 PostgreSQL / pgvector，未接入真实模型、OCR、语音、RAG、Tavily、SymPy、worker 或多班级；没有 Docker Compose 部署与 CI。公开演示账号只用于本地；正式账号管理和公网防护仍待实现。师生招募与组织试用仍暂定。
- GitHub / 密钥状态：未上传，未执行完整项目与历史密钥扫描，状态仍为未核验；本轮无需外部 API 密钥，预设账号密码是公开演示凭据。没有 GitHub 上传确认。
- 下一步：用户体验并调整页面；认领 A/B/C；按接口约定接入真实模型，再落实 PostgreSQL / pgvector、正式认证与多班级权限。
- 补充界面验收：默认较矮桌面窗口中曾出现侧栏底部账号控件被挤出可视区，已为侧栏添加滚动并收紧短窗口间距，实际点击退出后成功返回登录页；移动端收起导航时隐藏其焦点目标。测试过程中发布的作业与作答保留为明确标注的演示记录。

## 2026-09-17 / 根目录一键启动器与目录说明

- 负责人 / AI 工具：Codex。用户要求在根目录制作一键启动器，并解释当前目录结构。
- 开发内容：新增一键启动.cmd，双击调用 start.ps1；增强脚本的端口与依赖检查、健康服务复用、逐服务就绪等待、前后端代理检测、并发互斥及自动打开浏览器。保留 -NoBrowser 供终端检查；后台服务隐藏运行，失败保留启动窗口并提示日志位置。仅当前 PowerShell 子进程设置执行方式，不修改系统执行策略，不自动下载安装依赖或关闭无关进程。
- 修改文件：一键启动.cmd、start.ps1、AGENTS.md、README.md、docs/architecture.md、本日志；没有新增或移动目录。
- 验证（通过）：实际经 Windows PowerShell 5.1 和 CMD 执行冷启动，前后端就绪且自动打开网页，退出码 0；在项目外的 D:/AI 目录再次执行 -NoBrowser，复用相同服务 PID，无重复服务。确认 Node 位于持久 PATH，文档相对链接有效。
- 修复记录：首次检测遇到 Vite 的 text/html 响应未带 charset，Windows PowerShell 5.1 将中文标题按错误编码解码；改为显式读取 UTF-8 响应字节后，冷启动与重复运行均通过。
- 文档同步：AGENTS.md 更新启动入口与文件树；README 更新双击启动步骤、失败排查、源码与运行产物的目录说明；architecture.md 更新启动机制与决策；本日志追加。接口约定、数据库迁移说明、配置模板、忽略规则及历史素材已核对，无变化，因为本次不改变接口、数据库或业务功能。
- 未执行：没有重跑业务 API 测试或前端构建，本次仅改变启动与说明；并发启动锁和端口冲突分支进行了代码核对，未单独模拟验收。
- GitHub / 密钥状态：未上传，未进行完整密钥扫描，仍为未核验。
- 下一步：日常直接双击根目录一键启动.cmd；团队新电脑先依 README 安装依赖，再使用启动器。

## 2026-09-17 / GitHub 首次上传准备（待确认）

- 负责人 / AI 工具：Codex。用户要求将完整项目上传到已创建、名称带 Agent 的 GitHub 仓库。
- 本地准备：初始化 main 分支并暂存 56 个文件，包含源码、依赖锁文件、启动器、文档与 10 个原始素材文件；初始化前无 Git 仓库，因此无本地历史提交需要追溯。尚未创建提交或上传；未改动业务目录。
- 密钥检查：直接读取 Git 暂存区内容，对 53 个文本或可提取文字的文件（含 PDF 与 DOCX XML）扫描常见服务令牌、私钥及带引号的凭据赋值模式，未发现匹配；人工核对 .env.example 为留空与占位符。公开演示账号凭据按 README 保留，不作为真实服务密钥。
- 排除项：本地数据库、环境、依赖、构建、缓存、日志及真实环境配置按 .gitignore 排除；未上传本地会话与用户记录。
- 检查遗漏：原始素材/output/imagegen 下三张 PNG 未做 OCR 或逐图视觉核验；两份 PDF 内嵌图像未做 OCR；模式扫描不能排除未知格式凭据。状态：已检查，待用户确认（包含上述遗漏）。
- 目标核对：已连接 GitHub 账号 scpxxx，但仓库搜索和可访问仓库列表未返回所述 Agent 仓库；尚不能确定准确地址或访问权限。需用户提供仓库链接。
- 文档同步：AGENTS.md 项目记忆、README 与本日志已更新；架构、接口约定、迁移说明、配置模板及忽略规则已核对无变化，因为本次仅准备版本管理与上传；历史素材原文不修改。
- 验证：暂存清单与忽略规则已核对；本次没有业务改动，未重复运行应用测试或构建。用户密钥确认尚未获得，未执行 GitHub 传输。
- 下一步：取得准确仓库地址及用户对真实密钥、模板、历史泄露和未自动核验图片的确认后，检查远端现状，再提交上传并核验。

## 2026-09-17 / GitHub 新账号连接核验

- 已核验账号 844346518scp-coder，仓库 https://github.com/844346518scp-coder/agent-school-llm-usts 为公开仓库，连接具有写权限。
- 只读 fetch 成功；远端 main 为 b4bebb0，已有两个提交，根目录包含 README.md、PCL.exe 与一张 AVIF 图片。未执行、删除或覆盖这些文件；尚未上传本地项目。
- AGENTS.md 已同步账号与仓库现状；其他文档与业务代码无变化。上传前密钥确认仍待用户回复；需明确此含既有文件的仓库确为目标。

## 2026-09-17 / 主动凭据核验与首次项目集成

- 用户明确要求在协作文件中增加 AI 帮助确认 API 密钥和其他秘密凭据，并直接上传、保留远端其他内容；以此作为本次委托检查和上传依据，不推定未来上传均获授权。
- AGENTS.md 补充主动核验、风险处理及明确授权后的执行规则；README 保留原仓库说明全文；架构记录接续远端历史的集成方式。原 PCL.exe 和 AVIF 图片字节不变，不执行程序，不删除历史。
- 检查：58 个暂存文件的文字或二进制可识别令牌模式无匹配；PDF 共 10 页均可提取文字且无嵌入图片，DOCX XML 已检查；三张规划 PNG 视觉检查未发现凭据。配置模板为空值和占位符，演示口令明确公开。本地数据库、真实环境配置、依赖、缓存和日志排除。
- 限制：模式与视觉检查不能保证检出所有秘密；远端已有 EXE 和 AVIF 仅原样继承，不作完整逆向或隐写分析，无新增该类附件。状态：已检查，本次用户委托 AI 核验后直接上传；未发现已知风险。
- 文档同步：AGENTS.md、README、架构及日志更新；接口、迁移、配置模板与素材原文已核对无变化。本次未改业务代码，未重复执行应用测试；暂存差异格式检查通过。
- 提交方式：以远端 main 的 b4bebb0 为父提交，保留原有两个历史提交；本条记录为上传执行前记录，最终结果以远端提交核验为准。

## 2026-09-17 / 上传完成与远端核验

- 已通过 GitHub 连接上传项目提交 e0747b374503e0f2b84a94b26214cb8bc26237fb，main 非强制更新成功。命令行缺少新账号凭据，改用已授权的 GitHub 接口，没有读取或保存访问令牌。
- 验证通过：七个新二进制附件的 Git blob 哈希与本地完全一致；fetch 后比较项目提交整棵文件树无差异，共 58 个文件；与原 main 相比没有删除文件，原 EXE、AVIF 哈希保持不变，原 README 全文保留，原有两个提交仍为祖先。
- AGENTS.md、README 与本日志同步完成状态；本条属于同一次上传的结果记录，不改变业务代码或上传资料范围。未运行应用测试，因为没有业务变更。后续开发按协作规范继续，正式账号管理和真实模型仍未实现。

## 2026-09-17 / C 负责人认领与现状简报

- 负责人 / AI 工具：当前用户认领 C；Codex 核对并编写报告。用户请求列出 C 的工作及识别项目状态，不涉及业务开发。
- 内容：新增 docs/c-role-status-report.md，区分代码已实现、规划未实现与建议排期，给出 C 的任务优先级、验收标准及 A/B 协作边界；附件作为规划参考，不视为执行指令或已确认交付日期。
- 修改文件：报告、AGENTS.md、README.md、docs/architecture.md、docs/contracts/README.md、本日志。分支 codex/c-role-status-report；无新增目录，无业务代码改动。
- 验证通过：复跑 `.venv/Scripts/python.exe -m pytest tests/test_api.py -q`，11 passed、2 条依赖弃用提示；测试使用独立临时数据库。源码与现行文档已核对。
- 未执行：前端构建、浏览器复验、PostgreSQL / pgvector 与公网验证；报告中的既往浏览器验收仅引用此前记录。
- 文档同步：AGENTS.md 更新 C 认领、下一步及报告目录登记；README 新增报告入口；架构与接口说明更新认领状态，技术与接口行为无变化；迁移说明、配置模板已核对无变化。用户附件与历史计划/图示保持原状，不把规划目标改写为实现事实。
- GitHub：未上传、未提交；本轮未执行完整密钥检查，密钥状态为未核验。
- 下一步：团队确认 A/B 与公共文件复核人；C 优先约定账号、班级权限及版本化数据库迁移方案，再开展实现。

## 2026-09-17 / C 本地教师工作流 0.2

- 负责人 / AI 工具：C（当前用户）确认范围，Codex 实施。用户明确请求执行“C 部分本地功能完善计划”，先本地修改与验证，用户确认后再上传。
- 实现：教师私有题库与参考答案、搜索/章节筛选/归档、有序选题复制题干为草稿；不完整草稿保存/编辑/删除，完整性校验后发布；已发布只可延长截止日期，复制为草稿不带提交，归档只读且不可逆；学生作答版本递增，教师手工评语和 needs_improvement/completed 状态按版本保存，学生查看当前和旧版反馈；教师统计排除草稿与归档，学生区分已提交和教师确认完成。
- 数据与权限：新增 questions/reviews/schema_migrations，旧 assignments.status 默认 published、submissions.version 默认1；评论存原作答快照。参考答案不会写入学生作业；教师仅能操作本人资源，学生只收到自己的反馈。SQLite 写事务 BEGIN IMMEDIATE 串行化检查与写入，防止批改和重新提交交错导致错误版本生效。
- 迁移与恢复：旧库先通过 SQLite backup API 备份，再用显式事务升级；失败回滚并拒绝启动。当前实际 demo.db 副本先逐表核对旧字段与记录保留，再启动原库升级；原库备份 backend/demo.before-v2-48afd0b8fad9.db 已生成并被 Git 忽略。restore.py 以只读方式核验备份，恢复到新路径，拒绝覆盖已有数据库。本轮0.2明确仅支持SQLite，PostgreSQL及其迁移后置。
- 修改文件：backend/app/main.py、platform/database.py、teaching/routes.py；frontend/src/App.vue、teacher/TeacherHome.vue、新增teacher/QuestionBank.vue、shared/AssignmentsView.vue、新增shared/FeedbackView.vue、shared/api.ts、shared/style.css、student/StudentHome.vue；前端两份包清单仅更新项目版本至0.2.0，无依赖升级；start.ps1 同步版本健康检查；新增 migrations/upgrade.py、restore.py、tests/test_migrations.py，扩展 tests/test_api.py；以及下述文档与配置说明。
- 分支 / 目录：codex/c-local-teaching；保留此前未提交的C报告与文档修改。只在现有目录新增普通文件，无新增业务目录。依赖、构建、数据库备份均为已忽略运行产物。
- 验证通过：`python -m pytest tests -q` 共19项，含原11项和新增题库保密、草稿隔离、生命周期、截止、跨教师/学生权限、反馈版本、并发批改/提交、统计及迁移/回滚/恢复场景；剩余2条是既有依赖弃用提示。`npm run build` 的TypeScript检查及生产构建通过，无大分块警告。
- 浏览器验收通过：在本地5173页面以师生演示账号跑通题库建题（公式）→选题生成草稿→保存→刷新恢复→发布→学生提交→教师待改进评语→学生重登查看→修改为第二版→旧反馈保留且当前待批改→教师确认完成→统计更新→归档→学生只读查看。另验证手机不完整草稿可保存；桌面界面和390×844手机尺寸的题库、草稿、教师批改及学生历史反馈无横向溢出，视觉检查通过；浏览器未记录脚本错误或警告。
- 启动验收通过：start.ps1 -NoBrowser 隐藏启动前后端，健康检查及前端代理就绪；实际数据升级后原演示记录仍在。当前服务保持本地运行。测试过程中保留明确标注的“演示验收 · 导数定义”题目、“演示验收 · 本地教师闭环”归档作业（两版反馈）、“演示验收 · 待完善草稿”，便于用户验收。
- 曾失败并已修复：实际旧库副本升级后的Windows临时目录清理发现sqlite3连接上下文未关闭文件句柄；改为contextlib.closing后，重复迁移、恢复及临时目录清理全部通过。浏览器批量操作曾碰到弹窗关闭动画与开发热更新重置页面，重新读取状态并等待弹窗关闭后继续，完整流程最终通过。
- 文档同步：AGENTS.md 更新0.2现状、范围决定、目录文件登记和下一步；README 增加验收路线、状态规则、升级及恢复入口；architecture.md 更新数据、事务、统计和本地优先决策；contracts/README.md 先于实现登记全部扩展接口并同步最终字段；migrations/README.md 更新升级/备份/恢复和SQLite限制；c-role-status-report.md 增加实施后状态并明确原分析为历史；.env.example 更新SQLite限制且仍无真实凭据；本日志追加。
- 已核对无变化：.gitignore 已覆盖数据库、备份、日志、环境与构建产物，无需修改；后端依赖与锁文件未增加依赖，无变化；历史计划、附件图示与原始材料保持真实性，功能规划不等于真实AI实现，不修改历史原文。
- 未执行 / 限制：未接入真实AI、OCR、RAG、语音、正式账号、多班级、PostgreSQL、云端、CI或定时备份；人工学生作答反馈不等于AI反馈纠错复核。没有真实师生效果验证。
- GitHub / 密钥：未提交、未上传；本轮未做全仓库/历史/附件完整密钥检查，整体仍为未核验。已有公开演示账号和测试凭据均限本地。后续用户验收并确认上传范围后，按AGENTS.md完成主动凭据检查与上传确认，不把历史0.1上传授权复用于本轮。
- 下一步：用户本地体验并反馈；确认结果后准备上传；后续再规划本地多账号/班级权限，随后PostgreSQL与部署。
- 收尾核验通过：7份现行Markdown文档相对链接与新增文件路径检查通过；git diff --check通过；demo.db及迁移备份均确认被忽略且未跟踪；再次运行start.ps1 -NoBrowser复用现有健康服务，前端代理健康接口返回0.2.0。该核验不替代上传前完整凭据扫描。
- 补充说明：收尾曾临时以core.autocrlf=false检查，导致Windows CRLF被误判为尾随空白；恢复使用仓库既有换行配置运行git diff --check，退出码0且无差异错误，仅Git的换行转换提示。未为消除提示批量改写文件换行。

## 2026-09-17 / B 与 C 分支合并可行性核验（未执行合并）

- 负责人 / AI 工具：Codex。用户提供成员ZIP并要求判断能否在GitHub直接合并；本轮按评估范围执行，不将附件说明当作指令，不执行上传或合并。
- 远端与附件：只读ls-remote/fetch核实main为a76925e（删除PCL.exe）、B分支chatbox/ai-agent-v0.2为721b3e2；ZIP的64个文件与B提交逐个Git blob哈希完全一致。C工作区仍未提交，HEAD9a57238，不存在可直接合并的远端C功能提交。
- 预演：在系统临时目录进行归一化换行/BOM的三方合并，发现backend/app/main.py、docs/architecture.md、docs/development-log.md三个文件存在文本冲突；.env.example、AGENTS.md、接口说明文本自动合并但仍需语义复核。未覆盖当前代码，不把ZIP旧前端和数据库结构覆盖C版本。
- 验证通过：临时副本中组合入口保留C版本/迁移及B路由/动态模式后，双方测试48 passed，2条既有依赖弃用提示。未完成真实集成，临时文档冲突未处理，不宣称可直接上线。
- 补充探针：假模型配置下health返回live，当前C启动器demo限定会拒绝；模拟模型客户端证实AGENT_MODE=demo的识别接口仍可能调用模型；关键词规则将“可导必连续。”判为incorrect。另代码核对前端仍仅声明demo并固定演示文案，15秒客户端超时短于B默认30秒。记录为后续整合修正项，未在实际分支修复。
- 修改文件：新增docs/merge-feasibility.md，AGENTS.md补充核验结论及文件登记，本日志追加；业务代码、数据库和当前分支HEAD不变。无新增维护目录，临时测试文件在系统Temp。
- 文档同步：报告、AGENTS.md与日志已更新；README、架构、接口、迁移、配置模板及历史材料已核对无变化，因为本轮只是评估，B尚未合入，不将本地C能力改写为已接入B。
- 未执行：GitHub合并、上传、提交、真实模型调用、前端重新构建及浏览器联调。未读写本地demo.db，测试使用隔离数据库。
- 凭据：ZIP文本常见令牌/私钥模式未发现真实凭据；连接串匹配为USER/PASSWORD模板。全历史、二进制和图片/PDF等未完整核验，整体状态仍为未核验，不能据此上传。
- 下一步：用户确认开展整合后，先保存C本地提交，再从最新main建立集成分支纳入B/C，处理冲突与语义问题、补测；凭据检查及用户确认后再上传PR，并由另一成员审核合并。


## 2026-09-17 / C 独立分支上传准备与凭据检查

- 负责人 / AI：Codex。用户明确要求以新分支上传当前本地项目；准备 codex/c-local-teaching，保留原9a57238基线。只读fetch确认最新main为4bd8519，已合并B的PR #1；本轮不整合B、不更新main、不部署。
- 范围：27个新增/修改的文本文件，包含C教师流程、迁移、前端、测试、配置模板、C简报和合并评估；完整候选树65个文件。9个既有附件与HEAD逐字节一致；PCL.exe仍为旧基线继承，本次无二进制新增，后续合并应保留main对它的删除。
- 凭据检查：候选工作区全部文件进行常见API令牌、私钥与凭据字面量扫描，未发现真实秘密；认证相关代码为运行时生成/读取，演示账号和测试样例为公开或合成数据，.env.example模型密钥等为空。核实环境配置、demo.db、迁移备份、依赖、构建和日志的忽略规则，均不进入提交。暂存区与新增提交将再次扫描核对。
- 历史/附件：提交前HEAD没有远端未包含的历史提交；本次仅增加C文本提交。附件哈希未变，沿用此前0.1上传时PDF文字/DOCX XML/三张PNG视觉检查结论；本轮不重复视觉检查，不对EXE逆向或隐写作保证。模式检查不能保证检出所有秘密。其他成员ZIP不纳入上传范围。
- 状态：已检查，待用户确认。已提醒“请删除待上传内容中的 API 密钥及其他凭据，并确认密钥状态。”依据AGENTS.md第6节，本次密钥状态确认后才向GitHub传输内容，不复用0.1历史授权。
- 验证：沿用同日当前C代码19项后端测试、TypeScript/生产构建及完整浏览器闭环的通过结果；本轮仅准备提交与同步文档，无业务变更，不重复运行应用测试。暂存差异格式与提交文件检查在提交时执行。
- 文档同步：AGENTS、README、架构、合并评估和本日志同步分支准备及远端状态；接口、迁移、C简报、配置模板、目录树与原始材料已核对，无功能或路径变化，无需另改。历史日志保留原状。

- 提交前复核通过：27个暂存文本文件的令牌/私钥/带口令连接串扫描无匹配，git diff --cached --check退出码0；无新增附件或本地数据。


## 2026-09-17 / C 独立分支上传完成

- 用户确认依据：在收到27文件范围、凭据检查结论与第6节提醒后，用户回复“没有，开始上传”。据此执行本次上传。
- 结果：GitHub新分支codex/c-local-teaching创建成功，功能提交955bbbe41560f3747bf578cc029e9766e5f3b27d，父提交9a57238；不改main或B分支、不部署。命令行认证未完成，终止该次push后使用已连接GitHub接口上传，无需读取令牌。
- 验证通过：远端功能树9bada338c6cc302abff34ca7c8769f515d3d4ef6与本地f332f59的文件树相同；fetch后git diff退出码0，65个文件逐项一致。接口创建提交使提交ID不同，文件内容未变；本地分支已关联远端，原本地提交保留在reflog。
- 文档同步：AGENTS、README、架构、C简报、合并评估和本日志更新完成状态；接口、迁移、配置、原始材料及目录树已核对无变化。这些结果记录属于同一次已确认上传的文档范围，上传前再次检查。无业务变更，沿用19项测试、构建及浏览器验收结果。
- 下一步：在独立集成分支处理B/C冲突及已记录兼容问题，验收后另行审核合并；本次未执行合并。


## 2026-09-18 / Windows免安装测试包与启动诊断

- 负责人 / AI：Codex。用户反馈其他电脑双击失败，截图明确为Frontend dependencies missing；根因是源码分发不携带node_modules及Python运行环境，原启动器要求开发环境。截图不证明Node或Python一定未安装，只证明前端依赖检查未通过。
- 分支/范围：从已上传C分支创建codex/portable-windows。新增根目录build-portable.py、backend/app/platform/portable.py、docs/portable-windows.md、tests/check_portable.py，修改start.ps1、health可选实例字段及忽略规则。未与B整合、未上传GitHub。无新增维护目录；dist和.tmp为已有忽略规则覆盖的构建/验收产物。
- 实现：开发机校验Python锁定包版本并构建前端，按允许列表打包CPython3.11.15完整运行时/标准库/原生DLL/依赖及许可证、前端静态资源、启动脚本和说明。运行时以_pth隔离系统Python和用户扩展，不复制.venv；测试端无需Node、pip或联网安装。仅复制应用.py和必要构建文件，不携带本机.env、数据库/会话、日志、Git、原始附件、PCL.exe或node_modules。
- 启动：有portable-manifest.json时使用包内runtime，默认127.0.0.1:8765（-Port可调），后台启动FastAPI并托管静态页面；校验本目录instance_id、页面和端口，文件锁防并发、重复启动复用，冲突明确报错且不结束别的进程。固定本包backend/demo.db，避免继承系统DATABASE_URL。源码模式仍使用8000/5173，缺依赖时提示测试者改用便携包。
- 纠正：初查曾怀疑main.py的静态路径；按实际父目录层级复核后确认原parents[2]正确，保留原实现，未作路径修复。
- 验证通过：TypeScript检查、生产构建；后端19项测试通过（2条既有弃用警告）；内置解释器导入检查。实际ZIP解压至中文/空格路径，在移除Python/Node的子进程PATH并设置无效PYTHONHOME/PYTHONPATH、外来DATABASE_URL后，逐文件哈希、无数据库/.env、首次启动、静态资源、登录、草稿保存、结束并重启服务后恢复、重复启动复用及外部端口占用拒绝全部通过。测试只结束自己创建的后端进程，不触碰原8000服务或本地demo.db。
- 曾失败及处理：首轮pytest受C盘剩余空间为0影响，出现SQLite database or disk is full；将当前测试进程TEMP/TMP改为D盘已忽略.tmp后，19项全部通过。未清理用户文件或修改全局环境变量。
- 产物：dist/shuban-windows-x64-20260918-082545.zip，29,513,315字节（约28.1MiB），SHA256为2921093b9809c8efe59170aee433f981608d165239ae9d5afeee64c5cddd7c39。配套.sha256文件和包内portable-manifest.json记录版本、来源提交/未提交状态及每个文件哈希。验收使用ZIP副本，原ZIP没有运行数据。
- 文档同步：AGENTS更新状态与目录文件登记，README新增测试者分发方式，architecture登记运行机制，contracts登记health可选字段，portable-windows说明构建、测试、端口和升级。迁移说明、配置模板、C简报、历史合并评估和原始资料已核对无变化：数据模型/业务范围/B-C整合状态均未变，历史报告不重写。
- 限制：当前验证为本机隔离环境模拟未安装开发工具，并非另一台干净Windows实机；Windows10/11 x64为目标，ARM/Mac/Linux及学校管理策略未验证。包是当前C版，B未集成；无真实AI或跨电脑数据同步。GitHub源码ZIP仍不含运行环境，需要把此便携ZIP作为测试分发物另行发送/发布。

- 追加修正：第一版ZIP在CMD复验中发现Windows预留8728–8827导致8765/8768绑定报10013。已改为自动模式优先18080、记住成功端口，并在占用/系统保留时使用系统分配可用端口；显式指定不可用端口则明确报错。无需关闭其他应用或更改系统预留规则。
- 第一版082545包已被083356最终候选替代，勿分发旧包。最终候选dist/shuban-windows-x64-20260918-083356.zip为29,513,577字节，SHA256：4da7edc6d91defe396aa37a24abf0fbe50cbafd78701a0b857ab467f1c8719b9。应用/前端/启动脚本常见令牌私钥模式无匹配；ZIP清单无.env、数据库、日志或.git。第三方运行时未做逆向/隐写审计。

- 最终验收通过：对083356 ZIP重新执行tests/check_portable.py，含此前全部场景及自动换端口、记住端口、实际CMD入口复用，全部通过；前端再次构建和包内导入通过。Python语法检查、git diff --check、文档相对链接及忽略规则核验通过。最终可交付上述083356包；源代码保持本地未提交、未上传。

## 2026-09-17 · B 模块（智能体）最小版：真实模型接入 + 课程检索引用 + 步骤反馈 + 基础诊断

- 日期 / 任务：2026-09-17 / 对齐 9/20 最小版：课程问答 · RAG 引用 · 拍照识别与确认 · 步骤反馈 · 错题收藏 · 历史记录。
- 负责人 / AI 工具：B 模块成员（待填）/ Chatbox。
- 开发了什么与原因：
  - 把只有固定演示文案的问答升级为“配置 → 课程检索 → 分层提示 → 模型调用 → 如实降级”的骨架；没有模型密钥时仍走完整流程，但只返回演示内容并标注 `mode=demo`。
  - 新增课程检索（本地 BM25，中文按字 bigram）与引用编号，解决“回答没有资料来源”的问题；知识点全部标记 `verified=false`，待课程资料复核。
  - 新增步骤反馈接口：只判断学生本次提交的一步，不返回整题答案；demo 模式只标错、不说“正确”。
  - 新增基础诊断接口：规则给出薄弱知识点与变式练习，结论可追溯到命中的关键词。
  - 新增能力探针 `/api/agent/status`，便于前端与测试脚本判断当前是 demo 还是 live。
- 修改文件：
  - 新增 `backend/app/ai/config.py`、`knowledge.py`、`prompts.py`、`llm.py`、`diagnosis.py`。
  - 修改 `backend/app/ai/service.py`（保留原演示文案与既有响应结构，新增 `/api/agent/*`）。
  - 修改 `backend/app/main.py`（注册 `agent_router`；`/api/health` 的 `agent_mode` 改为如实反映生效模式）。
  - 新增 `tests/test_agent.py`、`tests/smoke_agent.py`。
  - 修改 `.env.example`（新增 `AGENT_MODE`、`MODEL_VISION_NAME`、超时与检索参数，全部为空值或默认值）；修改 `.gitignore`（忽略冒烟输出）。
- 验证命令或方式 / 结果（通过、失败、未执行）：
  - `python -m pytest -q` → **40 passed**（含原有 11 项 API 测试全部通过）。
  - `python tests/smoke_agent.py` → **通过**：登录、`/api/agent/status`、`/api/agent/ask`、`/api/agent/feedback`、`/api/agent/diagnosis`、`/api/agent/ask/stream`（SSE 3 帧）行为符合预期；`/api/agent/recognize` 按设计返回 503（未配置模型，不返回编造结果）。
- 目录结构变更 / 用户确认依据：**无新增目录**，只在既有 `backend/app/ai/` 与 `tests/` 下新增文件。
- 文档同步清单（逐项写已更新，或已核对无变化及原因）：
  - AGENTS.md 项目记忆与目录：已追加“B 模块（智能体）实现说明（2026-09-17）”。
  - README.md：已核对无变化（本次不涉及启动方式与仓库总览）。
  - docs/architecture.md：已追加“智能体（B）处理流水线（2026-09-17 追加）”。
  - docs/contracts/ 下全部约定：已更新 `/api/health` 行与“仍待定义”段，并追加“智能体接口 v0.2（B 模块）”。
  - 其他相关说明、配置模板、计划与图示：已更新 `.env.example`、`.gitignore`；无新增图示。
- GitHub 上传 / 密钥状态 / 检查范围 / 用户确认依据：**未上传**；新增配置项全部为空值或默认值，仓库内无真实密钥，`.env` 未入库。
- 遗留问题与下一步：
  1. 课程知识点内容需课程资料复核，复核后把 `knowledge.py` 中对应 `verified` 置为 True。
  2. 配置真实模型（`MODEL_BASE_URL` / `MODEL_API_KEY` / `MODEL_NAME`）后重跑 `tests/smoke_agent.py`，确认 live 链路与超时降级。
  3. 前端（A）按 `docs/contracts/README.md` 的 v0.2 接入 `mode`、`references`、`/api/agent/feedback`、识别确认流程。
  4. 9/21–24 再补：长期记忆、推荐练习的个性化、资源检索与总结评价、语音。

## 2026-09-18 / 普通源码自动初始化与 B/C 本地集成完成

- 负责人 / AI：Codex。用户明确纠正为“其他人下载原本文件夹即可运行，而不是打包版”，并要求合并B、处理兼容性与更新文档。本条替代前一便携包方案作为默认分发方式；历史便携工具和记录保留。
- 基线：用户提供的agent-school-llm-usts-main.zip与origin/main提交4bd8519的64个Git文件逐字节一致；main已包含B提交721b3e2。先把既有便携工作保存为本地40df412，再建codex/source-bootstrap-integration，合入origin/main。保留C全部教师功能与数据迁移，保留远端PCL.exe删除；入口、架构、接口和日志冲突合并双方内容。本次只保存本地合并提交，没有push或改动远端main。
- 启动修复：新增根目录bootstrap.ps1，项目内下载并SHA256校验固定Python3.13.13、Node22.23.2、pip26.2.1；安装后端锁定依赖与npm ci，按源码指纹构建前端。start.ps1默认自动初始化后同源启动，-Dev保留原开发模式，-SetupOnly仅准备环境。首次联网、缓存复用，无管理员、系统PATH或全局开发工具要求。全部运行环境、缓存和临时文件位于已忽略生成目录；未新增维护目录。
- 兼容修复：main同时保留C数据库初始化和B路由，health支持demo/live及agent_version；启动器不误复用旧C服务。前端AI等待90秒，模型网络超时限制1–60秒；错误不自动重试。问答和历史按每条回复来源标识模型/演示，显示引用、待复核与降级原因；来源前缀避免模型引用演示字样时被误判。demo禁止识别/模型调用，关键词步骤反馈仅unclear；SSE中途失败以replace标记替换未完成输出。模型错误不回显提供商响应体/凭据。数据库版本仍为2，无新迁移。
- 涉及文件：bootstrap.ps1、start.ps1、platform/portable.py、main.py、B模块config/llm/diagnosis/service及其合入文件，前端App/StudentHome/AgentView/RecordsView/api/style，tests/check_source.py/test_api.py/test_agent.py/smoke_agent.py，build-portable.py及下列文档/忽略配置。冒烟脚本改为独立临时库且强制demo，避免改动实际数据或调用用户模型。
- 验证通过：原C19项+B29项共48项首先通过；新增9项兼容回归后，在自动下载的Python3.13环境完整57项通过（2条既有弃用警告，268.91秒）。来源前缀最后调整后针对性回归1项通过。独立B冒烟覆盖登录、状态、问答、反馈、诊断、SSE三帧与识别预期503通过。TypeScript及Vite生产构建通过。
- 源码验收通过：tests/check_source.py从受版本管理源码建立不含.env/数据/依赖/构建的新副本，在无Python/Node的PATH及无效PYTHONHOME/PYTHONPATH下，真实下载安装构建、登录、私有题库、草稿发布、学生提交、教师反馈、B演示问答引用、静态资源、重复启动复用、重启持久化均通过；禁用网络代理后缓存启动通过，live使用本地假模型验证。最终bootstrap再复制到中文/空格验收目录，以实际一键启动.cmd复验成功并核验health，结束该验收自身PID。未对其他电脑实机作已验证声明。
- 浏览器验收通过：本地18080页面登录、问答、公式、引用待复核、刷新保留登录及历史来源标记正常；保留原数据库记录，并新增一条明确的集成验收演示问答。源代码服务继续在18080供用户体验，原8000后端未终止。旧Vite/esbuild因占用本项目依赖，核对所属PID后停止并重新安装；不终止无关进程。
- 失败与修正：npm ci曾因本项目旧esbuild锁定报EPERM，停止所属开发服务后重装成功；首次隔离冷启动因继承PSModulePath导致Windows PowerShell找不到Get-FileHash，改为进程内补全系统模块路径后通过。嵌入式Python相对_pth导致pytest无法导入backend，改为每次生成UTF-8绝对项目/依赖路径，导入及针对性测试通过。冒烟新增断言曾误将创建记录的201当作200，按既有接口约定修正后通过。C盘无剩余空间，所有本轮安装/测试临时目录使用项目盘，未清理用户文件。
- 文档同步：AGENTS（状态、目录、下一步）、README（源码使用与边界）、docs/source-startup（新）、architecture、contracts、merge-feasibility、c-role-status-report、portable-windows、migrations/README、.env.example和本日志均已更新；.gitignore覆盖生成物。其他原始PDF/DOCX/图示已核对无变化，属于历史依据，不改写为当前实现。B原始日志“关键词标错/薄弱结论”等为历史行为，以本条unclear/候选复核说明为现行规则；原smoke真实模型建议已被当前隔离演示脚本替代。文档相对链接、冲突标记、PowerShell语法、差异格式与忽略规则在提交前核对。
- 限制/下一步：尚未执行另一台Windows实机、真实模型效果、正式身份/多班级、PostgreSQL或云部署验收。首次需要可访问下载源且项目目录可写；Windows10/11 x64是自动启动目标，其他系统未支持。此次未做新的完整上传凭据审计、未上传；现有GitHub旧ZIP不会自动修复，后续确认上传范围并履行凭据检查/确认后才发布。当前本地可运行与远端main状态须区分。

## 2026-09-18 / 原 C 分支更新准备与凭据核验

- 用户要求“提交到原来的github分支上”；目标为origin/codex/c-local-teaching，fetch核实仍为f841e01，且是集成提交b8d6fdf的祖先，可以非强制快进；不修改main。
- 待上传范围：源码自动初始化、B/C集成及兼容修复、测试与文档，以及之前本地保存的可选便携工具；对原C分支35条文件变化（含保留远端PCL.exe删除）。不上传运行包、.env、数据库、缓存、日志和安装环境。
- 凭据检查：78个跟踪文件及43个尚未被origin引用的历史blob，共113次文本模式扫描，API令牌、私钥、带凭据URL无匹配；模板敏感配置为空，测试值为合成值，公开演示账号保留。暂存区原为空；此条及AGENTS状态更新提交前复核。8个既有二进制附件与远端C内容相同，无新增附件；本轮未重新作视觉或隐写审计，不能保证自动扫描覆盖所有载体。B既有提交已在远端main，本轮当前文件仍纳入扫描。
- 验证：工作区原干净、远端祖先关系及忽略规则通过；本轮仅上传准备和状态记录，沿用上一条57项测试、源码冷启动及构建结果，不重复应用测试。
- 文档同步：AGENTS与本日志更新准备状态；README、架构、接口、迁移、启动说明和历史材料已核对，无功能变化，现有“尚未上传”仍准确，无需改写。
- 状态：已检查，待用户确认。按AGENTS第6节提示删除待上传内容中的API密钥及其他凭据并确认状态；前次C上传确认不覆盖此次新增源码/历史范围。尚未传输内容，确认后再更新同一分支。


## 2026-09-18 / 原 C 分支上传并合入 main 完成

- 授权依据：用户在收到凭据检查范围、遗漏与上传提醒后明确要求“上传分支，之后与mian分支合并不要修改其他分支”，按main处理。此次仅更新codex/c-local-teaching与main，不修改B或其他远端分支。
- 上传：git非强制push将原C分支从f841e01更新至5e4e850；沿用仓库既有PR #2，更新其功能描述与验证信息。GitHub确认mergeable，以期望head=5e4e850限制合并，成功生成main合并提交b52f1949f106de83a177a07c75ae146420afbf22。
- 凭据：沿用上一条78文件/43历史blob检查，本轮新增内容仅为审核过的状态说明，不含秘密值；上传前已再次提醒。既有附件未变且本轮未重新视觉审计，保留该限制。
- 验证：fetch确认main合并结果，本地快进成功；应用代码与已验证集成版本一致，沿用57项测试与构建/冷启动结果。本轮只同步发布状态文档，提交前检查差异格式与凭据模式；完成后核对两个目标分支及未修改的B分支。
- 文档同步：AGENTS、README、架构、合并评估、源码启动与本日志更新已发布状态；接口、迁移、C功能说明、配置及原始素材已核对无功能变化，无需修改。后续仍需其他Windows实机和真实模型效果验收；上传不是公网部署。

## 2026-09-19 / 全部现行文档状态统一

- 负责人 / AI工具：当前用户认领C；Codex执行。用户要求更新所有文档信息，本轮只核对现状并统一文档，不修改业务代码、数据、目录结构或远端仓库。
- 统一结论：当前权威基线为数伴0.2本地B/C集成版；C教师题库、作业生命周期、版本化作答、人工反馈、统计和SQLite v2迁移已完成，集成代码已更新原C分支并通过PR #2合入main。真实模型效果、另一台Windows实机、正式账号、多班级、PostgreSQL、AI反馈复核、审计和公网部署仍未完成。
- C下一阶段：把“本地教师闭环验收/准备上传”从当前待办中移除，改为先形成正式账号、班级、成员、课程、教师授权和资源可见范围的数据/接口方案，再实施多班级后端权限和教师班级管理；至少用两个教师、两个班级和多个学生做横向越权验收。PostgreSQL、AI复核和部署在此之后分阶段验证。
- 修改文件：AGENTS.md、README.md、docs/architecture.md、docs/c-role-status-report.md、docs/contracts/README.md、docs/merge-feasibility.md、docs/source-startup.md、docs/portable-windows.md、migrations/README.md及本日志。无新增、删除、重命名或移动目录。
- 文档同步：AGENTS更新日期、项目记忆和下一步；README增加当前状态与C任务入口；架构修正旧集成状态、源码启动、AI规则边界和下一阶段权限原则；C报告重写为当前权威状态；接口补充当前单演示班级限制与下一阶段约定范围；合并报告明确为已完成后的历史快照；源码/便携说明区分当前默认方案与旧C-only ZIP；迁移说明明确下一次数据模型变化必须新增版本。本日志保留全部历史记录，不回写旧条目。
- 已核对无变化：.env.example配置注释仍准确且没有真实密钥；.gitignore、依赖锁文件和原始PDF/DOCX/图示/提示词均不受本次状态统一影响，保持原文。历史材料中的分工日期和功能规划不改写为当前实现。
- 验证：文档相对链接、过期现行表述、冲突标记、Markdown标题、目录树引用和`git diff --check`在本条完成后统一检查；结果以随后收尾记录为准。未运行后端测试、前端构建、浏览器、PostgreSQL或真实模型，因为本轮没有代码变更；最近完整应用验证仍是2026-09-18的57项集成测试及对应构建/联调记录。
- GitHub / 凭据：未提交、未上传，未进行新的全历史/附件凭据审计；本轮只是本地文档修改，不产生上传授权。以后若上传这些新增内容，必须按AGENTS.md第6节重新检查范围、提醒并取得用户确认。
- 下一步：用户确认文档口径后，再单独制定账号与多班级数据/接口方案；尚未获得直接修改数据库或新增目录的授权。
- 收尾只读远端核对：发现当前工作区HEAD为285f1aa，而`origin/main`已到168928d；其后两个提交cb5a9c5/168928d增加`PhotoSearchDialog.vue`、AgentView拍照入口、两个根目录静态原型，并重新加入此前删除的`PCL.exe`。这些文件不在当前工作区，本轮未合并、执行、删除或修改。
- 远端增量事实边界：Vue拍照入口复用既有`POST /api/agent/recognize`，贡献记录为TypeScript/Vite构建通过，未做浏览器真机、401/502/503后端联调或真实视觉模型验证；独立“AI视频解析”原型默认请求当前后端不存在的`/api/agent/video-search`，只能列为原型。远端日志中“未上传/未push”属于提交产生前的历史描述，当前提交已客观存在于main，不能继续当作现状。
- 因上述发现进一步同步：AGENTS、README、architecture、C状态报告、contracts、merge-feasibility和source-startup均增加“当前工作区/远端main”区分；portable与migrations原有版本边界仍准确。开始多班级开发前，C应先协调A侧复核拍照共享前端、决定原型与`PCL.exe`处理，并形成共同分支基线。
- 收尾验证通过：仓库10份Markdown现行文档全部在本次产生更新；全部本地Markdown链接可解析；无合并冲突标记；`git diff --check`通过（仅显示仓库既有LF→CRLF提示，不是差异错误）；只读`git grep`确认远端main后端没有`video-search`路由。应用测试、构建和浏览器仍未执行，原因同上。

## 2026-09-19 / 教师工作空间 v0.3、真实班级与全部文档同步

- 负责人/工具与授权：当前用户负责C，Codex及并行后端/前端/测试代理实施。用户后续授权“把教师端演示用的部分落实、替换演示”，并要求更新全部文档；本条取代较早文档统一阶段的“暂不改表/账号班级待设计”。没有上传、合并远端或新增维护目录的授权。
- 分支/范围：从285f1aa建立本地codex/teacher-workspace，保留原有未提交文档修改。未合入远端168928d拍照/独立原型/PCL.exe，未修改其他分支；本轮源码和文档未提交、未上传。
- 功能：空库先创建首位教师，不默认植入公开账号/作业；教师可创建同级教师和所属学生、修改本人姓名/密码、维护学生姓名/停用/重置。新学生/同级教师须首登改密；后端限制改密前业务访问，改密/重置撤销旧会话。保留PBKDF2摘要、Cookie和个人问答隔离，不声称正式身份或公网安全已完成。
- 班级/作业：新建编辑班级、归档/恢复、成员加入/移出/恢复；学生账号创建归属与班级成员分别管理。发布需有效目标班和至少一位有效学生，事务内固定收件人；后加成员不自动得到旧作业，退班保留历史只读，归档班级禁止提交/反馈。复制归档班级作业时新草稿不选班，避免无法编辑；未归档班级复制保留班级。
- 教师界面：真实头像/姓名、真实班级/学生人数、应交/已交/未交及人工反馈统计，按班筛选，成员搜索与当前列表CSV导出。CSV使用UTF-8 BOM、引号转义及公式前缀防护。学生待办尊重can_submit/class_archived，不把退班或归档任务继续提示为可提交。
- 数据：schema v3新增classrooms/class_members/assignment_recipients，users新增active/must_change_password/is_demo/created_by，assignments新增class_id。旧库先before-v3备份再事务迁移，每位旧教师建立历史教学班，保留旧可见范围/提交/反馈；单教师旧库可推定学生账号管理人，多教师不擅自赋权。SHUBAN_SEED_DEMO显式开启且用户表为空才种子初始化，不随重启覆盖已有数据。
- 启动兼容：health保留version/agent_version0.2.0，新增teaching_version0.3.0与schema_version3。源码/便携启动器和-Dev检查新增标识；同目录旧后台明确拒绝复用，不自动换端口绕过旧库写入者。不保证发现任意其他路径/端口进程，更新前仍须停止所有同库写入。
- 源文件范围：backend/app/main.py、platform/auth.py/database.py/portable.py、teaching/routes.py及新增classes.py；frontend/src/App.vue、teacher/TeacherHome.vue及新增ClassesView.vue、shared/LoginView.vue/AssignmentsView.vue/api.ts/style.css及新增AccountSettings.vue、student/StudentHome.vue；migrations/upgrade.py、start.ps1、.env.example。没有新增依赖或维护目录。
- 测试范围：新增tests/test_teacher_workspace.py（15项）、test_teacher_migration_v3.py（4项）、test_launcher_versions.py（5项）；调整既有test_api.py/test_migrations.py/test_agent.py和smoke_agent/check_source/check_portable的隔离、显式种子及目标班级。所有模型测试使用demo或本地模拟，不调用真实服务。
- 验证通过：独立新增19项HTTP/迁移；B单独36项；最终完整pytest tests -q为81 passed、2条既有依赖弃用警告、309.30秒。B独立冒烟登录/状态/问答/反馈/诊断/SSE及演示识别预期503通过。前端vue-tsc和Vite最终构建通过，Python编译、PowerShell语法通过。
- **发现、影响与修正**：早期完整测试中，B公开状态探针的with TestClient(app)未独立隔离启动；在test_api去除全局DATABASE_URL副作用后，于15:54:22启动了真实main并把backend/demo.db迁至v3。没有执行测试数据清空或业务写入。自动备份backend/demo.before-v3-fa3c72f99b74.db保留；只读逐项比较users2/sessions2/conversations4/assignments4/submissions2/questions1/reviews2的原字段全相同，新增历史班级1/成员1/收件记录3，完整性通过。不能把本轮写成“未触碰实际库”。
- 隔离修复：test_agent公开探针改为不运行lifespan，装initialize_database禁止调用哨兵；其测试模块默认demo。test_api显式独立engine/sessionmaker及依赖覆盖，避免导入顺序误用默认库。修复后完整81项重跑前后实际demo.db的SHA256、118784字节大小、修改时间完全一致，未新增迁移备份。早期81项虽然断言通过，但不能作为隔离验收结论，最终以修复后重跑为准。
- 实际旧库复验通过：用上述真实v2备份以mode=ro+SQLite backup复制到.tmp/actual-v2-migration-9ecc43bd0fee43c7a4190420eec3e1db.db，验证原表原列全等、before-v3副本备份完整、升级v3及重复运行幂等；不回滚或覆盖当前用户库。
- 服务升级通过：先实测旧18080版本拦截exit1且不关进程。再核对进程命令/目录，仅停止本项目旧25288及8000的34492/43088，以新版启动18080（当次PID27416）；health含v0.3/v3，首页及9个JS/CSS/SVG资源200，重复启动复用同PID，8000无残留监听。重启前后全11张表所有字段逐记录一致。QA18319使用独立.tmp库，不混入用户数据。
- 浏览器通过：独立测试库无默认演示数据，显示首位教师建档；API创建合成账号并验证首登改密，页面完成教师登录→建班→读取名册→选班发布→学生登录提交→教师读取并保存人工反馈→班级1/1提交及完成1的真实统计。默认桌面与390×844手机尺寸班级页无横向页面溢出，浏览器未见JS错误。账号设置/临时密码的安全逻辑由HTTP回归覆盖，不声称所有密码表单均已手动验收。
- 浏览器限制：直接启动Chrome受环境策略限制，改用应用内浏览器进行页面验收。点击CSV导出未捕获download完成事件，控制台无错误；导出生成逻辑已实现并检查，但未取得下载文件，不宣称CSV落盘或Excel兼容已验证。此项保留给标准Chrome/Edge实际下载验收。
- 文档同步（全部现行10份）：AGENTS更新规则时点、状态/树/分支/数据升级事件/待办；README更新首用、账号、班级发布、v3数据与边界；architecture更新模型/授权/快照/启动决策；contracts更新v0.3端点、字段、权限及兼容；C报告更新能力/限制/验收；migrations说明真实迁移、备份和恢复；source-startup说明首次建档、种子与健康标识；portable-windows明确历史包不含v0.3且未重建；merge-feasibility保留历史并补当前未发布分支；本日志追加事实不改历史。配置模板同步SHUBAN_SEED_DEMO=false及schema v3。
- 已核对无变化：.gitignore已排除数据库/备份/.tmp/运行时/日志；前后端依赖锁文件无新增依赖；历史建设计划书、原始PDF/DOCX/图示保持原文，本轮不改写材料或编造来源。本次业务实现不需要新增课程材料，未把内置BM25课程资料改标为已复核。
- 未执行：另一台Windows实机、完整源码重新冷下载安装、便携重包及隔离ZIP、真实模型/视觉模型、真实师生试用、PostgreSQL/pgvector、公网部署。check_source/check_portable仅适配了新参数，没有沿用0.2结果声称本轮重跑。
- GitHub/凭据：未提交、未上传、未做本轮完整待推送历史/附件秘密审计，无本轮上传授权；公开演示凭据仅用于明确的种子与合成测试。任何后续上传按AGENTS第6节重新执行检查、提醒及确认。
- 下一步：用户体验真实班级与反馈流程；完成标准浏览器CSV下载和新机验证，与A协调拍照/原型增量；正式身份、共同授课、AI复核、PostgreSQL和部署仍分阶段推进。收尾链接/差异检查和QA清理见随后记录。

## 2026-09-19 / v0.3 收尾补强与文档一致性核对

- 负责人/工具：Codex主代理及前端、测试代理，延续上条用户授权；未扩大到远端集成、正式认证、模型实连或部署。
- CSV加固：新增现有shared目录内的classProgress.ts，将成员搜索/状态筛选与CSV生成共用；链接挂载后点击、移除并延后1秒释放对象URL。新增现有tests目录内的test_class_progress.mjs，不增加依赖或目录。UTF-8 BOM、中文/逗号/引号/多行往返、公式及前导空白防护、当前列表筛选、正常/异常下载清理共6项原生Node测试通过；主代理收尾复跑仍6 passed。首次测试用Blob.text读取时BOM被解码移除，改为arrayBuffer逐字节断言后通过，未为适配测试改变实际导出格式。
- 后端测试补强：test_teacher_workspace.py明确检查同级教师首次改密、跨班学生去重，以及退班/停用不改历史应交/提交/反馈；test_teacher_migration_v3.py明确检查旧作业class_id非NULL，并新增多教师历史账号manageable=false、账号修改拒绝但本班成员可维护的HTTP回归。4项针对性测试通过（26.54秒），共可收集82项；没有重新完整执行82项。此前隔离修复后的完整81项通过仍是最近全量回归，不能写为“82项全量通过”。
- 前端最终vue-tsc与Vite构建通过。加固后在独立QA库重新登录并查看班级1/1提交、完成1；点击导出仍未在10秒内捕获download事件，浏览器error/warn日志为空。不能确定是应用内浏览器下载支持还是其他兼容问题，未得到实际文件，不宣称落盘/Excel兼容通过；该限制保留给标准Chrome/Edge验收。
- 文档同步：AGENTS登记新增文件、准确测试数量及当前下一步；README补CSV导出范围/保管提醒/Node命令；架构登记筛选、序列化及下载生命周期；接口补CSV列与前端生成边界；C报告更新已验收范围和剩余交付检查；迁移说明补多教师历史账号权限回归；本日志追加收尾，不删除或改写历史结论。
- 已核对，无进一步变化：source-startup的源码初始化和冷启动未重验边界、portable-windows的历史ZIP与未重包声明、merge-feasibility的本地/远端和历史合并边界均仍准确；.env.example与.gitignore无需再改，依赖锁文件及历史原始资料保持原状。全部10份现行Markdown均已在本轮同步，历史计划/附件不改写为已实现。
- 收尾检查通过：10份Markdown的本地链接均可解析，无合并冲突标记；默认git diff --check通过，仅既有LF→CRLF提示。实际库、自动备份、QA库和前端dist均被忽略。实际demo.db哈希仍与81项完整回归后的基线一致，未出现本次QA业务数据混入。
- 测试环境：QA页面已退出合成账号并关闭。收尾只读进程/监听检查时原QA PID27824已不存在，18319/18080/8000均无监听；未据此推断退出原因，也未继续重启或终止其他进程。没有删除任何数据库、备份、QA产物或用户文件；需要体验时双击一键启动.cmd，不承诺后台仍在运行。
- 交付状态：教师端原固定班级/人数已由持久化账号、成员、发布快照和人工反馈统计替换。真实模型效果、学校身份体系、跨教师协作、PostgreSQL、标准浏览器CSV落盘和新机器交付验证仍明确列为未完成，不把本地流程等同于正式上线。分支仍为codex/teacher-workspace，未提交、未上传、未合入远端增量。

## 2026-09-19 / 教师 v0.3 上传前检查（待确认）

- 目标：把本地`codex/teacher-workspace`的本轮改动提交并快进上传到既有远端分支`codex/c-local-teaching`，不修改main或其他分支。检查时本地HEAD与远端目标均为`285f1aa`，没有额外待推送历史；未来仅新增本轮提交。
- 检查范围：31个已跟踪修改文件和8个现有目录内新增文本文件，共39个文件；覆盖代码、配置模板、测试、启动脚本和全部现行文档。暂存区为空；本轮没有新增或修改二进制附件。
- 凭据检查：常见私钥、GitHub/OpenAI样式令牌、AWS访问键、JWT、带口令URL和非空密钥赋值扫描未发现真实秘密。`database.py`唯一通用规则命中是从环境读取`DATABASE_URL`并回落本地SQLite路径，不含连接凭据；测试中的密码仅为合成回归数据。`.env.example`的`DATABASE_URL`和`MODEL_API_KEY`保持空值。
- 排除与遗漏：实际`backend/demo.db`、自动备份、根目录`.env`、QA数据库和`frontend/dist`均被gitignore排除，不在上传范围。自动文本扫描不能证明既有远端历史或未修改二进制载体从未含秘密；但它们不会由本次新增提交再次引入，且本轮无变更附件。
- 当前结论：**已检查，待用户确认**。执行push前须再次提醒删除API密钥及其他凭据，并以用户对本报告的下一条明确回复作为本次确认依据；没有该回复则保持本地待上传状态。确认后仅记录在协作会话，不追加包含口令或密钥的日志内容。

## 2026-09-20 / 补记上一轮 v0.3 上传结果

- 上条上传前检查之后，用户已回复“好的”“确认”，同意原分支及所提醒的凭据状态。已提交8799cd01fd69cfca23868735480ea499ef333605（feat: implement teacher workspace v0.3），并快进推送原远端codex/c-local-teaching；本地仍为codex/teacher-workspace。未修改main或其他远端分支，未合入拍照/原型/PCL.exe。
- 本轮开工时工作区干净；只读核对HEAD与本地已同步的origin/codex/c-local-teaching均为8799cd0。此补记更正现行文档遗留的“v0.3尚未上传”，不改写旧检查/实施时点的记录，也不表示后续修改获得上传授权。检查范围与遗漏沿用上条，未发现新秘密，不记录任何真实凭据。

## 2026-09-20 / 班级优先教师入口与内嵌章节题库

- 负责人/工具：C负责人委托Codex实施。用户要求取消独立题库按钮、作业合入班级管理、成员单独显示、参考同济第八版按章分类。本轮仅调整前端和说明，不新增维护目录或依赖，不修改后端、数据库模型、迁移、B模块或远端。
- 入口：教师侧栏从7项减为5项（教学概览、班级管理、教学助手、对话记录、账号设置）。班级卡片直接布置/查看本班作业，成员进入独立页面，保留返回入口；全部作业与未分班草稿在班级管理内仍可访问。概览快捷入口对齐新流程，学生侧栏保持不变。
- 选题：QuestionBank作为作业编辑内的选题器，保留私有题目新建、编辑、归档和参考答案。上/下册12章均可筛选，支持自定义章节及旧章名前缀；跨章选择、移除、调整顺序后追加标题/题干，保留原要求、标题、日期及班级，不带入私有参考答案。加入前重新核对有效题目；空/重复/归档/超过30题或合并超过3000字符均拒绝。取消选题不清空作业，也不自动创建草稿；题库编辑仍即时保存，不随取消作业撤销。
- 目录来源：已核对高等教育出版社同济大学数学科学学院《高等数学》第八版上下册官方目录，ISBN分别9787040589818、9787040588682，来源链接与12章名称登记于architecture。只采用章级分类，题目由教师维护；未导入教材习题、未新增B课程资料、未改变知识片段verified标记。
- 文件：修改frontend/src/App.vue、teacher/ClassesView.vue、teacher/QuestionBank.vue、shared/AssignmentsView.vue、shared/style.css；现有shared内新增courseChapters.ts，tests内新增test_question_selection.mjs。代码复核补齐草稿改班时先切换范围再刷新列表，以及点击侧栏班级管理返回列表的状态重置。
- 自动验证通过：原6项CSV与新增4项章节/选题原生Node测试共10 passed；覆盖章节/自定义兼容、上下册结构、顺序/原文/公式/私有答案隔离和边界拒绝。最终vue-tsc及Vite生产构建通过（2913模块，Vite 25.17秒）。没有新增依赖。
- 页面验证通过：使用独立.tmp/class-ui-20260920-01.db、显式demo种子及18320端口，测试题目/作业/学生均为合成数据。桌面侧栏与2班卡片、独立成员页、本班已发布/草稿筛选、全部作业及未分班草稿可达；选第2章和第12章、调整顺序、保留原要求并发布到一班成功，二班不显示该任务。取消选题保留表单；第10章空分类中新建题目保存成功。概览新建入口可达；最终构建复验草稿从二班改到一班后立即显示新标题和班级，点击侧栏可从成员页返回班级列表。学生登录后看见已发布题干/公式并提交，显示第1版待批改；学生侧无题库与草稿入口。浏览器error/warn日志为空。
- 小屏验证通过：390×844下班级卡片、独立成员页和选题窗口截图可读，无页面横向溢出；成员宽表在自己的容器内横向滚动，长选题窗口纵向滚动。已恢复默认视口。不是另一台手机/Windows实机验收。
- 数据/边界：本次页面业务写入只发往隔离QA服务；没有迁移实现或真实库业务改动。开工读取实际demo.db哈希因占用失败，未取得本轮基线，不声称本轮完成真实库哈希前后对比。此前9/19实际迁移事件及备份仍以历史记录为准。本轮没有调用真实模型、发布公网或修改用户账号。
- 未执行：Python全套82项、实际库迁移/恢复、CSV落盘/Excel、完整源码冷下载、便携重包、另一台Windows、真实模型/视觉模型和真实师生效果。最近Python全量仍是9/19隔离修复后的81项，随后4项针对性补强，不改写为82项全量通过。
- 文档同步：AGENTS更新当前记忆/目录/上传边界；README更新5项侧栏、班级内工作流、章节选题和测试命令；architecture记录组件职责、兼容和教材来源；contracts明确HTTP/schema不变及选题规则；C报告更新功能/验证；source-startup更新分支/前端重建说明；portable-windows区分旧ZIP、已上传v0.3与本地界面；merge-feasibility更正发布状态并保留历史；migrations说明无新迁移/topic回写；本日志追加上传补记与开发事实。全部10份现行Markdown已同步。
- 已核对，无变化：.env.example与.gitignore不需要新增配置，前后端锁文件无依赖变化；既有历史PDF/DOCX/图示、计划与原始素材保持原文，未为本次分类要求改写或复制附件。未新增业务目录。
- GitHub：此轮新增范围尚未提交/上传，未做新的全历史/附件秘密审计，不沿用上一轮8799cd0授权。后续如需上传，按AGENTS第6节重新检查、提醒与确认。
- 下一步：用户体验新入口；继续补标准浏览器CSV下载和新机验收，与A协调远端拍照增量。文档链接/差异和QA服务收尾结果见随后补记。
- 收尾检查：10份现行Markdown本地链接均可解析，无合并冲突标记，git diff --check通过（仅既有LF→CRLF提示）；新增源码/测试均在既有目录，QA数据库与前端dist被忽略。测试账号已退出、页面已关闭，默认视口已恢复；检查时本次QA PID41088已退出，未额外终止其他服务或删除测试/用户数据。当前17个改动文件（含2个新增文件）均留在本地，未提交/上传。

## 2026-09-20 / 对话记录并入Agent界面

- 负责人/授权：Codex，依据用户“对话记录集成到agent界面”。延续教师端精简：去掉教师侧栏独立对话记录，当前保留教学概览、班级管理、教学助手、账号设置4项。保留上一轮所有未提交修改，不上传或合并远端。
- 实现：AgentView聊天顶部新增完整历史弹窗，桌面最近问题区提供“全部记录”；复用RecordsView并增加可选embedded/disabled属性，支持全部/已收藏、按问题或章节搜索、手动刷新和打开历史回复。小屏仍可从聊天顶部进入，不依赖桌面右栏。打开/关闭记录保留未发送内容；新问题清空输入。发送/收藏期间禁用切换、新问题及重复写请求，减少响应覆盖当前记录的竞态。学生原有学习记录/收藏导航保留，共享Agent也可使用新增入口。
- 边界：只调整App.vue、shared/AgentView.vue、RecordsView.vue与style.css，未新增文件、目录或依赖。本次无新接口或schema迁移，沿用个人Conversation列表/收藏权限；历史回看不等于自动携带上下文或长期记忆，模型/演示来源及公式展示保留。
- 验证通过：vue-tsc及Vite生产构建通过（2913模块、Vite 17.38秒）。独立QA端口18320继续使用被忽略的.tmp/class-ui-20260920-01.db，强制demo；创建7条合成历史验证超过最近5条的完整列表、搜索命中与空结果、已收藏筛选、旧回复打开、未发送输入保留；页面新建第8条问答并收藏后立即出现在记录中，刷新记录和重载页面仍保留。390×844下顶部入口可达、弹窗可读、页面无横向溢出；浏览器error/warn日志为空。没有调用真实模型或使用真实学生数据。
- 验收限制：本轮未重跑Python全套、前轮10项CSV/选题Node测试、真实模型、另一台实机、源码冷启动或便携包；前轮测试结论仍保留各自范围，不作为新增历史交互的自动化测试结论。实际数据库没有通过本次QA服务写入，未做其哈希前后对比。
- 文档同步：AGENTS更新当前记忆和4项教师导航；README增加Agent历史操作说明；architecture登记组件复用、小屏入口及并发边界；contracts明确复用原有接口；C报告更新功能与侧栏。已核对无变化：source-startup（重建/刷新方式不变）、portable-windows（旧包仍不含本地增量）、merge-feasibility（共享AgentView仍须团队复核，未合远端）、migrations（schema v3不变）；配置模板、忽略规则、锁文件、历史素材/计划/图示无影响。全部10份现行Markdown已核对，历史日志不改写。
- 收尾：页面已发起退出并关闭，临时视口恢复默认；检查时QA PID30528已不存在，未额外终止其他进程或删除数据库/文件。git diff --check通过（仅既有换行提示）。本轮及前轮界面增量仍在本地，未提交/上传；上传须重新按AGENTS检查范围并确认。下一步由用户体验新入口。

## 2026-09-21 / 学生设置、批量成员操作与章节分组直接选题

- 负责人/授权：C负责人委托Codex，依据用户两张截图要求收拢学生行按钮、左上增加批量操作、按章节顺序展示题目而非先分类再选题，并保留截止时间。保留前轮全部未提交修改，不改变目录、依赖、后端或远端。
- 成员交互：行内仅一个“设置”按钮，弹窗保留姓名、密码、成员移出/恢复、账号启停，manageable账号边界不变。左上角批量模式显示复选框，支持当前可见列表中的成员移出/恢复；全选不含隐藏成员，筛选、切换页面和刷新清空勾选。执行前确认固定班级及名单，只改变与目标状态不同的成员；逐人复用既有PATCH授权，首个失败停止后续请求，不回滚成功项、不自动重试超时，刷新并提示成功/未执行数量和需核实状态。不是账号批量停用、批量导入或原子事务。
- 选题交互：去掉题库章节筛选下拉，按教材章号1–12展示有题目的分组，自定义章节置后，搜索仍按组呈现；旧中文/数字/带空格章名前缀合并到标准章名。题目新建/编辑仍保留章节字段用于归类，未导入教材题目。作业表单删除所属章节下拉、保留截止日期；空题干加入同章题自动得出topic，跨章及与已有不同章节内容混合为综合练习，手写新作业/旧空topic回落综合练习，未重新选题的旧非空topic保留。原题干追加、选题顺序、30题/3000字及私有答案隔离不变。
- 代码文件：teacher/ClassesView.vue、QuestionBank.vue，shared/AssignmentsView.vue、courseChapters.ts、classProgress.ts、style.css，tests/test_class_progress.mjs与test_question_selection.mjs。没有新增维护文件或目录（courseChapters及选题测试为上轮尚未提交的新增文件）。
- 自动验证通过：14项Node测试（原10项及新增4项）覆盖教材数值章序、自定义/旧章名、自动topic、当前列表批量目标筛选、失败停止不重试，以及原CSV/选题安全回归。最终vue-tsc及Vite构建通过，2913模块，Vite 54.99秒；构建后列宽和旧空topic兼容修正均包含在产物中。
- 页面验证：沿用隔离.tmp/class-ui-20260920-01.db与18320端口、强制demo，新增合成班级和3名合成学生。已验证设置入口的4项操作可见，选择2人移出后第3人保持在班，再批量恢复2人成功；切换搜索后已选数量归零。第1、2、10、12章和自定义组按序出现，无章节筛选框；跨章选题后标题/日期保留并成功保存草稿，重开内容/日期仍在，搜索命中和空结果、取消选题保留表单通过。恢复测试一次页面等待超时仅因请求尚在执行，后续界面及服务日志确认两次PATCH均成功，没有重复提交。小屏与收尾复验见后续补记。
- 文档同步：AGENTS更新当前状态/上传边界；README更新操作说明与14项检查；architecture和contracts登记分组、topic兼容、批量非原子/失败边界；C报告更新功能/验收；migrations明确无新schema与topic保留；source-startup、portable-windows、merge-feasibility同步本地9/20至21增量及重建/未上传范围；本日志追加事实，不回写历史。
- 已核对，无功能变化：.env.example、.gitignore、前后端依赖和锁文件不需要修改；配置、启动机制、迁移实现、B资料与verified状态保持不变。历史计划、PDF/DOCX/图示/原始素材不受交互变更影响，保留原文，不为更新而改写。
- 未执行/边界：未重跑Python全套82项、迁移/实际库哈希比较、CSV落盘与Excel、源码冷下载、便携重包、其他实机或真实模型。浏览器密码重置未实际操作，仅验证入口保留；批量网络中途失败由注入失败的Node测试覆盖，未声称浏览器故障注入通过。QA写入只针对隔离数据库，不迁移或操作真实学生数据。尚未提交/上传，未做新的全历史/附件秘密审计，不沿用旧授权。
- 下一步：用户体验新交互；继续标准浏览器CSV与新机交付验证。如需上传，重新检查本次范围并确认凭据状态。
- 收尾验证通过：最终构建重新加载后复验成员设置/批量入口和复选框列宽；390×844下成员工具栏与选题窗口可读，页面宽度未溢出，宽表只在自身容器滚动。取消选题仍保留题干/日期，控制台error/warn为空。14项Node测试最终复跑通过，10份现行Markdown本地链接均可解析，git diff --check通过（仅既有换行提示）。
- 清理与限制：浏览器已确认退出到登录页、临时视口恢复、测试页关闭。第一次沙箱内进程查询被拒绝，不能据该空结果推断已退出；随后获准在沙箱外重新核对命令行，仅停止本次QA PID7632/18320服务。没有停止其他服务，也未删除数据库、备份或用户文件。全部21个本地变更文件（含此前2个未跟踪文件）保留，未提交/上传。

## 2026-09-21 / 资料题单、师生模拟联动与AI/OCR缺口验收

- 负责人/授权：Codex主代理与资料、后端、回归代理，依据用户要求收集题目、开隔离服务、测试师生联动、实际AI、拍照与相关知识点。按测试/诊断范围执行，不擅自修复业务、合并远端或上传；保留全部原有未提交改动。
- 新增普通文件：docs/simulation-test-cases.md（同济第八版12章各1题、2道OCR题干、答案/知识点和高教社/OpenStax官方来源）；docs/simulation-test-report.md；tests/check_simulation.py；tests/check_ai_contracts.py。均在既有目录，没有增加依赖或维护目录。题目独立拟定/明确改编并核算，不复制教材习题集；题单不是模型测评结果，不自动扩展7知识点/2主题的B资料库。
- 隔离：新建.tmp/simulation-20260921-a.db，显式DATABASE_URL、AGENT_MODE=demo、SHUBAN_SEED_DEMO=true、SHUBAN_INSTANCE_ID=simulation-20260921-a，127.0.0.1:18321由PID23028运行。原18080服务只读状态，不做业务写入或停止。HTTP脚本首次写前检查本机/精确实例/demo，报告新路径仅限.tmp；9项MockTransport拒绝探针0写请求。标记不能单独证明DB隔离，须以启动参数为准。
- 自动检查通过：完整pytest tests -q为82 passed、2 warnings、354.82秒（进程365.72秒）；B demo冒烟8项断言通过（6.87秒）；14项Node通过（518.22ms）；vue-tsc与Vite构建通过（2913模块、Vite25.37秒）。既有警告是Starlette httpx TestClient、AnyIO BlockingPortal弃用。Python回归显式临时数据库、demo、禁用dotenv，live用假响应；没有真实模型请求。
- HTTP联动：18场景通过，覆盖双教师权限/私人题库、草稿保密、发布快照、初稿/反馈/修改/旧反馈/过期版本409、后加/退班/恢复、统计及私人问答隔离/收藏重登，demo识别503。首次运行错误要求明确demo也有非空notice，14场景后失败；核对实现后改为mode+固定演示正文+引用断言，再次18场景通过，没有修改业务迁就断言。两次合成数据保留在QA库。
- 页面闭环通过部分：教师页面发布导数定义题，两人收件；学生提交仅代入函数值的初稿，教师待改进评语，学生差商/极限/切线解释的第2版，教师确认完成；学生可展开首版反馈及答案。最终构建重载后会话和统计保留。班级最终统计2人、2份作业、应交3、已交2、未交1、完成2。此为合成数据持久化，不是真实教学效果。
- 页面失败：学生提问保存与教师切换统计相近执行后，教师页面出现连接错误，服务日志多次database is locked（含current_user读取User），health仍200；手动只读重新加载恢复，学生历史只有一次该提问，没有重试写入或重启掩盖。浏览器error/warn列表为空不能否定后端失败，整体并发稳定性未通过。后续定位见补记及报告。
- AI/OCR：根目录未发现.env，模型地址/密钥/模型名/视觉模型名配置存在性均false；现有状态demo/model_ready=false/recognize=false。实际模型与视觉质量未执行。学生积分题得到明确标记的导数固定演示及不相干引用，不计正确解题。本地页面明确拍照后续接入，缓存origin/main168928d仅有拍照确认回填与suggested_topic标签，没有知识点弹窗；只读核对，不fetch/合并/执行附件。
- AI合成缺陷复现：8项函数观测中6项问题（积分/级数建议导数、伪base64/text/plain接受、空JSON返回live空文本、warnings数字抛TypeError），另demo503与模拟ModelCallFailed转502符合现状。脚本禁用/stub dotenv，拦截网络/DB连接/建表，未import main且防护触发0次；报告明确NOT_PASSED_KNOWN_ISSUES，exit0仅表示记录成功。TypeError对应HTTP500为推断，非真实HTTP观察。不把假live当真实模型验收。
- 数据核对：实际demo.db在完整82项回归前后SHA256均99EEA0B1AC6972E1E2702FA81CE8A08D1E0C03A7FEA130339816D3F18431F174，118784字节、mtime相同，历史备份元信息未变。页面收尾再次读哈希时被正在使用的文件锁拒绝，不据此宣称最终哈希已验证，也不停止用户18080服务；本轮QA写入仍使用明确的独立库。
- 文档同步：AGENTS更新项目记忆、4个新增普通文件登记及下一步；README/C报告更新最新回归、缺陷和阻塞；architecture/contracts说明测试隔离、识别实际字段和未修复问题；source-startup区分隔离业务验收与冷下载；merge-feasibility更新缓存远端只读观察；migrations说明schema不变及运行期锁问题；本日志追加，未改写历史。
- 已核对，无变化：portable-windows仍是旧C-only包/未重包边界；.env.example/.gitignore配置与忽略规则足够，前后端依赖锁无变化；历史计划、PDF/DOCX/图示和原始素材不受本次测试影响，保持原文。题单资料未用于宣称原B知识点已复核。
- 待办/授权：用户需在本地.env配置真实服务（不向聊天发送密钥），并明确是否授权SQLite并发、识别校验、知识推荐修复及拍照入口/知识点展示集成。真实AI、OCR/相机、标准浏览器CSV落盘、新机、源码冷下载和便携重包未完成；当前测试结论是部分通过并发现缺口，非全部完成。未提交/上传，未做新的待推送历史/附件凭据审计。
- 锁问题补测：在另一新建合成库运行8路并发80请求，50读/10提交/20演示问答均200/201，4.704秒，无SQL错误；与真实页面时序不同，未覆盖断连/取消/外部进程，故只能记未再次复现，不能宣称修复或确定根因。只读代码/驱动核查排除了“认证每次都清会话”及“所有SELECT天然长期读事务”的直接断言。临时诊断代码和JSON留在忽略.tmp，无业务修改。主代理首次运行探针时网络阻断误拦Windows事件循环内部socketpair，尚未执行请求；调整为先初始化内部唤醒管道、再禁止应用网络后得到上述80请求结果。未访问真实模型/QA库/实际用户库。
- 最终收尾：两端已确认退出并关闭测试tab；检查时QA PID23028不存在、18321监听0，旧exec句柄已结束，未推断退出原因、未停止其他服务。QA库只读integrity_check=ok，作业4/作答3/反馈6；数据库、备份和合成数据未删除。12份现行Markdown本地链接无缺失、无冲突标记，git diff --check及两份新增脚本py_compile通过；构建/QA/探针/冒烟产物被忽略。再次仅检查配置是否存在，.env与4项模型配置仍缺失，没有输出任何凭据。当前25个变更文件（含6个未跟踪文件）含此前全部界面修改，本轮尚未提交/上传。
- 续验阻塞复核：重新完整阅读AGENTS/README和最近日志；配置存在性检查仍为.env不存在、4项模型变量未配置，本地PhotoSearchDialog不存在，AgentView仍为后续接入文案，识别仍仅suggested_topic。未收到修复/集成授权，没有重启服务、重复demo测试或修改业务。目标保留完整范围并标为阻塞，待本地配置及范围确认。AGENTS和报告同步该状态；其余文档已记录相同功能/验证边界，无新增契约、目录、依赖或配置变更，无需改写既有结果。

## 2026-09-21 / 免费Qwen本机真实模型续测与上传条件核验

- 授权/负责人：用户要求“挑一个免费模型测试，通过后直接上传分支”；Codex主代理执行，研究代理核对官方模型/运行参数并准备临时脚本，审计代理只读预审。本次是测试与条件上传授权，不扩大为业务修复、合入远端拍照或修改main。完整阅读协作规范、README、最近日志及相关接口代码，保留全部先前25个改动文件。
- 选择与下载：官方Qwen3-VL-2B-Instruct-GGUF（Apache-2.0）Q4_K_M约1.11GB、mmproj Q8_0约445MB，固定仓库提交及逐文件官方SHA256校验通过；官方llama.cpp b10964 Vulkan/CPU ZIP亦校验通过。来源链接与摘要详见simulation-test-report。公开GitHub API初查限流，未反复重试或寻找凭据；读取官方公开发布页取得下载及摘要。权重一度未传输，最终原下载完整完成，两个下载退出0，没有使用第三方镜像。
- 本机运行：GTX1050Ti4GB/驱动511.79的Vulkan版启动无正常输出，version退出-1073741819；未确定根因、未改驱动。官方CPU版0.4.1-dev/build10964成功，模型PID15200/18323；全新隔离数据库.tmp/simulation-qwen-local-20260921.db，应用PID37056/18324、实例simulation-qwen-local-20260921，禁用dotenv，live环境仅在测试进程中，日常.env不变。模型key为本地服务非秘密占位，不是云端密钥；只绑定回环，Web UI关闭。一次组合重启/加固命令被工具拒绝且未执行；随后独立新建QA应用，未把拒绝的配置写成已生效。
- 参数：单slot、CPU4线程、context4096、batch256/ub128、image-max-tokens768；应用timeout60（已有允许上限）、max_tokens500、temperature0，top_k默认4。图像token配置收到grounding至少1024建议，记录为限制，不宣称最优。未改业务超时、提示词、模型调用实现或依赖锁。
- 真实结果：直接模型短请求Q01只求数值，HTTP200输出3，finish_reason=stop，5.91秒、输入39/输出2tokens，证明真模型在运行。应用包含资料/分层提示的Q01/Q02分别62.621/65.105秒后HTTP200/demo且有超时notice；不计真实数学正确/错误。连续失败后停止12题批次，Q03已有模型任务但无客户端结果，Q04–Q12及保存重登/教师问答/SSE未执行。本次停止使用现有测试进程句柄，部分JSON没有正常收尾字段；另行在报告明确，不当成完整12题验收。模型日志有cancel及稍后release，第二题可能含取消等待，不是严格性能基准。
- OCR：系统绘图生成两张800×300清晰合成PNG，仅题干/数学式，主代理实际查看后送入识别，未发送答案。OCR01/OCR02分别63.220/63.066秒后502且明确超时，无返回文本，不能评价符号准确率。不是手机照片，未做相机/上传页面或知识点弹窗。临时脚本首次因嵌入式Python未含脚本目录导致import失败（未请求服务）；仅补临时sys.path后两项请求完成。
- 结果文件：.tmp/qwen-live-results-bd32e6b674a24a41aef6e142691f9c93.json为人工停止的部分报告；.tmp/qwen-ocr-results-4b1bec3d483047a4b49461a10f0bba1c.json为两项失败报告。脚本、图片、模型日志、QA库均被忽略，未增加维护目录；模型缓存及工具在.runtime/free-model-qwen3vl2b-20260921保留，不纳入源码或默认启动。没有使用真实师生数据、云端账号或付费服务。
- 上传预审（非放行）：86个跟踪+6个未跟踪候选共92文件，84文本扫描常见token/私钥/JWT/凭据URL/配置字面量，25个改动均为文本，未发现已知真实秘密；匹配为字段/空值、公开演示和合成测试值。8个既有二进制与缓存目标一致，未重做视觉及嵌入元数据审计。缓存目标与HEAD均8799cd0，待推送历史/暂存为空；未fetch实时远端。忽略规则排除.env/.runtime/.tmp/数据库/日志/构建。此为预审，文档最终修改及未来暂存后须复查、提醒并确认。测试未通过，条件上传未执行，未提交或修改任何远端分支。
- 文档同步：AGENTS更新真实续测状态及条件授权；README/C报告更新“短请求可用但完整接入失败”；architecture记录隔离临时模型与不改变依赖/启动；contracts记录真实超时降级与502；source-startup明确缓存不随源码/便携包交付；题单与测试报告区分标准答案、部分真实请求及实际失败；本日志追加，不回写旧验证历史。
- 已核对，无变化：merge-feasibility的缓存远端差异未变，未fetch/合并；portable-windows未重包且不含模型；migrations/README仍schema3，无迁移或实际库操作；.env.example/.gitignore无需修改，前后端源码及依赖锁不变；原始素材/计划/PDF/DOCX/图示未改写。未重跑82项Python、14项Node、构建、源码冷下载或便携包，不沿用先前结果声称本次重跑。
- 后续：当前不满足上传条件。可选官方标注免费且需API Key的GLM-4.6V-Flash云端视觉服务，未注册或实测，需用户本地配置；不要把密钥发聊天。已有SQLite锁、识别校验、知识覆盖、拍照入口/弹窗问题未修复，仍需明确实施范围。收尾与最终文档检查见补记。
- 收尾：通过本次exec句柄中断QA应用，核对模型PID15200可执行路径后仅停止该模型；最终PID15200/37056均不在，18323/18324监听0。原18080仍为PID4120，未停止/重启；只读QA库integrity_check=ok、conversations=0。没有再次读取或改写实际用户数据库，也不扩展先前实际库哈希结论。模型缓存、测试图片和合成库保留，未删除材料。文档diff空白检查通过（仅既有LF/CRLF提示），二进制缓存不属于待上传内容。
- 最终复核：12份当前Markdown共33个本地链接全部存在，冲突标记0；外链/锚点不据此声明逐项联网可用。安全预审复跑仍为92候选（84文本/8未变二进制）、25个改动，未发现已知真实凭据，模型/报告/图片/库均被忽略；暂存及相对缓存目标的待推送提交/对象仍0。二进制视觉/元数据与实时远端仍未核验，不能将预审写成上传放行。

## 2026-09-21 / 用户要求归档状态、上传原C分支并合入main：准备检查

- 授权：用户在已收到免费模型超时报告后要求“记录一下状态，上传并合并分支”。本次按原分支codex/c-local-teaching→main准备，不再把之前“测试通过”当发布前提，但真实失败、已知缺陷和未执行项原样保留；不扩大到业务修复或其他远端分支。开工已提醒删除待上传API密钥和其他凭据、确认密钥状态，实际上传仍待审计报告及本轮确认。
- 基线：实时fetch明确只取main和原C分支、无tags；结果main仍168928dd3b6d93a6011ec611bd0729dac7b24a8b，C仍8799cd01fd69cfca23868735480ea499ef333605，与本地HEAD一致。本地25个未提交变更全部保留。
- 预演：用.tmp中的独立GIT_INDEX_FILE形成当前文件快照及未挂分支的临时提交94d9ee5，再git merge-tree --write-tree与main合并，退出1报告6文件冲突：AGENTS、contracts、development-log、App.vue、AgentView.vue、style.css。没有checkout、实际index写入、分支提交或进入工作区merge；临时Git对象不属于待推送分支历史。
- 目录纠正与确认事项：main相对共同基线285f1aa实际26个文件变化，包含拍照搜题-20260918T062840513Z/、数伴教育智能体前端-20260918T001305320Z/两个完整原型目录及内部.apps-builder/assets/history/delivery路径，并非早先简写的仅根目录原型。计划同路径保留main已有内容及5,750,272字节PCL.exe，不运行、不删除；将纳入当前C工作区前需用户确认目录范围。替代为只上传C、暂缓main合并。现阶段未增加维护目录或引入远端组件。
- 验证：本次合并前14项Node通过、2.795秒；vue-tsc和Vite构建通过，2913模块、Vite45.75秒。未重跑82项Python、浏览器闭环或真实模型；后续冲突整合完成还须重新验证，不能将预演和原树构建视为合并后通过。
- 文档：AGENTS更新最新请求、基线、冲突和待确认；README/C状态报告更新归档发布准备；simulation-test-report说明后续发布授权不改变失败结果；merge-feasibility登记目录完整路径、保留方案和替代方案；本日志追加事实。architecture/contracts/source-startup/portable-windows/migrations/题单已核对，无变化：实际业务/契约/目录尚未合入，旧验收范围和未上传状态仍准确；.env.example/.gitignore/依赖锁不变；历史素材、计划和图示未改写。
- 凭据核验与独立代码预审并行进行：仅输出路径/分类与摘要，不回显秘密，不运行远端附件。工作区与暂存/待推送历史、main增量对象的实际范围和遗漏将在收尾追加。当前未提交、上传、创建PR或合并，等待凭据与目录确认后继续。
- 审计结果：25改动、92候选（84文本/8未变二进制）扫描未发现已知真实秘密；暂存为空，当前HEAD相对原C无新提交。另覆盖main增量25文本路径及C相对main一个提交内39个新增历史文本blob，含完整原型目录，匹配均为字段/公开演示或合成测试值。PCL只核对Git元信息及9/18重新加入记录，未检查可执行安全、签名、内嵌凭据或来源合法性；既有8附件未重做视觉/元数据审计。配置/模型缓存/QA库/日志/dist继续忽略。状态为“已检查，待用户确认”，后续合并树和暂存仍需复查。
- 独立代码预审：建议保留本地v0.3导航/班级及Agent记录/locked逻辑，同时协调远端Camera/PhotoSearchDialog入口；样式保留本地换行/小屏规则，避免重复.chat-header-actions互相覆盖。远端识别回填与旧输入拼接可能超过2000字符，后续整合应明确处理，不静默截断公式。当前仅记录发现，没有修改业务代码或声称已解决冲突。
- 收尾检查：12份现行Markdown、36个本地链接均存在，工作区冲突标记0（虚拟合并冲突只存在未挂分支Git对象中）；git diff --check通过，暂存仍空，本地分支仍codex/teacher-workspace。没有停止日常服务、操作实际数据库或删除文件。等待本轮确认，不把“准备完毕”写成已上传/合并。
- 自动续行阻塞审计：上传准备当轮发现的凭据/目录确认缺口，在随后两轮自动续行仍未获得用户回复；上一轮仅复核既有状态，属于无新增进展，不是等待运行中的任务。再次读取AGENTS、README、最近日志及报告，工作区仍25个变更、暂存为空，.env及4项模型环境配置存在性均false，PhotoSearchDialog仍不存在；不输出环境变量值、不重复启动模型或服务。
- 完整目标审计：资料与12章题单已完成、顺序师生联动已有通过证据但并发锁问题仍在；真实模型只证明短请求可用，应用问答和OCR仍超时；拍照页面及知识点弹窗没有本地可验收实现。因此不标为完成。当前不能在缺少发布确认及后续实施范围/可用模型条件下继续安全推进，按连续三轮同一阻塞转为受阻，等待用户输入。仅更新AGENTS和本日志；其他文档沿用上一条同步清单，无新增业务、目录、接口、依赖或测试结果，不改写原报告中的失败与未执行项。

## 2026-09-21 / 仅上传原C分支：范围确认与上传前验证

- 用户明确将范围改为“只上传分支，不合并”：只更新远端`codex/c-local-teaching`，不修改`main`、B分支或main上的PCL.exe/两套原型目录，不处理此前虚拟预演的6处冲突。
- 本地工作区保留25个修改文件和6个新增文件；14项Node回归通过，前端`vue-tsc`与Vite生产构建通过（2913模块）。未运行Python全套、浏览器闭环或真实模型，沿用已有报告中的边界，不将本次构建写成AI/OCR通过。
- 最终只读凭据审计重新扫描92个非忽略候选（84文本、8二进制），未发现已知真实凭据；29个匹配均分类为字段、公开演示或合成/占位值，未发现需人工复核项。8个既有二进制仅核对与目标分支未变，未做视觉或嵌入元数据审计；`.env`、模型缓存、QA库、日志和dist仍被忽略。
- 远端基线实时核对：`origin/codex/c-local-teaching=8799cd0`，`origin/main=168928d`；暂存和相对C分支待推送历史为空。下一步在最终暂存复查后创建本地提交，非强制推送C分支并fetch确认，绝不合并main。
- 文档同步：已核对并更新AGENTS、README、C状态报告、合并核验和本日志；architecture、contracts、source-startup、portable-windows、migrations、题单及测试报告无业务/接口/目录变化，保留原有失败与未执行范围。

## 2026-09-21 / 原C分支上传完成（不合并main）

- 范围与授权：用户明确“只上传分支，不合并”。仅更新`codex/c-local-teaching`，不修改或合并`main`，不引入main的拍照组件、两套原型目录或PCL.exe，也不处理此前6处虚拟合并冲突。
- 验证：14项Node回归通过；前端`vue-tsc`与Vite生产构建通过（2913模块，构建耗时约1分49秒）。未重跑Python全套、浏览器闭环或真实模型，沿用报告中的失败/未执行边界。
- 提交与上传：25个变更文件（含6个新增文件）已提交为`78777995aa118f542696bfa2f7bd449d9ca6aaaf`，使用非强制`git push`从`8799cd0`快进更新远端`codex/c-local-teaching`。随后实时fetch确认本地与远端C一致；远端`main`仍为`168928dd3b6d93a6011ec611bd0729dac7b24a8b`，未修改其他分支。
- 凭据核验：最终扫描92个非忽略候选（84文本、8二进制），未发现已知真实凭据；29个匹配均分类为字段、公开演示或合成/占位值，无人工复核项。8个既有二进制只核对与C目标未变，未做视觉/嵌入元数据审计；忽略的`.env`、模型缓存、QA库、日志和dist未上传。
- 文档同步：AGENTS、README、C状态报告、合并核验及本日志已更新为“只上传分支”；architecture、contracts、source-startup、portable-windows、migrations、题单和测试报告已核对，无业务、接口、目录或依赖变化，保留真实模型/OCR失败和拍照知识点弹窗未验收结论。

## 2026-09-18 / 新增拍照搜题移动 Web 原型（独立目录，未接入前后端代码）

- 负责人 / AI：千问工作助理（用户 C 侧发起）。用户要求实现拍照搜题效果，并说明用 getUserMedia 取流、canvas 框选、toDataURL 压缩后传后端；同时提到 wx.createCameraContext 小程序路径。
- 范围与授权：经用户选择，交付形态为移动 Web/H5 页面（不产出微信原生小程序包），识别来源为“可配置接口地址 + 本地演示兜底”。用户指定落点为项目根 D:\agent-school-llm-usts，据此在其下新建独立子目录 拍照搜题-20260918T062840513Z。未修改 frontend/src（A 负责）与 backend/app（B 负责）任何文件，不改依赖、不改迁移、不改接口约定。
- 目录变更依据：AGENTS.md 第 3 节要求新增目录先确认。已在生成前向用户列出三个候选（新建独立原型目录 / 放进已有演示目录 / 已忽略 tmp），用户回复项目根路径，按其选择以 parent 语义新建子目录；本条记录确认依据。
- 实现要点（单文件静态页面，无构建、无运行时依赖）：
  1. 相机：navigator.mediaDevices.getUserMedia 后置优先，OverconstrainedError 自动回退任意摄像头；权限拒绝、无设备、被占用、非安全上下文分别给出具体原因与排查指引，并提供相册导入替代路径。
  2. 框选：canvas 绘制遮罩、圆角选框、四角把手与三分网格；支持 8 手柄缩放、整体拖动、框外任意方向重新框选；最小尺寸校验，过小自动恢复默认。
  3. 裁剪压缩：按 object-fit:cover 反推视频源矩形做精确裁剪（原始分辨率），再按最长边与 JPEG 质量缩放导出；前置摄像头镜像与预览一致；展示裁剪前后分辨率、体积、Base64 长度。参数改动基于保留的全分辨率画布重新导出。
  4. 上传：POST 到可配置地址，请求体 { image_base64, media_type, hint }，与 backend/app/ai/service.py 的 RecognizeInput 字段一致；默认携带 X-Requested-With: shuban-web 与 same-origin 凭据，对齐 frontend/src/shared/api.ts 的现有约定，三项均可在设置中关闭。
  5. 真实性标注：非 live 返回一律标注“演示模式 · 未连接真实模型”，演示文本自身首行也带该标注；接口失败与后端 503 原文如实展示，不把演示文案冒充识别结果。识别文本可编辑后才确认保存（对应后端 requires_confirmation）。
  6. 设置抽屉内提供相机与安全上下文自检（协议、isSecureContext、getUserMedia 可用性、轨道分辨率、闪光灯可控性、后端地址）。
- 已知边界与限制（未验收项）：
  1. 未做真实浏览器运行验证，也未在真机上调用相机；本轮仅完成静态语法与本地引用抽验（0 BLOCKER、0 WARN）。
  2. 未与后端联调；默认相对地址 /api/agent/recognize 需页面与后端同源部署，用 file:// 打开时需填完整地址，且跨域时会被浏览器拦截（页面会如实报错）。
  3. 后端未配置视觉模型时 /api/agent/recognize 返回 503，这是既有行为，页面按错误展示而非编造结果。
  4. 该目录不属于 frontend 构建产物，不参与一键启动与测试；AGENTS.md 中“拍照识别等前端流程”未完成项状态不变，本次只是独立原型，不能视为 A 端功能已实现。
- 文档同步：AGENTS.md 目录树与项目记忆已登记本原型目录及授权依据；本日志追加本条。README、docs/architecture.md、docs/contracts/README.md、docs/source-startup.md、migrations/README.md、.env.example、.gitignore 已核对，无需修改——本次未改入口、启动方式、接口约定、数据表结构或忽略规则；新目录位于项目根且为独立静态原型，若后续不入库可由用户自行忽略。原始素材与历史材料保持原样，未改写。
- 验证：静态抽验通过；后端 pytest、前端 TypeScript/Vite 构建本轮未执行（未触及相关代码）。
- 下一步建议：如要真正接入，需 A 侧决定是把该流程并入 frontend/src/student 还是作为独立页面由后端静态托管；接入后需在真实模型下验证 503/502 分支与识别质量，并在手机真机验证 https 下的相机权限。


## 2026-09-18 / 拍照搜题接入真实前端 AgentView（新组件 + 最小侵入）

- 负责人 / AI：千问工作助理（用户 C 侧发起）。用户要求把上一条的拍照搜题原型“塞到智能体网页里，做一个小功能”。给出两个候选（apps-builder 演示页 / 真实前端 AgentView.vue），用户明确选择真实前端 AgentView.vue。
- 协作边界提示：AgentView.vue 与 style.css 属 A 负责的共享文件。本轮按最小侵入原则改动——主逻辑全部放新组件，共享文件只加入口与样式，未改问答发送、收藏、主题选择、记录选择等既有逻辑；按第 4 节，合并前应由 A 或另一成员复核。
- 新增文件：frontend/src/shared/PhotoSearchDialog.vue（约 24 KB，Vue 3 + TS + scoped CSS，沿用项目 CSS 变量与 el-dialog / primary-button / outline-button / small-pill / form-error 既有类名，无新增依赖）。
- 修改文件：
  1. frontend/src/shared/AgentView.vue：导入 Camera 图标与新组件；新增 photoOpen 状态、applyRecognized() 与 guessRecognizedTopic()；对话头加「拍照搜题」按钮（与原「新问题」并列于 .chat-header-actions）；侧栏 coming-note 从“拍照识题 · 步骤反馈 / 页面入口后续接入”改为“步骤反馈 · 语音 / 拍照搜题已可用”；模板挂载组件。diff 为 +26/-3。
  2. frontend/src/shared/style.css：末尾追加 .chat-header-actions 与 .photo-dialog .el-dialog__body 两条规则，未改既有规则。
  3. frontend/src/App.vue：帮助弹窗“已经可以体验”补入拍照搜题取景框选与压缩上传；“能力边界”从“拍照识题…页面入口待接入”改为“入口已开放，识别结果需人工确认后带入提问，未配置视觉模型时后端返回不可用提示”，步骤反馈与诊断仍标为待接入。
  4. docs/contracts/README.md：删除“仍待定义：拍照识别的上传与确认流程细节”，新增“2026-09-18 · 拍照识别前端调用约定”一节，记录上传字段、传输方式（复用 api.ts，90 秒 isAiWrite 超时、不自动重试）、确认流程（仅回填提问框、发送走既有 POST /api/conversations、组件不入库）、来源标注（仅 mode==='live' 视为真实）、失败处理（503/502/网络异常如实展示 detail，不回退为编造文本）与仍待定义项。
  5. AGENTS.md：第 2 节未完成项移除“拍照识别等前端流程”，新增拍照搜题前端入口条目；第 3 节目录树 shared/ 登记 PhotoSearchDialog.vue。
- 实现要点：
  1. 相机：getUserMedia 后置优先，OverconstrainedError 自动回退任意摄像头；权限拒绝、无设备、被占用、非安全上下文分别给出分类原因与排查指引，并提供相册导入走同一框选路径；支持翻转、闪光灯（仅 getCapabilities().torch 为真时显示）。
  2. 框选：canvas 遮罩 + 选框 + 四角把手 + 三分网格；Pointer Events 支持 8 手柄缩放、整体拖动、框外任意方向重新框选与 setPointerCapture；选框过小自动恢复默认；实时显示选框像素尺寸。
  3. 裁剪压缩：按 object-fit:cover 反推源矩形做原始分辨率精确裁剪，再按最长边（480–2048）与 JPEG 质量（0.40–0.98）或 PNG 导出；前置摄像头镜像与预览一致；参数变更基于保留的全分辨率画布重新导出；展示裁剪前后分辨率、体积与来源。
  4. 上传：复用 api.ts 的 api()，POST /agent/recognize，body 为 { image_base64, media_type, hint }，与 service.py 的 RecognizeInput 字段一致（base64 不含 data: 前缀，hint 限长 200）；自动带 Content-Type、X-Requested-With: shuban-web 与 same-origin 凭据。
  5. 确认闭环：识别文本可编辑；「用这段文字提问」仅回填提问框（已有内容则换行追加，截断至 2000）并按关键词建议主题，由学生自行决定是否发送。发送仍走既有 send() → POST /api/conversations，组件不自行入库，符合后端注释“确认动作复用 POST /api/conversations”与 contracts 的 requires_confirmation 约定。
  6. 真实性：仅 mode==='live' 标为真实接口结果；confidence / warnings / suggested_topic 原样展示；503、502 与网络异常均展示后端 detail 原文，不生成编造的识别文本；非 live 给出明确警示。
  7. 生命周期：弹窗打开时才申请相机，关闭与组件卸载即停止全部轨道并 revokeObjectURL，避免摄像头指示灯常亮。
- 验证：
  1. vue-tsc --noEmit 首轮 12 项类型错误（toast 参数个数 11 处、ev.currentTarget 可能为 null 且无 setPointerCapture），修正 toast 签名增加可选 isError 参数、指针捕获改为类型收窄后可选调用；复查 exit 0。
  2. npm run build（vue-tsc --noEmit && vite build）exit 0，2910 modules transformed，7.92s，产物 index-Ci1w2ail.js 104.19 kB。
  3. 后端 pytest 本轮未执行（未改动 backend 任何文件）。
- 环境说明：默认沙箱下 vite build 因 esbuild 需 spawn 原生子进程报 spawn EPERM，属沙箱限制而非代码问题；已在沙箱外用项目自带 .runtime/node-v22.23.2 重跑通过。dist 为已忽略产物。
- 未验证项（不得写成已完成）：
  1. 未做真实浏览器运行验证，未在真机调用相机；未验证移动端触控框选手感与 iOS Safari 的 playsinline / getUserMedia 行为。
  2. 未与后端联调；未验证 401 会话过期事件、503（未配置视觉模型）、502（调用失败）三条分支在真实服务下的展示。
  3. 未验证真实视觉模型的识别质量、置信度与多题切分；本项目当前 AGENT_MODE 未配置真实模型时识别接口按既有行为返回 503。
  4. 桌面浏览器通过 http://localhost 属安全上下文可用相机；若以局域网 IP 访问会被浏览器拒绝，页面会分类提示，尚未实测。
- GitHub 上传 / 密钥状态：未上传，未做任何 push。新增与修改文件均为源码与文档，不含密钥、令牌、真实账号密码或学生隐私；组件不存储图片到 localStorage，裁剪结果仅存于内存，弹窗关闭即释放。本轮未新增配置项，.env.example 未改。
- 遗留与下一步：
  1. 按第 4 节请 A（或另一成员）复核 AgentView.vue / style.css / App.vue 改动，走短期分支与 PR，不在 main 直接合并。
  2. 起后端做真实联调，覆盖 503 / 502 / 401 三条分支与识别文本回填后的提问保存。
  3. 配置视觉模型后验证识别质量，并决定是否补多题切分与公式人工修正。
  4. 上一条的独立原型目录 拍照搜题-20260918T062840513Z 仍保留为参考实现，与本组件无代码依赖；是否删除或忽略由用户决定，本轮未删除任何文件。


## 2026-09-18 / 拍照搜题原型并入「AI 视频解析」（v2）

- 负责人 / AI：千问工作助理。用户要求在既有拍照搜题页内并入一个「AI 视频搜索与内嵌播放」功能，明确规格为：顶部带解析按钮的输入框 + 下方视频卡片容器；Fetch 调用模拟后端接口，返回含标题与 URL 的 JSON；前端动态创建 video 标签注入页面实现内嵌无缝播放。用户另附六条后续功能建议（错题收藏分类、倒计时专注模式、同标签重练、语音搜题、语音讲解、全双工语音答疑），本轮仅记录为后续开发清单，未实现。
- 落点确认：给出三个候选（新建独立页面 / 并入现有拍照搜题页 / 接进 Vue 真实前端），用户选择「并入现有拍照搜题页」。按 apps-builder 迭代流程以 exact 语义复用既有项目目录 拍照搜题-20260918T062840513Z（projectId 7d407761 不变），未新建目录；入口文件名保持不变，原地修改后定版为 v2。未改动 frontend/、backend/ 任何文件，未新增依赖。
- 新增视图（SECTION: view-video）：取景页顶栏新增入口按钮，点击进入视频解析视图；进入时调用 stopStream() 释放相机，避免摄像头指示灯在不需要时仍亮。视图含关键词输入框（Enter 触发、支持中文输入法 isComposing 判断）、5 个高数快捷词、播放器容器、解析结果列表、最近搜索、底部说明。
- 接口与数据映射：POST 到可配置地址（默认 /api/agent/video-search），body 为 { keyword, query, topic, limit }；复用识别接口相同的 X-Requested-With 与同源凭据开关。返回结构做兼容归一化，依次尝试 items / videos / results / list / data.items / data.list / data 数组 / 单条对象；标题与地址字段名可在设置中自定义（默认 title / url），并按常见别名回退（video_url、src、playUrl、mp4 等）。字段名映射错误时明确提示当前使用的字段名，便于排查。
- 内嵌播放：动态 createElement('video') 注入播放器容器，controls + playsInline + preload=metadata；切换条目时先暂停并移除上一路，避免多路解码与声音叠加；自动播放失败按「有声 → 静音 → 待手动播放」逐级降级并提供「开启声音」；监听 loadedmetadata / playing / pause / ended / error，error 按 error.code 映射为可读原因（中止、网络中断、解码失败、地址不可用）；展示实际时长与分辨率。
- 安全与真实性处理：只接受 http(s) 与同源相对地址，javascript: 等协议被拦截并明确报告丢弃条数；https 页面下的 http 视频地址给出混合内容预警，说明会被浏览器拦截而非静默失败；非接口来源一律标注「示例数据 · 非真实解析结果」，并写明视频为公开占位素材、与所搜关键词内容无关。
- 示例视频源核实：未凭空手写 URL。先用 PowerShell Invoke-WebRequest 探测，HTTPS 源全部失败，改用项目自带 Node 复验后确认 4 个源真实可用（media.w3.org 的 Sintel 预告片 4372373 字节、interactive-examples.mdn.mozilla.net 的 CC0 花朵 1128375 字节、test-videos.co.uk 的大兔子 991017 字节、vjs.zencdn.net 的 oceans 23014356 字节），均为 HEAD 200 / video/mp4。PowerShell 侧失败为本机 TLS 环境问题，非源失效；内置示例仅取其中三个较小的源。
- 本轮修复的三处自身缺陷：
  1. 相机并发守卫失效：camStarting=true 置于 stopStream() 之前，而 stopStream 会复位该标志，导致守卫当场无效、仍可能重复取流；改为在 stopStream 之后再置位并记录代次令牌。
  2. 重复取流：reframe() 与 showView('camera') 都会调用 startCamera，异步窗口内 camReady 尚未置位，会发起第二次权限请求并掐断首次建立的流；统一由 showView 按需启动，reframe 不再直接调用。
  3. 过期 DOM 引用：clearVsPlayer() 重建 #vsEmpty 后，缓存的 el.vsEmpty 仍指向已脱离文档的旧节点，导致下次播放时占位层删不掉、与视频画面叠加；改为按 id 实时查找。
  另新增代次令牌 camToken：stopStream 递增令牌，在途取流回调发现令牌不匹配即释放轨道，避免用户中途切走视图后摄像头仍被占用。
- 验证：静态完整性抽验通过（8 个脚本块，0 BLOCKER / 0 WARN），并在修复守卫顺序后复验一次；交叉核对视频相关 14 个函数的定义与调用一一对应，无未定义调用与残留过期引用。**未做真实浏览器运行验证**，未在真机测试视频播放、自动播放降级与混合内容拦截的实际表现；示例视频源仅通过 HEAD 请求确认可达，未下载播放验证解码。
- GitHub 上传 / 密钥状态：未上传，未执行任何 push。本轮改动均在原型目录内，不含密钥、令牌或真实账号密码；视频地址为公开素材地址，不涉及隐私。
- 后续开发清单（用户本轮提出，均未实现）：
  1. 错题收藏与分类标签（识别结果页加「加入错题本」，localStorage 结构化存取）。
  2. 实时倒计时专注模式（自定义时长、背景变暗只留题目区、结束提示音）。
  3. 同标签错题一键重练（filter + 随机打乱组成迷你测验）。
  4. 语音搜题（Web Speech API 或第三方 ASR，按住说话转文字后检索）。
  5. AI 语音讲解（TTS 朗读解析步骤）。
  6. 实时全双工语音答疑（需后端与第三方语音服务，当前静态页面无法真实交付）。
  其中第 6 项涉及真实外部服务与后端能力，不属于本地静态页面可交付范围；第 4、5 项需引入第三方密钥，接入前须先确认服务商与密钥管理方式。
- 说明：本目录为独立原型，与 frontend/src 中已接入的 PhotoSearchDialog.vue 无代码依赖，两者可独立演进。是否将视频解析能力并入 Vue 前端由用户与 A 侧责任人决定。

## 2026-09-21 / C分支与main整合及验证

- 授权/负责人：用户最新明确要求“将我们的分支与主分支合并”；Codex执行，取代此前仅上传限制。已有范围说明列明main两套原型目录和PCL.exe，本次按原路径保留纳入，未执行附件、不新增独立业务目录，不修改其他远端分支。
- 基线/冲突：开工工作区干净，本地及远端C均为cfd1025，实时main为168928d。合入main时6处冲突（AGENTS、contracts、development-log、App、AgentView、style）；逐项合并，保留C教师v0.3/精简导航/成员设置与批量操作/章节题库/Agent历史，接入main拍照入口。双方开发日志均完整保留，未以旧main覆盖账号/班级和schema v3实现。
- 回填兼容：拍照按钮遵守Agent的发送/收藏锁；识别文本与现有提问合计超过2000字时提示并保留两处文本，接受后由父组件关闭弹窗，避免静默截断公式。保留窄屏按钮换行，帮助文案同步拍照入口及视觉配置要求。PhotoSearchDialog其余实现沿用main，原型视频接口仍无后端实现。
- 验证通过：完整82项Python回归，154.96秒、2条既有弃用警告，显式独立.tmp数据库/demo/禁用dotenv；14项Node回归，666.17ms；vue-tsc及Vite生产构建，2916模块、24.61秒；对实际Vue脚本的8场景隔离探针通过（正常追加、2000字边界、合计超限/单条超限、发送/收藏锁、教师主题、历史回看）。探针以mock替代Vue生命周期与浏览器依赖，不是摄像头/模型实测，脚本留在忽略.tmp。
- 未执行：浏览器/真机、真实视觉模型、新源码冷下载安装和便携重包。SQLite锁、OCR校验/知识覆盖以及真实模型超时结论保留；本轮合并不宣称这些问题已解决。未启动日常服务或写入实际数据库。
- 文档同步：AGENTS更新最新授权/状态/目录，README/architecture/C报告/source-startup更新统一工作区与能力边界，contracts明确回填长度与失败保留，merge-feasibility将旧合并前分析标为历史，portable/migrations纠正已上传状态，simulation-test-report补充后续代码整合但保留原测试结论；本日志追加。simulation-test-cases已核对，无变化（题单/答案与本次合并无关）；配置模板、依赖锁、历史素材和原型保持原样。12份现行Markdown、36个本地链接检查通过，无冲突标记。
- 更正上一轮“25个修改文件和6个新增文件”：实际为25个变更合计，包含19个修改与6个新增，以git提交统计为准。
- 发布准备：最终暂存及提交历史须再扫描凭据，核对原型/PCL.exe与main原字节一致，然后非强制更新C并创建合入main的PR。上传/PR最终结果另记，不把本地整合当作远端已完成。
- 上传前审计：当前112个非忽略文件（103文本、9二进制）、34个暂存变更扫描无待人工复核凭据项；另核对main的26路径增量和C相对main的69个历史blob，94份文本扫描无已知秘密。PCL.exe及两套原型目录与main逐blob一致，其他8个二进制沿用C原文件；未做二进制视觉/嵌入元数据或可执行安全审计。旧扫描脚本提示PCL不在C旧基线属于预期新增，已另与main核对一致，不把该提示当扫描失败。C后端、迁移脚本、启动器与依赖锁逐文件不变；本轮真实配置、QA数据库、缓存、模型和临时探针继续忽略。
- 当前会话授权依据：用户已委托“你帮忙确认上传”，最新又明确要求将C分支与main合并；按本轮核验结果和已说明的目录范围直接继续，不重复索取同一确认。上传前再次提醒清除API密钥及其他凭据；未发现需撤销/轮换或历史重写的已知秘密，扫描不是全部秘密不存在的证明。
- 发布完成：合并提交e6ba91a33b74351d30c1fd2623e4e05617e82ffb经非强制推送更新C，创建并关联[PR #3](https://github.com/844346518scp-coder/agent-school-llm-usts/pull/3)。GitHub确认mergeable=true，无已配置的提交状态或PR工作流；使用期望head=e6ba91a调用正常merge，成功生成main提交847f1b39b7b7fb74c920739fafc4f99e5a363513。未绕过分支保护；未取得另一成员审阅，不宣称已有人审，按用户明确合并要求执行。
- 最终核验：提交后对待合入main的84个文本历史blob再次扫描无已知秘密；实时fetch确认main合并成功，git diff确认847f1b3与已验证e6ba91a文件树一致。本地仅快进跟随main；收尾追加此发布记录，并同步AGENTS/README/合并核验状态，其他文档已准确描述整合后功能和未完成项，无需再次改动。收尾只有状态文档，没有重复跑应用回归；状态文档通过差异/链接及凭据复查后同步C和main。

## 2026-09-21 · B 模块第二阶段：长期记忆、推荐、评价、总结、资源检索 + 契约修复

- 日期 / 任务：2026-09-21 / 功能扩展版（9/21–24）B 部分：长期记忆、推荐练习、评价（费曼复述/自评）、总结复习、资源检索；同时修复 2026-09-21 模拟验收列出的 6 项 AI 契约缺口。
- 负责人 / AI 工具：B 模块成员（待填）/ Chatbox。
- 开发了什么与原因：
  - **修 6 项已知缺口**：拍照入参严格校验（base64 / 图片 MIME / 文件头）、空识别结果报 502、`warnings` 非列表不再抛非受控异常、主题推断覆盖不足时返回 `null`（不猜）、知识库扩展到「一元函数积分学」「无穷级数」。
  - **长期记忆**：追加式事件表 + 拉普拉斯平滑掌握度；证据只取自评/复述评价、步骤反馈、诊断命中三类可核对信号，普通问答记 `exposed`（权重 0）；学生可清空自己的记忆。
  - **推荐练习**：按 `weak → learning → unseen` 排序，每条都给依据；`exclude` 支持“换一批”。
  - **评价**：按知识点信号覆盖率给出档位（基本到位/有遗漏/需要重讲）、缺失项与追问。
  - **总结复习**：汇总窗口内提问记录 + 记忆薄弱项，给出 `highlights` 与 `next_steps`。
  - **资源检索**：概念/例题/练习/资料四类条目，带出处与 `verified` 状态。
  - 新接口单独放 `phase2.py`，避免继续频繁改三方共用的 `service.py`。
- 修改文件：
  - 新增 `backend/app/ai/memory.py`、`insight.py`、`phase2.py`、`tests/test_agent_phase2.py`（19 项测试）。
  - 修改 `backend/app/ai/knowledge.py`（+5 知识点、+资源索引并追加 6 条跨模块资料/导览条目、主题推断证据门槛）、`service.py`（识别校验与错误处理、记忆接线、status 能力位）、`prompts.py`（总结提示词）。
  - 修改 `tests/test_api.py`、`tests/smoke_agent.py`：识别用例改用真实一像素 PNG。原因：旧用例用假 base64（`'a'*16`），契约修复后会在入参阶段先 422，无法再验证“演示模式拒绝识别”这一条，因此按新契约换夹具（与 `check_ai_contracts.py` 用同一张图）。
- 验证命令或方式 / 结果（通过、失败、未执行）：
  - `python -m pytest -q` → **101 passed**（含教师端与迁移测试，无回归）。
  - `python tests/check_ai_contracts.py` → **issues_reproduced=0、behaviors_as_expected=8**（原 6 项缺口清零）。
  - `python tests/smoke_agent.py` → **通过**：登录、status、ask、feedback、diagnosis、recognize（503）、conversations、SSE、review、memory、recommend、summary、resources/search 全部符合预期。
  - `python tests/check_source.py` → **通过**（冷启动 + 自动安装 + 资源 + B/C 流程 + 离线缓存启动 + 模拟 live 模式）。
- 目录结构变更 / 用户确认依据：无新增目录；**未改数据库表结构**。长期记忆使用 B 自有表 `ai_learning_events`，由 `memory.py` 用独立 `MetaData` 惰性建表，不加入 `platform.database.Base`，避免触发 `migrations/upgrade.py` 的 v3 完整性校验。
- 文档同步清单（逐项写已更新，或已核对无变化及原因）：
  - AGENTS.md 项目记忆与目录：已追加“B 模块第二阶段实现说明”与 6 条新硬约束。
  - README.md：已核对无变化（本次不涉及启动方式与总览）。
  - docs/architecture.md：已追加“长期记忆与学情洞察（B 第二阶段）”。
  - docs/contracts/ 下全部约定：已追加“智能体接口 v0.3（B 模块第二阶段）”，含 6 项缺口修复说明、记忆口径与存储、前端接入建议。
  - 其他相关说明、配置模板、计划与图示：无新增配置项（沿用第一阶段 `AGENT_MODE` / `MODEL_*`）。
- GitHub 上传 / 密钥状态 / 检查范围 / 用户确认依据：已推送并合并（PR #4，squash 到 `db4b69f`；远程与本地功能分支均已删除）；新增代码不含密钥，仓库内仍无真实密钥，`.env` 未入库；已跑 `check_source.py` 的密钥与源码检查。
- 遗留问题与下一步：
  1. 教师端知识点掌握度按班聚合尚未实现（当前记忆只对本人开放）。
  2. 资源库在本次追加了 6 条跨模块资料/导览条目（章节导览、极限计算思路图、积分方法选择表等），仍全部 `verified=false`，需复核人确认；外部课件/视频接入后同样要走复核流程。
  3. 异步任务与语音未实现（`capabilities.async_tasks=false`）。
  4. `verified=false` 的知识点与资源条目需课程资料复核人确认后置为 True。

## 2026-09-21 · 桌面窗口入口（可选，不改变现有网页启动方式）

- 日期 / 任务：2026-09-21 / 为本地 Web 应用增加一个可选的“桌面窗口”入口。用户明确要求**新增功能、不得覆盖或改变现有网页版启动方式**（`一键启动.cmd` / `start.ps1` 行为保持原样）。
- 负责人 / AI 工具：待确认（启动器与交付方向）/ Chatbox。
- 开发了什么与原因：应用是 FastAPI + Vue 的本地 Web 应用，双击 `一键启动.cmd` 后由浏览器打开 `127.0.0.1:18080`，没有桌面客户端。本次让同一页面显示在**无地址栏、无标签栏**的应用窗口中，使用体验更接近桌面软件，同时不引入任何新依赖。
  - `desktop.py`：只用标准库。按与 `backend/app/platform/portable.py` **相同**的 instance_id 计算、`backend/launcher-port.txt` 端口文件、`backend/launcher.lock` 单实例锁、health 与首页校验，复用已运行服务，或按同一命令（`portable.py --serve --source --port N`）启动后端；随后用 Edge/Chrome 的 `--app=` 打开窗口，使用独立 profile `.runtime/desktop-profile`（已被忽略）。
  - `start-desktop.ps1`：先调用既有 `bootstrap.ps1` 准备环境，再运行 `desktop.py`；文件保持纯 ASCII，避免 PowerShell 5.1 将无 BOM 脚本按 ANSI 解码。
  - `一键启动(桌面窗口).cmd`：双击入口，风格与既有 cmd 一致（英文提示，仅失败时 pause）。
- **pywebview 实测结论（未采用）**：项目运行时是嵌入式 CPython 3.13.13，不含 setuptools，`pip install pywebview` 只能解析到 3.4（更高版本依赖仅有源码包的 `proxy_tools`，无法构建）；装入后 `import webview` 即令进程以 .NET CLR 异常退出（退出码 `0xE0434352`，来自 pythonnet），属进程级崩溃，Python 层 `try/except` 无法捕获。故改用零依赖的 app 模式窗口，未新增依赖。
- 修改文件：新增 `desktop.py`、`start-desktop.ps1`、`一键启动(桌面窗口).cmd`；同步 `README.md`、`AGENTS.md`、`docs/source-startup.md`；本日志追加。
- 验证命令或方式 / 结果（通过、失败、未执行）：
  - 通过：`python -B -m py_compile desktop.py`；`start-desktop.ps1` 经 PowerShell 解析器检查为 0 错误；两个新文本文件均无 BOM（`.cmd` 仍为纯 ASCII，与既有入口一致）。
  - 通过：`desktop.py --check` 正确识别并复用已在运行的 18080 服务（输出 `Ready: http://127.0.0.1:18080/`），且不开窗。
  - 通过：`start-desktop.ps1 -CheckOnly` 走完整链路（`bootstrap.ps1` 输出正常 + 复用成功）。
  - 通过：双击 `一键启动(桌面窗口).cmd` 打开应用窗口，窗口标题为 `数伴 · 高数学习伙伴`，窗口主进程是该独立 profile 的 msedge 实例（本机 Edge 153.0.4234.48）；命令行窗口在开窗后自行退出。
  - 未执行：另一台 Windows 机器、Windows ARM、Mac/Linux 验证；Chrome 分支未实测（本机先命中 Edge）；未做窗口内逐项点击验收。
- 明确边界（不得当作已有能力）：关闭窗口后后端**继续运行**，与网页版一致（Edge 关窗后其进程本身也可能常驻，属浏览器行为）；**未实现**“关窗即停服务”；不含桌面安装包 / PyInstaller / Tauri / Electron 打包；便携 ZIP 与 `build-portable.py` 不包含该入口。
- 目录结构变更 / 用户确认依据：无新增或重命名目录；三个新文件均在仓库根目录，属 AGENTS.md 第 3 节允许的“既有目录内新增普通文件”，无需额外的目录变更确认。
- 文档同步清单（逐项）：AGENTS.md（目录树 + 项目记忆 + 桌面入口说明）；README.md（新增“可选：桌面窗口入口”）；docs/source-startup.md（新增同类说明）；docs/contracts、docs/architecture 已核对无变化（不涉及接口与模块边界）；`.env.example`、`backend/requirements*.txt`、`frontend/package.json` 无变化（未新增依赖）；本日志追加。
- GitHub 上传 / 密钥状态 / 用户确认依据：本轮待上传提交已扫描，无凭据匹配；用户随后明确要求“推并合并”。分支 `chatbox/desktop-shell` 已推送，创建 [PR #6](https://github.com/844346518scp-coder/agent-school-llm-usts/pull/6) 并以 squash 合并进 main（`c1d7cf5`），本地与远程功能分支均已删除。本次改动不含密钥，未改数据库、依赖与既有 HTTP 接口。按用户明确要求执行，未取得另一成员审阅；常规流程仍建议先 Review 再合并。
- 遗留问题与下一步：关窗即停服务、真正的桌面安装包、便携包集成、Chrome 路径实测、另一台 Windows 实机验收。

## 2026-09-21 · 界面主题（浅色 / 深色）

- 日期 / 任务：2026-09-21 / 用户反馈“设计得不够好，想加一个浅色、深色模式选项”，要求新增主题切换能力。
- 负责人 / AI 工具：待确认（前端表现层）/ Chatbox。
- 开发了什么与原因：前端此前固定为浅色（学生紫 / 教师绿两套强调色），无主题系统。
  - 新增 `frontend/src/shared/theme.ts`：`light` / `dark` / `system` 三态，持久化到 `localStorage` 的 `shuban-theme`，监听 `prefers-color-scheme` 变化，渲染通过 `<html class="dark">` 生效。
  - 新增 `frontend/src/shared/ThemeToggle.vue`：一个按钮循环 浅色 → 深色 → 跟随系统，登录页（`login-top`）与工作台顶栏（`topbar-right`）各放一个；窄屏只留图标。
  - `frontend/index.html`：新增内联脚本，在 Vue 挂载前读取偏好并设置 `html.dark` 与 `color-scheme`，避免首屏闪白。
  - `frontend/src/main.ts`：引入 Element Plus 深色变量与生成的深色样式表。
- 深色样式为什么是生成而不是手写：`frontend/src/shared/style.css` 里有 **335 处颜色字面量、309 个互不相同的值**（几乎没有设计 token），手写深色表必然在下次改界面时失效。
  - 新增 `build-dark-theme.py`：解析 `style.css` 与各组件 `<style>` 块，按属性归类（`background` → 表面、`color` → 文字、`border` → 描边、`box-shadow` → 阴影、`--*` 变量按名称分类），在 HLS 空间做保持色相的变换（表面变暗、文字变亮、描边变暗、阴影转黑），输出 `frontend/src/shared/dark-theme.css`（272 条规则，约 15 KB）。
  - **未改动 `style.css`**（公共文件），浅色模式的渲染路径与改动前完全一致；深色规则全部以 `html.dark` 开头。
- 修改文件：新增 `build-dark-theme.py`、`frontend/src/shared/{theme.ts,ThemeToggle.vue,dark-theme.css}`；修改 `frontend/index.html`、`frontend/src/main.ts`、`frontend/src/App.vue`、`frontend/src/shared/LoginView.vue`；同步文档。
- 验证命令或方式 / 结果（通过、失败、未执行）：
  - 通过：`npm run build`（= `vue-tsc --noEmit` + `vite build`），多次运行均成功（最近一次 13.97s）。
  - 通过：用无头 Edge（`--headless=new --screenshot`）实际截图。深色登录页、深色工作台（教师主题）、浅色工作台三张均已人工查看，配色协调、文字对比度正常。
  - 通过：与**改动前构建**的基线截图逐张对比，浅色工作台外观一致（主题按钮除外）。
  - **发现并修复一处回归**：生成器起初只给逗号列表的第一个选择器加前缀，导致 `html.dark .work-toolbar input, .work-toolbar select{background:var(--surface,#252525)}` 的第二个选择器变成全局规则，浅色下“教学数据范围”下拉框被渲染成深色。已改为逐选择器加前缀，并在生成器末尾加入守卫（任何选择器逃出 `html.dark` 即中止），修复后重新生成并截图确认浅色恢复。
  - 未执行：真实浏览器（非无头）内的逐页点击验收、移动端尺寸验收、其他浏览器验收。
- 目录结构变更 / 用户确认依据：无新增或重命名目录；新增文件都在仓库根目录与 `frontend/src/shared/`（既有目录内新增普通文件）。
- 文档同步清单（逐项）：`AGENTS.md`（目录树 + 项目记忆）；`README.md`（新增“界面主题（浅色 / 深色）”）；`docs/architecture.md`（新增表现层说明）；本日志追加。`docs/contracts`、`.env.example`、依赖锁、`frontend/package.json` 已核对无变化（无新依赖、无接口与数据变更）。
- GitHub 上传 / 密钥状态 / 用户确认依据：本轮待上传提交已扫描，无凭据匹配；用户随后要求“合并”。分支 `chatbox/theme-toggle` 已推送并创建 [PR #7](https://github.com/844346518scp-coder/agent-school-llm-usts/pull/7)，以 squash 合并进 main（`e69d3cc`），本地与远程功能分支均已删除；同时按用户要求删除仓库内两个未跟踪目录 `.minecraft/`、`PCL/`（仅限本项目文件夹，根目录 `PCL.exe` 保留）。未取得另一成员审阅。
- 遗留问题与下一步：尚无独立“主题”设置页（只有按钮）；不支持自定义强调色；插图与图表未做手工深色版本（SVG 内联颜色仍是浅色系）；主题未跨设备同步。界面配色若调整，需重新运行 `python build-dark-theme.py` 并重建前端。

### ͬ��׷���޸�����ɫģʽ��©��ɫ�ؼ��֣��û����棩

�û�����ɫģʽ�·�����**ѧϰ��¼**ҳ����������**�˺�����**ҳ����������ǰ�ɫ��ԭ������ɫ�������������ֻʶ��ʮ��������������`style.css` �е� 3 �� `background:white`��`.outline-button`��`.dialog-form` �ı����ؼ���`.records-toolbar`��û�б�ת�������û�ж�Ӧ����ɫ����

- �޸���`build-dark-theme.py` ���� CSS ��ɫ�ؼ���֧�֣����� 23 ������ɫ�����ȹ�һ��Ϊʮ����������ͬһ�� HLS �任����ͬʱɨ��ȷ��ģ���ڲ�ʹ�� `rgb()` / `hsl()` ��ʽ��Ҳ���������� `style` ��ɫ���ԡ�
- ���ɽ����273 ������ԭ 272��������/��ȫ�Ĺ���ʾ����`html.dark .records-toolbar{background:#252525;color:#c8bfcf;}`��
- ��֤�����¹���������ͷ Edge ��ȡ��ɫ�µ�ѧϰ��¼ҳ���˺�����ҳ��������������������Ϊ��ɫ����ȷ����ɫ�����в���������ǳɫ����������
- δִ�У�����ͷ���������ҳ������ա�

## 2026-09-23 · 三版 UI 设计稿（独立静态原型，未接入 frontend）

- 日期 / 任务：2026-09-23 / 用户要求作为 UI 设计专家，基于现有界面出三版差异化设计稿，面向教师与学生双角色，细节自行决定，格式沿用项目已有界面，要求与市场同类产品有明显创新差异。
- 负责人 / AI 工具：C（当前用户）/ 千问工作助理。
- 开发了什么与原因：现有前端为学生淡紫、教师青绿的单一视觉体系，缺少可横向评审的差异化设计方向。本轮产出一份可在浏览器直接体验的高保真设计稿，用于团队比选设计语言，不改动任何业务代码。
  - 三套设计语言按结构性差异设计，而非换色：
    - V1 极简 · 数学排版：界面按数学讲义排印，导航用 §01–§07 等宽编号替代图标、当前项以 2px 朱红左竖线标示；智能体引用改为正文上标 + 底部虚线脚注；面板标题自动生成 §编号；圆角 0–2px、默认无阴影；标题用衬线体，主区叠 32px 稿纸横线。
    - V2 科技 · 坐标导航：深色仪表盘，侧栏本身是一条渐变坐标轴，导航项为轴上节点，当前项圆点放大发光；统计值用等宽体并带呼吸光点；概览主视觉为带渐变填充的实时曲线，教师端为班级雷达散点；banner 叠扫描线；公式改等宽发光。
    - V3 温和 · 纸本成长：暖纸色手账，章节卡片带半透明胶带且默认轻微旋转、hover 归正抬起；掌握度用「种子 → 发芽 → 成树」成长隐喻替代百分比进度条；主按钮带 3px 实底、按下下沉；提示与输入区改虚线边框；重点用橙色荧光底色而非加粗；标题用楷体。
  - 覆盖四屏：学生·学习概览、数伴智能体（师生共用，文案随角色变化）、教师·教学概览、教师·班级管理；另附账号设置占位屏以示意导航完整。信息架构、导航项、模块划分与文案骨架沿用现有 App.vue / StudentHome.vue / TeacherHome.vue / AgentView.vue，便于评审时与现行界面对照。
  - 顶部为评审控制台（非产品界面的一部分）：设计语言切换、师生角色切换、本版设计说明弹窗（含配色令牌、字体与形态、5 条创新点、适用判断）。
  - 可操作交互：对话屏可发送提问并出现准备中状态、演示标注、引用来源、收藏与开新问题；班级屏可切换班级与教学数据范围、展开成员查看 v1/v2 作答版本与反馈状态；成长隐喻组件按提交率点亮。
- 修改文件：仅新增设计稿目录及其内容（入口 HTML、history/v1 快照与交付版、MANIFEST.md）；未修改 frontend、backend、migrations、tests 下任何文件，未修改 style.css 或任何 Vue 组件。
- 验证命令或方式 / 结果（通过、失败、未执行）：
  - 通过：静态完整性抽验 `check-static-integrity.mjs`，两次运行（生成后、修复后）均 0 BLOCKER / 0 WARN。
  - 通过：交付脚本 `make_delivery.py --version 1` 正常产出单文件交付版，退出码 0。
  - 通过：通读核对 6 项验收标准（REQ-001 至 REQ-006）对应实现均在位。
  - 修复 2 处自身缺陷：① 教师身份下点击「和数伴聊一聊」会落到 student-agent 路由，导致侧栏高亮与面包屑错位，已加角色别名映射并把 teacher-agent 登记为已绘制页；② 样式表存在一组重复且互相覆盖的 `.nav-item.role-*-only` 显隐规则（导航实际由脚本按角色渲染），已清理为单一规则并注明用途。
  - 未执行：真实浏览器运行验收、移动端实机验收、无头截图比对、跨浏览器验收。本轮为设计稿，未启动后端服务，未连接任何真实模型或数据库。
- 目录结构变更 / 用户确认依据：**新增了一个根级目录**（设计稿独立目录，含 history/v1 与 delivery 子目录）。依据：① 本次设计稿任务由用户直接发起并要求交付；② AGENTS.md 第 3 节已存在同类先例——两套 apps-builder 原型目录（拍照搜题、数伴教育智能体前端）即为同技能产出的独立目录；③ 该目录不参与 frontend 构建、不被一键启动引用、不修改任何既有文件，与 main 已纳入的原型目录性质一致。如团队希望改为其他落点或不入库，可直接移动或忽略该目录。
- 文档同步清单（逐项）：
  - AGENTS.md 项目记忆与目录：已更新（新增本轮状态条目；目录树未列入该设计稿目录，与两套既有原型目录的处理方式保持一致，其存在与性质由本日志与项目记忆条目记录）。
  - README.md：已核对，无变化。设计稿为独立评审用原型，不构成新的启动入口或运行方式，README 描述的源码启动、桌面窗口入口与一键启动流程均不受影响。
  - docs/architecture.md：已核对，无变化。未新增模块边界、未改动前后端职责划分，设计稿不属于运行架构的一部分。
  - docs/contracts/ 下全部约定：已核对，无变化。未新增或修改任何接口，设计稿不发起真实请求。
  - .env.example：已核对，无变化，无新增配置项。
  - 依赖锁（backend/requirements.lock.txt、frontend/package-lock.json）：已核对，无变化，未新增任何依赖；设计稿为单文件 HTML，无外部运行时依赖。
  - 其他相关说明、配置模板、计划与图示：docs/source-startup.md、docs/portable-windows.md、docs/simulation-test-cases.md、docs/simulation-test-report.md、docs/c-role-status-report.md、docs/merge-feasibility.md、migrations/README.md 均已核对，无变化（本轮不涉及启动、便携、迁移、模拟验收与合并）。
  - 本日志：已追加本条。
- GitHub 上传 / 密钥状态 / 检查范围 / 用户确认依据：**未上传，未执行 git 任何写操作**。设计稿内容为虚构示例数据（示例学生姓名、班级、作业与统计数字），不含真实凭据、真实学生数据或 API 密钥；未读取 .env，未在产物中保留任何绝对路径。本轮未收到上传授权，故停留在本地。若后续需要上传，须按 AGENTS.md 第 6 节完成可审阅的密钥检查并取得用户当次确认。
- 遗留问题与下一步：
  - 设计稿为静态原型，未接入 frontend/src，未验证在 Element Plus 与 KaTeX 真实组件体系下的可实现性；三版的令牌若要落地，需要先把现有 style.css 的 335 处颜色字面量改造为设计令牌，否则无法整体换肤。
  - V2 为深色优先，与 9/21 已上线的 build-dark-theme.py 生成式深色方案存在两条不同路径，落地前需决定取舍。
  - V3 教师端信息密度偏低，批量批改效率不如 V1/V2；若选 V3，建议教师端另出紧凑变体。
  - 未绘制屏：课程探索、我的作业、复习收藏、学习记录、题库选题、登录页；点击这些导航项会给出「不在本设计稿绘制范围内」的提示。
  - 下一步：等待团队选定方向后，再评估是否改造为 Vue 主题层；本轮不擅自改动现有界面。
## 2026-09-24 · B 模块个人 UI 构思稿入库（未完工的极简UI）

- 日期 / 任务：2026-09-24 / 落实会议要求的「个人 UI 设计图」，把个人构思稿放入仓库供群内集中评审与投票。
- 负责人 / AI 工具：B 模块 / Chatbox。
- 开发内容：根目录新增 `未完工的极简UI/`，内含 `shuban-ui-v1.html`（单文件原型）与 `README.md`（打开方式、设计取向、各板块现状、未完成项）。未改动 `frontend/src`，不参与构建。
- 设计取向与差异：本稿是「一套设计语言 × 三档密度」（精简 / 常规 / 繁杂，只切换 CSS 变量）；C 稿是「三套独立设计语言选一套」。落地成本差异是评审的核心分歧点。
- 覆盖板块：Own（随机参数曲线图案，2D/3D/4D，可拖拽视角）、Agent（对话 / 历史记录，划词提问）、Class（我的班级 / 班级排行榜）、More（功能卡片 / 自主学习 / 功能码）、Setting（视觉设计 / 页面与顺序 / 账号）。
- 验证：单文件在 Edge 逐屏截图确认；浅色与深色、三种风格、主题色与标记色切换生效；Own 拖拽视角、Agent 悬停展开与吸底输入框已实测。
- 未完成：教师端界面；功能清单文档；Own 的「外观」子页；语音输入、视频外链等尚无界面入口。
- 目录结构变更 / 用户确认依据：新增根级目录，由用户明确要求。
- GitHub 上传 / 密钥状态：按用户要求直接提交到 main，未走 PR 与审阅；不含密钥，未改依赖与数据库。
- 附注：本文件先前含非 UTF-8 字节，追加时改用兼容方式写入，建议后续统一为 UTF-8。


## 2026-09-25 · 修复 Agent 输出的 Markdown 兼容（会议第 4 条）

- 日期 / 任务：2026-09-25 / 落实会议功能点「Markdown 兼容：修复 Agent 输出带星号等乱码问题」。
- 负责人 / AI 工具：B 模块 / Chatbox。
- 问题：`frontend/src/shared/MathText.vue` 只把 `$...$` 交给 KaTeX，其余片段用 `{{ }}` 纯文本插值，模型输出的 `**重点**`、`- 列表`、`代码` 会带着符号原样显示（即"星号乱码"）。
- 修复：新增 `frontend/src/shared/markdown.ts`（轻量 Markdown 渲染，不引入新依赖），覆盖标题、有序/无序列表、引用、分隔线、代码块与行内代码/粗体/斜体；`MathText.vue` 的非公式片段改走它。因 MathText 是公共渲染组件，Agent 回答、诊断、阶段总结、费曼复述一次性全部修好。
- 安全：输出经 v-html 插入，故 renderMarkdown 内部先转义 HTML 再替换；KaTeX 保持 trust: false。
- 已知取舍：`> 引用` 需同时匹配转义后的 `&gt;`（已处理）；表格、链接、嵌套列表不做解析，按普通段落显示。
- 验证方式 / 结果：`npm run build`（vue-tsc 类型检查 + vite 打包）通过；把 markdown.ts 的真实代码抽出注入测试页逐项核对 —— 粗体、斜体、有序/无序列表、引用、标题、代码块、分隔线、行内代码均正确，无星号/反引号残留。
- 未完成：表格、链接、嵌套列表未支持；未在真实模型输出上抽样验证（demo 模式回答不含 Markdown 标记）。
- 文档同步：本日志与 AGENTS.md 项目记忆；无依赖与接口变更，未改 frontend/package.json。
- GitHub 上传 / 密钥状态：按用户要求直接提交到 main；不含密钥。

