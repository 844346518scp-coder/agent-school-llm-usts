# 开发日志

采用追加方式保存每次开发与交接记录。不得记录密钥原文或真实学生隐私。

## 记录模板

```text
日期 / 任务：
负责人 / AI 工具：
开发了什么与原因：
修改文件：
验证命令或方式 / 结果（通过、失败、未执行）：
目录结构变更 / 用户确认依据（没有则写无）：
文档同步清单（逐项写已更新，或已核对无变化及原因）：
  - AGENTS.md 项目记忆与目录：
  - README.md：
  - docs/architecture.md：
  - docs/contracts/ 下全部约定：
  - 其他相关说明、配置模板、计划与图示：
GitHub 上传 / 密钥状态 / 检查范围 / 用户确认依据：
遗留问题与下一步：
```

## 2026-09-17 / 初始化协作底座

- 负责人 / AI 工具：Codex；A/B/C 实际人员待认领。
- 开发内容：创建学生端、教师端、平台与 AI 模块的基础目录；编写跨工具共用的 AGENTS.md、README、架构与接口入口、开发日志、环境模板及 Git 忽略规则。
- 原因：三名成员使用的 AI 工具不统一，需要共同规则、持久项目记忆和清晰职责。
- 修改文件：根目录 AGENTS.md、README.md、.gitignore、.env.example；docs 中架构、接口入口、本日志；各空业务目录 .gitkeep。
- 目录授权：用户本次明确要求搭建基本文件夹结构；既有原始素材与 output 保持原位。
- 文档同步：AGENTS.md、README、架构、接口入口与本日志已创建；环境模板仅空值。既有规划图示和素材不因本次目录初始化而改变，保留为历史资料；当前实施状态以 AGENTS.md 为准。
- 验证：已核对新增文件存在、文档相对链接与目录树对应关系；未执行应用测试，因为没有应用代码。
- GitHub / 密钥状态：未上传；本次新增模板无真实密钥；未进行全项目及历史密钥扫描，整体状态为未核验，没有上传确认。
- 下一步：认领分工，落实技术版本与首批接口，再搭建可运行骨架。

## 2026-09-17 / 素材清理执行受阻

- 负责人 / AI 工具：Codex。
- 任务与授权：用户明确要求执行“只保留开发骨架”方案，删除原始素材与 output 两个目录及 10 个素材文件，不另建备份。
- 已完成检查：完整阅读协作规范及相关文档；核实两个目标的绝对路径均位于当前项目内，目录内无符号链接或其他重解析点，文件清单与计划一致。
- 执行结果：递归删除命令被自动审批检查拒绝；改为明确列出 10 个文件逐个删除、再移除空目录的方案同样被拒绝。返回原因均为 blocked by policy，未提供更具体原因。命令未启动，素材未删除。
- 文档同步：本日志追加受阻记录；AGENTS.md、README.md、架构说明已核对，因清理尚未执行，不将目录树及项目状态改写为已清理；接口说明、.env.example 与 .gitignore 已核对，无变化，本次不涉及接口或配置调整。
- 验证：路径与素材清单检查通过；清理验收未通过；未运行应用测试，因为没有应用代码。
- GitHub / 密钥状态：未上传，未执行完整密钥扫描，仍为未核验。
- 下一步：待删除限制解除或用户手动删除这两个目录后，重新核实实际目录并完成项目记忆、目录树、README 和架构决策同步。

## 2026-09-17 / 素材清理完成与文档同步

- 负责人 / AI 工具：用户手动删除素材；Codex 核验并同步文档。
- 授权与完成依据：用户确认“只保留开发骨架”方案，随后告知“删除了”；实地检查确认原始素材与 output 两个目录均不存在，原计划中的 10 个素材文件已不在原路径。本次未创建备份目录。
- 修改内容：AGENTS.md 更新目录树、项目记忆及历史材料可用性说明；README.md 更新当前状态；docs/architecture.md 追加清理决策；本日志追加完成记录。此前“素材清理执行受阻”条目保留为历史，阻塞已通过用户手动删除解决。
- 文档同步：上述四份文档已更新；docs/contracts/README.md、.env.example、.gitignore 已核对，无变化，因为本次未涉及接口、配置或忽略策略；已删除的资料不再作为本地可读取依据。原有协作规则、项目方向、职责边界及技术建议保持有效。
- 验证结果：通过——两个素材目录不存在、15 个应保留文件全部存在、全部 Markdown 相对链接有效；当前目录树与实际目录一致，历史表述仅在明确的历史记录中保留。未运行应用测试，因为没有应用代码。
- GitHub / 密钥状态：未上传；未执行完整密钥扫描，状态仍为未核验。
- 下一步：认领分工，落实技术版本与首批接口，在现有目录中搭建可运行骨架。

## 2026-09-17 / 数伴 0.1 登录与教学学习 MVP

- 负责人 / AI 工具：Codex。用户明确授权搭建可体验页面、两类登录、预设账号、记住密码、智能体窗口，并依据原有讨论自由补充功能、更新文档。
- 开发内容：实现学生浅紫色与教师浅绿色登录 / 工作台；搭建真实 FastAPI 身份校验、PBKDF2 密码摘要、可撤销的 Cookie 会话；记住密码以七天会话实现，不在浏览器保存明文密码；实现课程入口、智能体固定演示问答、KaTeX 公式、收藏 / 搜索 / 历史、实际提问图表、教师发布作业、学生提交及教师查看答案；补充响应式导航、错误提示、加载状态、空状态和本地启动脚本。
- 技术依据：本轮开工发现原始素材目录已恢复，实际读取原始素材/output/pdf/高数学习智能体建设计划书.pdf 第 4 页，并重新读取关联任务。采用 Vue 3、TypeScript、Element Plus、KaTeX、ECharts、FastAPI；默认 SQLite 支持即装即用，SQLAlchemy 保留 PostgreSQL 连接支持。依赖版本锁定在前后端锁文件。
- 目录情况：只在已存在的 frontend、backend、migrations、tests、docs 等目录内添加文件；新增 .venv、node_modules、dist、缓存为已忽略的依赖 / 构建产物，未新增业务目录。恢复的原始素材在本轮开工时已存在，未被本轮移动或删除，已据实更新目录树。
- 修改范围：frontend 中配置、入口与 student / teacher / shared 页面；backend 中依赖、FastAPI 入口、platform / teaching / ai 模块；tests/test_api.py；migrations/README.md；start.ps1、.env.example、.gitignore；全部现行协作文档。
- 验证（通过）：后端 `python -m pytest tests/test_api.py -q` 共 11 项，覆盖两角色登录、错误凭据、角色不匹配、会话有效期 / 撤销、密码摘要、问答持久化与隔离、收藏、未知问题诚实回复、作业发布 / 提交 / 修改、字段校验和写请求头限制。首次测试遇到 Windows 临时数据库文件占用，已增加 engine.dispose 后清理，再次测试通过；剩余 2 条为依赖库弃用提示，不影响测试。
- 验证（通过）：frontend 的 `npm run build` 完成 TypeScript 检查及生产构建；按需引入 Element Plus，最终 UI JS 约 88 KB（gzip 32 KB），最终构建无大分块警告。
- 浏览器验收（通过）：学生登录、固定极限反馈与公式、收藏及刷新 / 重登后的记录；教师登录并发布“演示 · 导数的几何意义”；学生提交演示答案；教师查看真实提交；记住登录后刷新恢复、退出返回登录、错误密码提示；1440×1000 桌面与 390×844 手机断点、手机菜单、登录与作业页面无横向溢出。浏览器未记录 JS error。
- 启动验收（通过）：已实际运行 start.ps1 隐藏启动前后端；健康接口正常；后端提供生产构建首页及 9 项引用资源均为 HTTP 200；生产页面能登录。重启后仍有 1 条已收藏演示对话、2 份作业（1 份初始作业、1 份验收时发布作业），以及验收时学生提交的演示答案，便于继续体验。
- 文档同步：AGENTS.md 更新现状、目录树、授权与下一步；README 更新安装、启动、账号、体验流程、配置与边界；architecture.md 更新实现和决策；contracts/README.md 登记全部已实现接口；migrations/README.md 说明初始化与迁移限制；.env.example 更新实际配置项；.gitignore 补充 SQLite 附属文件；本日志追加。恢复的历史 PDF / 图示已核对为规划参考，未修改原文，计划中的未来能力不视为已实现。
- 未执行 / 限制：未实连 PostgreSQL / pgvector，未接入真实模型、OCR、语音、RAG、Tavily、SymPy、worker 或多班级；没有 Docker Compose 部署与 CI。公开演示账号只用于本地；正式账号管理和公网防护仍待实现。师生招募与组织试用仍暂定。
- GitHub / 密钥状态：未上传，未执行完整项目与历史密钥扫描，状态仍为未核验；本轮无需外部 API 密钥，预设账号密码是公开演示凭据。没有 GitHub 上传确认。
- 下一步：用户体验并调整页面；认领 A/B/C；按接口约定接入真实模型，再落实 PostgreSQL / pgvector、正式认证与多班级权限。
- 补充界面验收：默认较矮桌面窗口中曾出现侧栏底部账号控件被挤出可视区，已为侧栏添加滚动并收紧短窗口间距，实际点击退出后成功返回登录页；移动端收起导航时隐藏其焦点目标。测试过程中发布的作业与作答保留为明确标注的演示记录。

## 2026-09-17 / 根目录一键启动器与目录说明

- 负责人 / AI 工具：Codex。用户要求在根目录制作一键启动器，并解释当前目录结构。
- 开发内容：新增一键启动.cmd，双击调用 start.ps1；增强脚本的端口与依赖检查、健康服务复用、逐服务就绪等待、前后端代理检测、并发互斥及自动打开浏览器。保留 -NoBrowser 供终端检查；后台服务隐藏运行，失败保留启动窗口并提示日志位置。仅当前 PowerShell 子进程设置执行方式，不修改系统执行策略，不自动下载安装依赖或关闭无关进程。
- 修改文件：一键启动.cmd、start.ps1、AGENTS.md、README.md、docs/architecture.md、本日志；没有新增或移动目录。
- 验证（通过）：实际经 Windows PowerShell 5.1 和 CMD 执行冷启动，前后端就绪且自动打开网页，退出码 0；在项目外的 D:/AI 目录再次执行 -NoBrowser，复用相同服务 PID，无重复服务。确认 Node 位于持久 PATH，文档相对链接有效。
- 修复记录：首次检测遇到 Vite 的 text/html 响应未带 charset，Windows PowerShell 5.1 将中文标题按错误编码解码；改为显式读取 UTF-8 响应字节后，冷启动与重复运行均通过。
- 文档同步：AGENTS.md 更新启动入口与文件树；README 更新双击启动步骤、失败排查、源码与运行产物的目录说明；architecture.md 更新启动机制与决策；本日志追加。接口约定、数据库迁移说明、配置模板、忽略规则及历史素材已核对，无变化，因为本次不改变接口、数据库或业务功能。
- 未执行：没有重跑业务 API 测试或前端构建，本次仅改变启动与说明；并发启动锁和端口冲突分支进行了代码核对，未单独模拟验收。
- GitHub / 密钥状态：未上传，未进行完整密钥扫描，仍为未核验。
- 下一步：日常直接双击根目录一键启动.cmd；团队新电脑先依 README 安装依赖，再使用启动器。

## 2026-09-17 / GitHub 首次上传准备（待确认）

- 负责人 / AI 工具：Codex。用户要求将完整项目上传到已创建、名称带 Agent 的 GitHub 仓库。
- 本地准备：初始化 main 分支并暂存 56 个文件，包含源码、依赖锁文件、启动器、文档与 10 个原始素材文件；初始化前无 Git 仓库，因此无本地历史提交需要追溯。尚未创建提交或上传；未改动业务目录。
- 密钥检查：直接读取 Git 暂存区内容，对 53 个文本或可提取文字的文件（含 PDF 与 DOCX XML）扫描常见服务令牌、私钥及带引号的凭据赋值模式，未发现匹配；人工核对 .env.example 为留空与占位符。公开演示账号凭据按 README 保留，不作为真实服务密钥。
- 排除项：本地数据库、环境、依赖、构建、缓存、日志及真实环境配置按 .gitignore 排除；未上传本地会话与用户记录。
- 检查遗漏：原始素材/output/imagegen 下三张 PNG 未做 OCR 或逐图视觉核验；两份 PDF 内嵌图像未做 OCR；模式扫描不能排除未知格式凭据。状态：已检查，待用户确认（包含上述遗漏）。
- 目标核对：已连接 GitHub 账号 scpxxx，但仓库搜索和可访问仓库列表未返回所述 Agent 仓库；尚不能确定准确地址或访问权限。需用户提供仓库链接。
- 文档同步：AGENTS.md 项目记忆、README 与本日志已更新；架构、接口约定、迁移说明、配置模板及忽略规则已核对无变化，因为本次仅准备版本管理与上传；历史素材原文不修改。
- 验证：暂存清单与忽略规则已核对；本次没有业务改动，未重复运行应用测试或构建。用户密钥确认尚未获得，未执行 GitHub 传输。
- 下一步：取得准确仓库地址及用户对真实密钥、模板、历史泄露和未自动核验图片的确认后，检查远端现状，再提交上传并核验。

## 2026-09-17 / GitHub 新账号连接核验

- 已核验账号 844346518scp-coder，仓库 https://github.com/844346518scp-coder/agent-school-llm-usts 为公开仓库，连接具有写权限。
- 只读 fetch 成功；远端 main 为 b4bebb0，已有两个提交，根目录包含 README.md、PCL.exe 与一张 AVIF 图片。未执行、删除或覆盖这些文件；尚未上传本地项目。
- AGENTS.md 已同步账号与仓库现状；其他文档与业务代码无变化。上传前密钥确认仍待用户回复；需明确此含既有文件的仓库确为目标。

## 2026-09-17 / 主动凭据核验与首次项目集成

- 用户明确要求在协作文件中增加 AI 帮助确认 API 密钥和其他秘密凭据，并直接上传、保留远端其他内容；以此作为本次委托检查和上传依据，不推定未来上传均获授权。
- AGENTS.md 补充主动核验、风险处理及明确授权后的执行规则；README 保留原仓库说明全文；架构记录接续远端历史的集成方式。原 PCL.exe 和 AVIF 图片字节不变，不执行程序，不删除历史。
- 检查：58 个暂存文件的文字或二进制可识别令牌模式无匹配；PDF 共 10 页均可提取文字且无嵌入图片，DOCX XML 已检查；三张规划 PNG 视觉检查未发现凭据。配置模板为空值和占位符，演示口令明确公开。本地数据库、真实环境配置、依赖、缓存和日志排除。
- 限制：模式与视觉检查不能保证检出所有秘密；远端已有 EXE 和 AVIF 仅原样继承，不作完整逆向或隐写分析，无新增该类附件。状态：已检查，本次用户委托 AI 核验后直接上传；未发现已知风险。
- 文档同步：AGENTS.md、README、架构及日志更新；接口、迁移、配置模板与素材原文已核对无变化。本次未改业务代码，未重复执行应用测试；暂存差异格式检查通过。
- 提交方式：以远端 main 的 b4bebb0 为父提交，保留原有两个历史提交；本条记录为上传执行前记录，最终结果以远端提交核验为准。

## 2026-09-17 / 上传完成与远端核验

- 已通过 GitHub 连接上传项目提交 e0747b374503e0f2b84a94b26214cb8bc26237fb，main 非强制更新成功。命令行缺少新账号凭据，改用已授权的 GitHub 接口，没有读取或保存访问令牌。
- 验证通过：七个新二进制附件的 Git blob 哈希与本地完全一致；fetch 后比较项目提交整棵文件树无差异，共 58 个文件；与原 main 相比没有删除文件，原 EXE、AVIF 哈希保持不变，原 README 全文保留，原有两个提交仍为祖先。
- AGENTS.md、README 与本日志同步完成状态；本条属于同一次上传的结果记录，不改变业务代码或上传资料范围。未运行应用测试，因为没有业务变更。后续开发按协作规范继续，正式账号管理和真实模型仍未实现。

## 2026-09-17 / C 负责人认领与现状简报

- 负责人 / AI 工具：当前用户认领 C；Codex 核对并编写报告。用户请求列出 C 的工作及识别项目状态，不涉及业务开发。
- 内容：新增 docs/c-role-status-report.md，区分代码已实现、规划未实现与建议排期，给出 C 的任务优先级、验收标准及 A/B 协作边界；附件作为规划参考，不视为执行指令或已确认交付日期。
- 修改文件：报告、AGENTS.md、README.md、docs/architecture.md、docs/contracts/README.md、本日志。分支 codex/c-role-status-report；无新增目录，无业务代码改动。
- 验证通过：复跑 `.venv/Scripts/python.exe -m pytest tests/test_api.py -q`，11 passed、2 条依赖弃用提示；测试使用独立临时数据库。源码与现行文档已核对。
- 未执行：前端构建、浏览器复验、PostgreSQL / pgvector 与公网验证；报告中的既往浏览器验收仅引用此前记录。
- 文档同步：AGENTS.md 更新 C 认领、下一步及报告目录登记；README 新增报告入口；架构与接口说明更新认领状态，技术与接口行为无变化；迁移说明、配置模板已核对无变化。用户附件与历史计划/图示保持原状，不把规划目标改写为实现事实。
- GitHub：未上传、未提交；本轮未执行完整密钥检查，密钥状态为未核验。
- 下一步：团队确认 A/B 与公共文件复核人；C 优先约定账号、班级权限及版本化数据库迁移方案，再开展实现。

## 2026-09-17 / C 本地教师工作流 0.2

- 负责人 / AI 工具：C（当前用户）确认范围，Codex 实施。用户明确请求执行“C 部分本地功能完善计划”，先本地修改与验证，用户确认后再上传。
- 实现：教师私有题库与参考答案、搜索/章节筛选/归档、有序选题复制题干为草稿；不完整草稿保存/编辑/删除，完整性校验后发布；已发布只可延长截止日期，复制为草稿不带提交，归档只读且不可逆；学生作答版本递增，教师手工评语和 needs_improvement/completed 状态按版本保存，学生查看当前和旧版反馈；教师统计排除草稿与归档，学生区分已提交和教师确认完成。
- 数据与权限：新增 questions/reviews/schema_migrations，旧 assignments.status 默认 published、submissions.version 默认1；评论存原作答快照。参考答案不会写入学生作业；教师仅能操作本人资源，学生只收到自己的反馈。SQLite 写事务 BEGIN IMMEDIATE 串行化检查与写入，防止批改和重新提交交错导致错误版本生效。
- 迁移与恢复：旧库先通过 SQLite backup API 备份，再用显式事务升级；失败回滚并拒绝启动。当前实际 demo.db 副本先逐表核对旧字段与记录保留，再启动原库升级；原库备份 backend/demo.before-v2-48afd0b8fad9.db 已生成并被 Git 忽略。restore.py 以只读方式核验备份，恢复到新路径，拒绝覆盖已有数据库。本轮0.2明确仅支持SQLite，PostgreSQL及其迁移后置。
- 修改文件：backend/app/main.py、platform/database.py、teaching/routes.py；frontend/src/App.vue、teacher/TeacherHome.vue、新增teacher/QuestionBank.vue、shared/AssignmentsView.vue、新增shared/FeedbackView.vue、shared/api.ts、shared/style.css、student/StudentHome.vue；前端两份包清单仅更新项目版本至0.2.0，无依赖升级；start.ps1 同步版本健康检查；新增 migrations/upgrade.py、restore.py、tests/test_migrations.py，扩展 tests/test_api.py；以及下述文档与配置说明。
- 分支 / 目录：codex/c-local-teaching；保留此前未提交的C报告与文档修改。只在现有目录新增普通文件，无新增业务目录。依赖、构建、数据库备份均为已忽略运行产物。
- 验证通过：`python -m pytest tests -q` 共19项，含原11项和新增题库保密、草稿隔离、生命周期、截止、跨教师/学生权限、反馈版本、并发批改/提交、统计及迁移/回滚/恢复场景；剩余2条是既有依赖弃用提示。`npm run build` 的TypeScript检查及生产构建通过，无大分块警告。
- 浏览器验收通过：在本地5173页面以师生演示账号跑通题库建题（公式）→选题生成草稿→保存→刷新恢复→发布→学生提交→教师待改进评语→学生重登查看→修改为第二版→旧反馈保留且当前待批改→教师确认完成→统计更新→归档→学生只读查看。另验证手机不完整草稿可保存；桌面界面和390×844手机尺寸的题库、草稿、教师批改及学生历史反馈无横向溢出，视觉检查通过；浏览器未记录脚本错误或警告。
- 启动验收通过：start.ps1 -NoBrowser 隐藏启动前后端，健康检查及前端代理就绪；实际数据升级后原演示记录仍在。当前服务保持本地运行。测试过程中保留明确标注的“演示验收 · 导数定义”题目、“演示验收 · 本地教师闭环”归档作业（两版反馈）、“演示验收 · 待完善草稿”，便于用户验收。
- 曾失败并已修复：实际旧库副本升级后的Windows临时目录清理发现sqlite3连接上下文未关闭文件句柄；改为contextlib.closing后，重复迁移、恢复及临时目录清理全部通过。浏览器批量操作曾碰到弹窗关闭动画与开发热更新重置页面，重新读取状态并等待弹窗关闭后继续，完整流程最终通过。
- 文档同步：AGENTS.md 更新0.2现状、范围决定、目录文件登记和下一步；README 增加验收路线、状态规则、升级及恢复入口；architecture.md 更新数据、事务、统计和本地优先决策；contracts/README.md 先于实现登记全部扩展接口并同步最终字段；migrations/README.md 更新升级/备份/恢复和SQLite限制；c-role-status-report.md 增加实施后状态并明确原分析为历史；.env.example 更新SQLite限制且仍无真实凭据；本日志追加。
- 已核对无变化：.gitignore 已覆盖数据库、备份、日志、环境与构建产物，无需修改；后端依赖与锁文件未增加依赖，无变化；历史计划、附件图示与原始材料保持真实性，功能规划不等于真实AI实现，不修改历史原文。
- 未执行 / 限制：未接入真实AI、OCR、RAG、语音、正式账号、多班级、PostgreSQL、云端、CI或定时备份；人工学生作答反馈不等于AI反馈纠错复核。没有真实师生效果验证。
- GitHub / 密钥：未提交、未上传；本轮未做全仓库/历史/附件完整密钥检查，整体仍为未核验。已有公开演示账号和测试凭据均限本地。后续用户验收并确认上传范围后，按AGENTS.md完成主动凭据检查与上传确认，不把历史0.1上传授权复用于本轮。
- 下一步：用户本地体验并反馈；确认结果后准备上传；后续再规划本地多账号/班级权限，随后PostgreSQL与部署。
- 收尾核验通过：7份现行Markdown文档相对链接与新增文件路径检查通过；git diff --check通过；demo.db及迁移备份均确认被忽略且未跟踪；再次运行start.ps1 -NoBrowser复用现有健康服务，前端代理健康接口返回0.2.0。该核验不替代上传前完整凭据扫描。
- 补充说明：收尾曾临时以core.autocrlf=false检查，导致Windows CRLF被误判为尾随空白；恢复使用仓库既有换行配置运行git diff --check，退出码0且无差异错误，仅Git的换行转换提示。未为消除提示批量改写文件换行。

## 2026-09-17 / B 与 C 分支合并可行性核验（未执行合并）

- 负责人 / AI 工具：Codex。用户提供成员ZIP并要求判断能否在GitHub直接合并；本轮按评估范围执行，不将附件说明当作指令，不执行上传或合并。
- 远端与附件：只读ls-remote/fetch核实main为a76925e（删除PCL.exe）、B分支chatbox/ai-agent-v0.2为721b3e2；ZIP的64个文件与B提交逐个Git blob哈希完全一致。C工作区仍未提交，HEAD9a57238，不存在可直接合并的远端C功能提交。
- 预演：在系统临时目录进行归一化换行/BOM的三方合并，发现backend/app/main.py、docs/architecture.md、docs/development-log.md三个文件存在文本冲突；.env.example、AGENTS.md、接口说明文本自动合并但仍需语义复核。未覆盖当前代码，不把ZIP旧前端和数据库结构覆盖C版本。
- 验证通过：临时副本中组合入口保留C版本/迁移及B路由/动态模式后，双方测试48 passed，2条既有依赖弃用提示。未完成真实集成，临时文档冲突未处理，不宣称可直接上线。
- 补充探针：假模型配置下health返回live，当前C启动器demo限定会拒绝；模拟模型客户端证实AGENT_MODE=demo的识别接口仍可能调用模型；关键词规则将“可导必连续。”判为incorrect。另代码核对前端仍仅声明demo并固定演示文案，15秒客户端超时短于B默认30秒。记录为后续整合修正项，未在实际分支修复。
- 修改文件：新增docs/merge-feasibility.md，AGENTS.md补充核验结论及文件登记，本日志追加；业务代码、数据库和当前分支HEAD不变。无新增维护目录，临时测试文件在系统Temp。
- 文档同步：报告、AGENTS.md与日志已更新；README、架构、接口、迁移、配置模板及历史材料已核对无变化，因为本轮只是评估，B尚未合入，不将本地C能力改写为已接入B。
- 未执行：GitHub合并、上传、提交、真实模型调用、前端重新构建及浏览器联调。未读写本地demo.db，测试使用隔离数据库。
- 凭据：ZIP文本常见令牌/私钥模式未发现真实凭据；连接串匹配为USER/PASSWORD模板。全历史、二进制和图片/PDF等未完整核验，整体状态仍为未核验，不能据此上传。
- 下一步：用户确认开展整合后，先保存C本地提交，再从最新main建立集成分支纳入B/C，处理冲突与语义问题、补测；凭据检查及用户确认后再上传PR，并由另一成员审核合并。


## 2026-09-17 / C 独立分支上传准备与凭据检查

- 负责人 / AI：Codex。用户明确要求以新分支上传当前本地项目；准备 codex/c-local-teaching，保留原9a57238基线。只读fetch确认最新main为4bd8519，已合并B的PR #1；本轮不整合B、不更新main、不部署。
- 范围：27个新增/修改的文本文件，包含C教师流程、迁移、前端、测试、配置模板、C简报和合并评估；完整候选树65个文件。9个既有附件与HEAD逐字节一致；PCL.exe仍为旧基线继承，本次无二进制新增，后续合并应保留main对它的删除。
- 凭据检查：候选工作区全部文件进行常见API令牌、私钥与凭据字面量扫描，未发现真实秘密；认证相关代码为运行时生成/读取，演示账号和测试样例为公开或合成数据，.env.example模型密钥等为空。核实环境配置、demo.db、迁移备份、依赖、构建和日志的忽略规则，均不进入提交。暂存区与新增提交将再次扫描核对。
- 历史/附件：提交前HEAD没有远端未包含的历史提交；本次仅增加C文本提交。附件哈希未变，沿用此前0.1上传时PDF文字/DOCX XML/三张PNG视觉检查结论；本轮不重复视觉检查，不对EXE逆向或隐写作保证。模式检查不能保证检出所有秘密。其他成员ZIP不纳入上传范围。
- 状态：已检查，待用户确认。已提醒“请删除待上传内容中的 API 密钥及其他凭据，并确认密钥状态。”依据AGENTS.md第6节，本次密钥状态确认后才向GitHub传输内容，不复用0.1历史授权。
- 验证：沿用同日当前C代码19项后端测试、TypeScript/生产构建及完整浏览器闭环的通过结果；本轮仅准备提交与同步文档，无业务变更，不重复运行应用测试。暂存差异格式与提交文件检查在提交时执行。
- 文档同步：AGENTS、README、架构、合并评估和本日志同步分支准备及远端状态；接口、迁移、C简报、配置模板、目录树与原始材料已核对，无功能或路径变化，无需另改。历史日志保留原状。

- 提交前复核通过：27个暂存文本文件的令牌/私钥/带口令连接串扫描无匹配，git diff --cached --check退出码0；无新增附件或本地数据。


## 2026-09-17 / C 独立分支上传完成

- 用户确认依据：在收到27文件范围、凭据检查结论与第6节提醒后，用户回复“没有，开始上传”。据此执行本次上传。
- 结果：GitHub新分支codex/c-local-teaching创建成功，功能提交955bbbe41560f3747bf578cc029e9766e5f3b27d，父提交9a57238；不改main或B分支、不部署。命令行认证未完成，终止该次push后使用已连接GitHub接口上传，无需读取令牌。
- 验证通过：远端功能树9bada338c6cc302abff34ca7c8769f515d3d4ef6与本地f332f59的文件树相同；fetch后git diff退出码0，65个文件逐项一致。接口创建提交使提交ID不同，文件内容未变；本地分支已关联远端，原本地提交保留在reflog。
- 文档同步：AGENTS、README、架构、C简报、合并评估和本日志更新完成状态；接口、迁移、配置、原始材料及目录树已核对无变化。这些结果记录属于同一次已确认上传的文档范围，上传前再次检查。无业务变更，沿用19项测试、构建及浏览器验收结果。
- 下一步：在独立集成分支处理B/C冲突及已记录兼容问题，验收后另行审核合并；本次未执行合并。


## 2026-09-18 / Windows免安装测试包与启动诊断

- 负责人 / AI：Codex。用户反馈其他电脑双击失败，截图明确为Frontend dependencies missing；根因是源码分发不携带node_modules及Python运行环境，原启动器要求开发环境。截图不证明Node或Python一定未安装，只证明前端依赖检查未通过。
- 分支/范围：从已上传C分支创建codex/portable-windows。新增根目录build-portable.py、backend/app/platform/portable.py、docs/portable-windows.md、tests/check_portable.py，修改start.ps1、health可选实例字段及忽略规则。未与B整合、未上传GitHub。无新增维护目录；dist和.tmp为已有忽略规则覆盖的构建/验收产物。
- 实现：开发机校验Python锁定包版本并构建前端，按允许列表打包CPython3.11.15完整运行时/标准库/原生DLL/依赖及许可证、前端静态资源、启动脚本和说明。运行时以_pth隔离系统Python和用户扩展，不复制.venv；测试端无需Node、pip或联网安装。仅复制应用.py和必要构建文件，不携带本机.env、数据库/会话、日志、Git、原始附件、PCL.exe或node_modules。
- 启动：有portable-manifest.json时使用包内runtime，默认127.0.0.1:8765（-Port可调），后台启动FastAPI并托管静态页面；校验本目录instance_id、页面和端口，文件锁防并发、重复启动复用，冲突明确报错且不结束别的进程。固定本包backend/demo.db，避免继承系统DATABASE_URL。源码模式仍使用8000/5173，缺依赖时提示测试者改用便携包。
- 纠正：初查曾怀疑main.py的静态路径；按实际父目录层级复核后确认原parents[2]正确，保留原实现，未作路径修复。
- 验证通过：TypeScript检查、生产构建；后端19项测试通过（2条既有弃用警告）；内置解释器导入检查。实际ZIP解压至中文/空格路径，在移除Python/Node的子进程PATH并设置无效PYTHONHOME/PYTHONPATH、外来DATABASE_URL后，逐文件哈希、无数据库/.env、首次启动、静态资源、登录、草稿保存、结束并重启服务后恢复、重复启动复用及外部端口占用拒绝全部通过。测试只结束自己创建的后端进程，不触碰原8000服务或本地demo.db。
- 曾失败及处理：首轮pytest受C盘剩余空间为0影响，出现SQLite database or disk is full；将当前测试进程TEMP/TMP改为D盘已忽略.tmp后，19项全部通过。未清理用户文件或修改全局环境变量。
- 产物：dist/shuban-windows-x64-20260918-082545.zip，29,513,315字节（约28.1MiB），SHA256为2921093b9809c8efe59170aee433f981608d165239ae9d5afeee64c5cddd7c39。配套.sha256文件和包内portable-manifest.json记录版本、来源提交/未提交状态及每个文件哈希。验收使用ZIP副本，原ZIP没有运行数据。
- 文档同步：AGENTS更新状态与目录文件登记，README新增测试者分发方式，architecture登记运行机制，contracts登记health可选字段，portable-windows说明构建、测试、端口和升级。迁移说明、配置模板、C简报、历史合并评估和原始资料已核对无变化：数据模型/业务范围/B-C整合状态均未变，历史报告不重写。
- 限制：当前验证为本机隔离环境模拟未安装开发工具，并非另一台干净Windows实机；Windows10/11 x64为目标，ARM/Mac/Linux及学校管理策略未验证。包是当前C版，B未集成；无真实AI或跨电脑数据同步。GitHub源码ZIP仍不含运行环境，需要把此便携ZIP作为测试分发物另行发送/发布。

- 追加修正：第一版ZIP在CMD复验中发现Windows预留8728–8827导致8765/8768绑定报10013。已改为自动模式优先18080、记住成功端口，并在占用/系统保留时使用系统分配可用端口；显式指定不可用端口则明确报错。无需关闭其他应用或更改系统预留规则。
- 第一版082545包已被083356最终候选替代，勿分发旧包。最终候选dist/shuban-windows-x64-20260918-083356.zip为29,513,577字节，SHA256：4da7edc6d91defe396aa37a24abf0fbe50cbafd78701a0b857ab467f1c8719b9。应用/前端/启动脚本常见令牌私钥模式无匹配；ZIP清单无.env、数据库、日志或.git。第三方运行时未做逆向/隐写审计。

- 最终验收通过：对083356 ZIP重新执行tests/check_portable.py，含此前全部场景及自动换端口、记住端口、实际CMD入口复用，全部通过；前端再次构建和包内导入通过。Python语法检查、git diff --check、文档相对链接及忽略规则核验通过。最终可交付上述083356包；源代码保持本地未提交、未上传。

## 2026-09-17 · B 模块（智能体）最小版：真实模型接入 + 课程检索引用 + 步骤反馈 + 基础诊断

- 日期 / 任务：2026-09-17 / 对齐 9/20 最小版：课程问答 · RAG 引用 · 拍照识别与确认 · 步骤反馈 · 错题收藏 · 历史记录。
- 负责人 / AI 工具：B 模块成员（待填）/ Chatbox。
- 开发了什么与原因：
  - 把只有固定演示文案的问答升级为“配置 → 课程检索 → 分层提示 → 模型调用 → 如实降级”的骨架；没有模型密钥时仍走完整流程，但只返回演示内容并标注 `mode=demo`。
  - 新增课程检索（本地 BM25，中文按字 bigram）与引用编号，解决“回答没有资料来源”的问题；知识点全部标记 `verified=false`，待课程资料复核。
  - 新增步骤反馈接口：只判断学生本次提交的一步，不返回整题答案；demo 模式只标错、不说“正确”。
  - 新增基础诊断接口：规则给出薄弱知识点与变式练习，结论可追溯到命中的关键词。
  - 新增能力探针 `/api/agent/status`，便于前端与测试脚本判断当前是 demo 还是 live。
- 修改文件：
  - 新增 `backend/app/ai/config.py`、`knowledge.py`、`prompts.py`、`llm.py`、`diagnosis.py`。
  - 修改 `backend/app/ai/service.py`（保留原演示文案与既有响应结构，新增 `/api/agent/*`）。
  - 修改 `backend/app/main.py`（注册 `agent_router`；`/api/health` 的 `agent_mode` 改为如实反映生效模式）。
  - 新增 `tests/test_agent.py`、`tests/smoke_agent.py`。
  - 修改 `.env.example`（新增 `AGENT_MODE`、`MODEL_VISION_NAME`、超时与检索参数，全部为空值或默认值）；修改 `.gitignore`（忽略冒烟输出）。
- 验证命令或方式 / 结果（通过、失败、未执行）：
  - `python -m pytest -q` → **40 passed**（含原有 11 项 API 测试全部通过）。
  - `python tests/smoke_agent.py` → **通过**：登录、`/api/agent/status`、`/api/agent/ask`、`/api/agent/feedback`、`/api/agent/diagnosis`、`/api/agent/ask/stream`（SSE 3 帧）行为符合预期；`/api/agent/recognize` 按设计返回 503（未配置模型，不返回编造结果）。
- 目录结构变更 / 用户确认依据：**无新增目录**，只在既有 `backend/app/ai/` 与 `tests/` 下新增文件。
- 文档同步清单（逐项写已更新，或已核对无变化及原因）：
  - AGENTS.md 项目记忆与目录：已追加“B 模块（智能体）实现说明（2026-09-17）”。
  - README.md：已核对无变化（本次不涉及启动方式与仓库总览）。
  - docs/architecture.md：已追加“智能体（B）处理流水线（2026-09-17 追加）”。
  - docs/contracts/ 下全部约定：已更新 `/api/health` 行与“仍待定义”段，并追加“智能体接口 v0.2（B 模块）”。
  - 其他相关说明、配置模板、计划与图示：已更新 `.env.example`、`.gitignore`；无新增图示。
- GitHub 上传 / 密钥状态 / 检查范围 / 用户确认依据：**未上传**；新增配置项全部为空值或默认值，仓库内无真实密钥，`.env` 未入库。
- 遗留问题与下一步：
  1. 课程知识点内容需课程资料复核，复核后把 `knowledge.py` 中对应 `verified` 置为 True。
  2. 配置真实模型（`MODEL_BASE_URL` / `MODEL_API_KEY` / `MODEL_NAME`）后重跑 `tests/smoke_agent.py`，确认 live 链路与超时降级。
  3. 前端（A）按 `docs/contracts/README.md` 的 v0.2 接入 `mode`、`references`、`/api/agent/feedback`、识别确认流程。
  4. 9/21–24 再补：长期记忆、推荐练习的个性化、资源检索与总结评价、语音。

## 2026-09-18 / 普通源码自动初始化与 B/C 本地集成完成

- 负责人 / AI：Codex。用户明确纠正为“其他人下载原本文件夹即可运行，而不是打包版”，并要求合并B、处理兼容性与更新文档。本条替代前一便携包方案作为默认分发方式；历史便携工具和记录保留。
- 基线：用户提供的agent-school-llm-usts-main.zip与origin/main提交4bd8519的64个Git文件逐字节一致；main已包含B提交721b3e2。先把既有便携工作保存为本地40df412，再建codex/source-bootstrap-integration，合入origin/main。保留C全部教师功能与数据迁移，保留远端PCL.exe删除；入口、架构、接口和日志冲突合并双方内容。本次只保存本地合并提交，没有push或改动远端main。
- 启动修复：新增根目录bootstrap.ps1，项目内下载并SHA256校验固定Python3.13.13、Node22.23.2、pip26.2.1；安装后端锁定依赖与npm ci，按源码指纹构建前端。start.ps1默认自动初始化后同源启动，-Dev保留原开发模式，-SetupOnly仅准备环境。首次联网、缓存复用，无管理员、系统PATH或全局开发工具要求。全部运行环境、缓存和临时文件位于已忽略生成目录；未新增维护目录。
- 兼容修复：main同时保留C数据库初始化和B路由，health支持demo/live及agent_version；启动器不误复用旧C服务。前端AI等待90秒，模型网络超时限制1–60秒；错误不自动重试。问答和历史按每条回复来源标识模型/演示，显示引用、待复核与降级原因；来源前缀避免模型引用演示字样时被误判。demo禁止识别/模型调用，关键词步骤反馈仅unclear；SSE中途失败以replace标记替换未完成输出。模型错误不回显提供商响应体/凭据。数据库版本仍为2，无新迁移。
- 涉及文件：bootstrap.ps1、start.ps1、platform/portable.py、main.py、B模块config/llm/diagnosis/service及其合入文件，前端App/StudentHome/AgentView/RecordsView/api/style，tests/check_source.py/test_api.py/test_agent.py/smoke_agent.py，build-portable.py及下列文档/忽略配置。冒烟脚本改为独立临时库且强制demo，避免改动实际数据或调用用户模型。
- 验证通过：原C19项+B29项共48项首先通过；新增9项兼容回归后，在自动下载的Python3.13环境完整57项通过（2条既有弃用警告，268.91秒）。来源前缀最后调整后针对性回归1项通过。独立B冒烟覆盖登录、状态、问答、反馈、诊断、SSE三帧与识别预期503通过。TypeScript及Vite生产构建通过。
- 源码验收通过：tests/check_source.py从受版本管理源码建立不含.env/数据/依赖/构建的新副本，在无Python/Node的PATH及无效PYTHONHOME/PYTHONPATH下，真实下载安装构建、登录、私有题库、草稿发布、学生提交、教师反馈、B演示问答引用、静态资源、重复启动复用、重启持久化均通过；禁用网络代理后缓存启动通过，live使用本地假模型验证。最终bootstrap再复制到中文/空格验收目录，以实际一键启动.cmd复验成功并核验health，结束该验收自身PID。未对其他电脑实机作已验证声明。
- 浏览器验收通过：本地18080页面登录、问答、公式、引用待复核、刷新保留登录及历史来源标记正常；保留原数据库记录，并新增一条明确的集成验收演示问答。源代码服务继续在18080供用户体验，原8000后端未终止。旧Vite/esbuild因占用本项目依赖，核对所属PID后停止并重新安装；不终止无关进程。
- 失败与修正：npm ci曾因本项目旧esbuild锁定报EPERM，停止所属开发服务后重装成功；首次隔离冷启动因继承PSModulePath导致Windows PowerShell找不到Get-FileHash，改为进程内补全系统模块路径后通过。嵌入式Python相对_pth导致pytest无法导入backend，改为每次生成UTF-8绝对项目/依赖路径，导入及针对性测试通过。冒烟新增断言曾误将创建记录的201当作200，按既有接口约定修正后通过。C盘无剩余空间，所有本轮安装/测试临时目录使用项目盘，未清理用户文件。
- 文档同步：AGENTS（状态、目录、下一步）、README（源码使用与边界）、docs/source-startup（新）、architecture、contracts、merge-feasibility、c-role-status-report、portable-windows、migrations/README、.env.example和本日志均已更新；.gitignore覆盖生成物。其他原始PDF/DOCX/图示已核对无变化，属于历史依据，不改写为当前实现。B原始日志“关键词标错/薄弱结论”等为历史行为，以本条unclear/候选复核说明为现行规则；原smoke真实模型建议已被当前隔离演示脚本替代。文档相对链接、冲突标记、PowerShell语法、差异格式与忽略规则在提交前核对。
- 限制/下一步：尚未执行另一台Windows实机、真实模型效果、正式身份/多班级、PostgreSQL或云部署验收。首次需要可访问下载源且项目目录可写；Windows10/11 x64是自动启动目标，其他系统未支持。此次未做新的完整上传凭据审计、未上传；现有GitHub旧ZIP不会自动修复，后续确认上传范围并履行凭据检查/确认后才发布。当前本地可运行与远端main状态须区分。

## 2026-09-18 / 原 C 分支更新准备与凭据核验

- 用户要求“提交到原来的github分支上”；目标为origin/codex/c-local-teaching，fetch核实仍为f841e01，且是集成提交b8d6fdf的祖先，可以非强制快进；不修改main。
- 待上传范围：源码自动初始化、B/C集成及兼容修复、测试与文档，以及之前本地保存的可选便携工具；对原C分支35条文件变化（含保留远端PCL.exe删除）。不上传运行包、.env、数据库、缓存、日志和安装环境。
- 凭据检查：78个跟踪文件及43个尚未被origin引用的历史blob，共113次文本模式扫描，API令牌、私钥、带凭据URL无匹配；模板敏感配置为空，测试值为合成值，公开演示账号保留。暂存区原为空；此条及AGENTS状态更新提交前复核。8个既有二进制附件与远端C内容相同，无新增附件；本轮未重新作视觉或隐写审计，不能保证自动扫描覆盖所有载体。B既有提交已在远端main，本轮当前文件仍纳入扫描。
- 验证：工作区原干净、远端祖先关系及忽略规则通过；本轮仅上传准备和状态记录，沿用上一条57项测试、源码冷启动及构建结果，不重复应用测试。
- 文档同步：AGENTS与本日志更新准备状态；README、架构、接口、迁移、启动说明和历史材料已核对，无功能变化，现有“尚未上传”仍准确，无需改写。
- 状态：已检查，待用户确认。按AGENTS第6节提示删除待上传内容中的API密钥及其他凭据并确认状态；前次C上传确认不覆盖此次新增源码/历史范围。尚未传输内容，确认后再更新同一分支。
